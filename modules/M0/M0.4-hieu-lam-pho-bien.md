---
title: M0.4 — 10 Hiểu Lầm Phổ Biến Phá Hoại Dự Án Next.js
description: Phân tích nguồn gốc, hệ quả production và mental model đúng cho 10 hiểu lầm nền tảng về Next.js — từ "Next.js chỉ là React + Router" đến caching và kiến trúc migration.
outline: deep
tags:
  - nextjs
  - mental-model
  - app-router
  - server-components
  - caching
  - migration
  - architecture
---

# M0.4 — 10 Hiểu Lầm Phổ Biến Phá Hoại Dự Án Next.js


## 1. Lesson Goal

Đây là bài cuối của Module M0 — đóng vai trò **radar**, không phải **thuốc**. Sau bài này, bạn cần làm được:

- Nhận ra khi MỘT TRONG 10 hiểu lầm này đang điều khiển một quyết định — của bạn, đồng nghiệp, hoặc chính đoạn code bạn đang đọc. Mỗi hiểu lầm sẽ có một "câu nói báo động" cụ thể — khi nghe thấy câu đó trong standup/code review, bạn biết phải dừng lại.
- Giải thích được **tại sao** hiểu lầm đó "có lý" trong một ngữ cảnh nào đó. Không ai "ngu" khi tin vào những điều này — họ đang áp dụng một mental model ĐÚNG-Ở-QUÁ-KHỨ vào HIỆN-TẠI. Hiểu được nguồn gốc giúp bạn sửa tận gốc, không chỉ "sửa triệu chứng".
- Dùng **"Lăng kính 3 chiều"** (mental model của bài này) để tự kiểm tra bất kỳ assumption MỚI nào về Next.js trong tương lai — không chỉ 10 cái này.
- Biết **module nào sẽ "trị" tận gốc** từng hiểu lầm (M2, M6, M7, M15...) — bài này chỉ chẩn đoán, việc điều trị nằm ở các module chuyên sâu.

Lưu ý quan trọng: cả 10 hiểu lầm dưới đây đều **từng là sự thật** ở một thời điểm/context nào đó của Next.js. Đó chính là lý do chúng nguy hiểm — chúng không phải "sai hoàn toàn", mà là "đúng nhưng đã hết hạn" hoặc "đúng một phần nhưng thiếu điều kiện áp dụng".



## 2. Evolution Context — "3 Lớp Di Sản Tư Duy"

Next.js đã trải qua ít nhất **3 lần đổi mô hình tư duy** (paradigm shift) lớn. Mỗi lần đổi, một lớp kiến thức cũ — từng đúng, từng được dạy rộng rãi — bị bỏ lại phía sau. Nhưng internet không "quên" theo kịp framework: blog cũ, course cũ, video YouTube cũ, và cả câu trả lời của AI model được train trên dữ liệu cũ vẫn tiếp tục lan truyền lớp kiến thức đó như thể nó còn đúng.

**Lớp 1 — Pre-Next.js / CSR React thuần** (~trước 2020, nhưng vẫn là mental model mặc định của hầu hết React dev mới)
> Niềm tin nền: "Component = hàm JavaScript chạy trong browser, render ra DOM, hết chuyện."
> Đúng 100% trong thế giới Create React App / Vite SPA.

**Lớp 2 — Pages Router Next.js (9–12, ~2020–2022)** — vẫn là **MAJORITY content** trên internet tính đến hiện tại
> Niềm tin nền: "Data fetching = các function export đặc biệt (`getServerSideProps`, `getStaticProps`), còn component vẫn là pure React nhận `props` như bình thường."
> Đúng 100% trong kỷ nguyên Pages Router — và đây là lý do M2 (Next.js Kế thừa) tồn tại như một module riêng.

**Lớp 3 — Early App Router (13–14, ~2023)** — giai đoạn "cả cộng đồng vừa học vừa dạy lại nhau"
> Niềm tin nền: "App Router = đổi thư mục + Server Components = server-side nên nhanh hơn, dùng cho tất cả + `'use client'` = code chạy ở client."
> Đây là giai đoạn các **oversimplification** lan truyền mạnh nhất — vì độ chính xác trung bình của nội dung "App Router tutorial" thời điểm đó rất thấp, nhưng số lượng lại rất nhiều.

Mỗi hiểu lầm trong bài này sẽ được gắn nhãn **"bắt nguồn từ lớp nào"** — để bạn thấy rõ: không phải bạn "không học kỹ", mà là bạn đang mang một bộ giả định từ một kỷ nguyên khác sang kỷ nguyên hiện tại.

Riêng **Nhóm D (Caching)** có một tính chất khác biệt: đây không chỉ là "tư duy cũ" mà là **"tư duy đúng-ở-version-trước, sai-ở-version-sau"** — vì *default behavior* của caching đã thay đổi qua các major version (13 → 14 → 15 → 16). Đây là loại hiểu lầm "động", cần re-verify liên tục — sẽ quay lại sâu ở M7.



## 3. Mental Model — "Lăng Kính 3 Chiều"

Đây là công cụ chính của bài học, và cũng là công cụ bạn dùng **cho mọi bài học sau này** trong chương trình. Với BẤT KỲ API, pattern, hay đoạn code Next.js nào, hãy hỏi 3 câu:

### 🔍 Chiều 1 — Vị trí thực thi (Execution Location)
> *Đoạn code này chạy ở đâu, và khi nào?*

- **Server hay Client?** (Không phải "trông giống React" là chạy ở client)
- **Build-time, Request-time, hay cả hai?** (SSG vs SSR vs ISR)
- **Node.js runtime hay Edge runtime?** (khác nhau về API khả dụng)

### 🔍 Chiều 2 — Vòng đời Cache (Cache Lifecycle)
> *Kết quả này được lưu ở đâu, tồn tại bao lâu, và ai/khi nào xoá nó?*

- Request Memoization? Data Cache? Full Route Cache? Router Cache (client)?
- Điều gì **trigger invalidation** — thời gian (`revalidate`), tag (`revalidateTag`), path, hay một lần deploy mới?

### 🔍 Chiều 3 — Tính ổn định theo Version (Version Stability)
> *Mental model này dựa trên hành vi MẶC ĐỊNH của version nào — và default đó còn đúng ở version bạn đang dùng không?*

---

**Cách dùng:** mỗi hiểu lầm dưới đây sẽ được đánh dấu **vi phạm chiều nào**. Bạn sẽ thấy một pattern lặp lại: phần lớn lỗi tư duy nghiêm trọng về Next.js đều quy về việc **collapse 3 chiều này thành 1** — coi mọi đoạn code đều "chỉ là JavaScript, chạy ở đâu đó, lúc nào đó, luôn giống nhau".



## 4. Core Concepts — 10 Hiểu Lầm

### Nhóm A — Hiểu lầm về bản chất framework

---

### Hiểu lầm #1: "Next.js chỉ là React + Router + SSR — học React kỹ là đủ"

🏷️ **Vi phạm:** Chiều 1 (Execution Location) + Chiều 2 (Cache Lifecycle)

📜 **Nguồn gốc:** Lớp 1 (CSR React)

🚨 **Câu nói báo động:** *"Tôi viết React giỏi rồi, Next.js chỉ là thêm routing với SSR thôi, có gì khó."*

**❌ Niềm tin:**

Next.js là một "lớp vỏ" mỏng bọc ngoài React — thêm file-based routing, thêm khả năng render trên server, nhưng bản chất component vẫn là React component bình thường, tuân theo đúng quy tắc React mà bạn đã biết.

**✅ Sự thật:**

Từ App Router, Next.js định nghĩa **mô hình thực thi riêng của nó** — không phải "React chạy ở 2 nơi", mà là **hai runtime khác nhau với quy tắc khác nhau, chia sẻ một cú pháp giống nhau**. Server Components là `async function` chạy trên server, không có state, không có lifecycle, không có event handler — đây không phải "React thiếu tính năng", mà là một **execution model khác hẳn**, có quy tắc riêng (data fetching trực tiếp trong component, serialization boundary khi truyền props sang Client Component, v.v.)

**💻 Minh chứng:**

```tsx
// app/dashboard/page.tsx
import { useState } from 'react'

export default async function DashboardPage() {
  // 💥 Lỗi: useState chỉ hoạt động trong Client Component
  const [filter, setFilter] = useState('all')

  const data = await fetchDashboardData(filter)
  return <div>{/* render data */}</div>
}
```

Với mental model "Next.js = React + Router", lỗi này **vô lý** — "tôi vẫn viết React đúng cú pháp mà, sao lỗi?". Nhưng với mental model đúng (Chiều 1: đây là Server Component, chạy trên server, không có vòng đời client), lỗi này hoàn toàn logic: `useState` đòi hỏi một instance sống trên client để giữ state qua các lần render — thứ không tồn tại trong execution model của Server Component.

**💥 Hệ quả production:**

Dev mang mental model React thuần sẽ debug **sai tầng**. Gặp lỗi, họ tìm "React hook rules", "tại sao state không update" — trong khi vấn đề thực sự là "đoạn code này đang chạy ở runtime nào". Hậu quả: hàng giờ debug sai hướng, và khi "fix" được (thường bằng cách thêm `'use client'` bừa — xem Hiểu lầm #6), root cause vẫn chưa được hiểu, tích lũy thành nợ kỹ thuật.

---

### Hiểu lầm #2: "Code/pattern từ tutorial cũ (Next 9–12) áp dụng được nguyên vẹn cho version mới"

🏷️ **Vi phạm:** Chiều 3 (Version Stability)

📜 **Nguồn gốc:** Lớp 2 (Pages Router) — đây là lý do M0.2 (Nhận diện Phiên bản) và toàn bộ M2 tồn tại

🚨 **Câu nói báo động:** *"Tôi follow đúng tutorial mà, copy y nguyên code mà không chạy."*

**❌ Niềm tin:**

Next.js là Next.js — API có thể đổi ít, nhưng "cấu trúc cơ bản" (data fetching qua function export, component nhận props) là bất biến. Một tutorial "Next.js Data Fetching" từ 2021 vẫn dạy đúng cách Next.js hoạt động hôm nay.

**✅ Sự thật:**

`getServerSideProps`, `getStaticProps`, `getStaticPaths` — toàn bộ API "lấy dữ liệu qua function export" của Pages Router **không tồn tại** trong `app/` directory. Đây không phải "đổi tên hàm" — đó là một family API hoàn toàn khác, gắn với một routing convention khác (`pages/` vs `app/`). Phần nguy hiểm nhất: code "trông giống hợp lệ" — TypeScript/Next.js không luôn báo lỗi rõ ràng, chúng chỉ... bị **ignore**.

**💻 Minh chứng:**

```tsx
// app/products/page.tsx
// ❌ Pattern copy từ tutorial Pages Router 2021
export async function getServerSideProps() {
  const res = await fetch('https://api.example.com/products')
  const products = await res.json()
  return { props: { products } }
}

export default function ProductsPage({ products }) {
  // 💥 products luôn là `undefined`
  // Next.js KHÔNG gọi getServerSideProps trong app/ — đây chỉ là một export vô nghĩa
  return <ul>{products.map(p => <li key={p.id}>{p.name}</li>)}</ul>
}
```

```tsx
// app/products/page.tsx
// ✅ App Router (Next 13+): Server Component tự fetch trực tiếp, không cần export đặc biệt
export default async function ProductsPage() {
  const res = await fetch('https://api.example.com/products')
  const products = await res.json()
  return <ul>{products.map(p => <li key={p.id}>{p.name}</li>)}</ul>
}
```

**💥 Hệ quả production:**

Đây CHÍNH XÁC là nguồn gốc của câu phàn nàn phổ biến nhất: *"tôi follow tutorial mà không chạy"*. Vì không có error rõ ràng (TypeScript có thể vẫn pass nếu không strict), dev sẽ rơi vào vòng lặp "copy code khác từ AI/forum → vẫn không chạy → copy thêm → codebase trở thành Frankenstein trộn 2 kỷ nguyên" — chính là chủ đề của M2.9 (Legacy Project Anatomy).

---

### Nhóm B — Hiểu lầm về Migration Pages Router → App Router

---

### Hiểu lầm #3: "App Router chỉ là đổi `pages/` → `app/`, logic fetch data giữ nguyên"

🏷️ **Vi phạm:** Chiều 1 (Execution Location) + Chiều 3 (Version Stability)

📜 **Nguồn gốc:** Lớp 3 (Early App Router messaging — "App Router = thư mục mới")

🚨 **Câu nói báo động:** *"Migrate dễ thôi, mình đổi tên folder rồi sửa import là xong."*


**❌ Niềm tin:**

Sự khác biệt giữa Pages Router và App Router chủ yếu là **routing convention** — `pages/blog/[slug].js` → `app/blog/[slug]/page.tsx`. Logic bên trong (data fetching, component structure) có thể giữ nguyên, chỉ cần "đổi vỏ".

**✅ Sự thật:**

Routing chỉ là phần **nhìn thấy được** của thay đổi. Bên dưới là một thay đổi sâu hơn nhiều: rendering model chuyển từ "Client Component nhận props từ một special function" sang "Server Component tự fetch async, có thể stream, có layout không re-mount khi navigate". Đây là lý do `getStaticPaths` → `generateStaticParams`, và `params`/`searchParams` trong App Router (Next 15+) là **Promise** cần `await` — một breaking change phản ánh đúng việc rendering model đã đổi, không chỉ là rename.

**💻 Minh chứng:**

```tsx
// pages/blog/[slug].js — Pages Router
export async function getStaticPaths() {
  const posts = await getAllPosts()
  return {
    paths: posts.map(p => ({ params: { slug: p.slug } })),
    fallback: 'blocking',
  }
}

export async function getStaticProps({ params }) {
  const post = await getPostBySlug(params.slug)
  return { props: { post }, revalidate: 60 }
}

export default function BlogPost({ post }) {
  return <article>{post.content}</article>
}
```

```tsx
// app/blog/[slug]/page.tsx
// "Migrate" kiểu chỉ đổi folder — giữ nguyên 2 export trên
export async function getStaticPaths() { /* ... */ }   // 💥 Next.js không gọi
export async function getStaticProps({ params }) { /* ... */ } // 💥 Next.js không gọi

export default function BlogPost({ post }) {
  return <article>{post.content}</article> // 💥 post là undefined
}
```

```tsx
// app/blog/[slug]/page.tsx
// ✅ App Router đúng cách
export async function generateStaticParams() {
  const posts = await getAllPosts()
  return posts.map(p => ({ slug: p.slug }))
}

export const revalidate = 60

export default async function BlogPost({
  params,
}: {
  params: Promise<{ slug: string }> // Next 15+: params là Promise
}) {
  const { slug } = await params
  const post = await getPostBySlug(slug)
  return <article>{post.content}</article>
}
```

**💥 Hệ quả production:**

Migration được estimate dựa trên "số file cần move" — vài ngày. Thực tế, mỗi page cần **re-architect data flow**, không chỉ move. Estimate "2 tuần" thành "2 tháng", và nếu deadline cố định, team sẽ làm "migrate nửa vời" → dẫn thẳng tới Hiểu lầm #4.

---

### Hiểu lầm #4: "Migrate từng route lẻ tẻ, không cần kế hoạch tổng thể, không ảnh hưởng phần còn lại"

🏷️ **Vi phạm:** Chiều 1 (Execution Location — shared code giữa 2 paradigm)

📜 **Nguồn gốc:** Lớp 2 + Lớp 3 (incremental adoption được marketing đúng ở cấp độ ROUTE, bị hiểu sai ở cấp độ SHARED CODE)

🚨 **Câu nói báo động:** *"Next.js support cả 2 router cùng lúc mà, mình cứ migrate dần từng page, không sao."*


**❌ Niềm tin:**

Next.js CHO PHÉP `app/` và `pages/` cùng tồn tại — đây là tính năng chính thức, được document rõ ràng cho incremental migration. Vậy migrate "page by page", mỗi page độc lập, không ảnh hưởng gì tới các page khác.

**✅ Sự thật:**

Đúng ở cấp độ **route** — `app/` và `pages/` co-exist được. Nhưng **shared code** (auth helpers, layout components, middleware, data access layer) thường được viết cho MỘT execution model. Khi route đầu tiên migrate sang App Router, shared code đó phải **support cả 2 paradigm cùng lúc** — tạo ra "dual maintenance tax": mọi thay đổi logic phải sửa ở 2 nơi, cho 2 model khác nhau.

**💻 Minh chứng:**

```ts
// lib/auth.ts — viết cho Pages Router, dựa vào `context.req`
import type { GetServerSidePropsContext } from 'next'

export function getUserFromContext(context: GetServerSidePropsContext) {
  const token = context.req.cookies.token
  return verifyToken(token)
}
```

```tsx
// pages/profile.tsx — vẫn hoạt động bình thường
export async function getServerSideProps(context) {
  const user = getUserFromContext(context) // ✅ OK, context có req
  return { props: { user } }
}
```

```tsx
// app/settings/page.tsx — route MỚI, viết theo App Router
import { getUserFromContext } from '@/lib/auth'

export default async function SettingsPage() {
  // 💥 App Router không expose "context.req" cho component
  // Không có gì để truyền vào getUserFromContext()
  const user = getUserFromContext(???)
  return <div>{user.name}</div>
}
```

```ts
// lib/auth.ts — buộc phải thêm phiên bản THỨ HAI cho App Router
import { cookies } from 'next/headers'

export async function getUserFromCookies() {
  const cookieStore = await cookies()
  const token = cookieStore.get('token')?.value
  return verifyToken(token)
}
// Giờ có 2 hàm làm cùng 1 việc, cho 2 paradigm — mọi thay đổi
// logic xác thực phải sửa ở CẢ HAI, dễ lệch nhau theo thời gian
```

**💥 Hệ quả production:**

"Migrate dần, không sao" trở thành **trạng thái nửa-migrate vĩnh viễn** — team không bao giờ có động lực/thời gian để hoàn thành phần còn lại, vì "mọi thứ vẫn chạy". Hệ quả tích lũy: cognitive load gấp đôi cho dev mới (phải biết 2 cách viết auth), UX không đồng nhất (một số route có layout/streaming, một số không), và 2 năm sau, không ai còn nhớ TẠI SAO `lib/auth.ts` có 2 hàm gần giống nhau.


---

### Nhóm C — Hiểu lầm về Server/Client Components

---

### Hiểu lầm #5: "`'use client'` nghĩa là code đó CHỈ chạy ở browser, server không render gì"

🏷️ **Vi phạm:** Chiều 1 (Execution Location) — đây là **mẹ của mọi hiểu lầm RSC**, gốc rễ của #6 và #7

📜 **Nguồn gốc:** Lớp 3 — chính cái TÊN "use client" đã gây hiểu sai, vì nó nghe như một lệnh "chạy ở client"

🚨 **Câu nói báo động:** *"Component này có `'use client'` nên server không đụng vào — sao log lại xuất hiện trong terminal?"*


**❌ Niềm tin:**

`'use client'` là một flag phân vùng môi trường thực thi: file có flag này → code chạy ở browser; file không có → code chạy ở server. Hai thế giới tách biệt hoàn toàn.

**✅ Sự thật:**

`'use client'` là **boundary marker cho bundler**, không phải instruction về nơi-chạy. Một Client Component vẫn được **render trên server lần đầu** (để tạo HTML cho SSR), sau đó **render lại trên client** (hydration) để gắn interactivity. `'use client'` chỉ đánh dấu: "module này (và mọi thứ nó import) sẽ được đóng gói gửi xuống client, và được PHÉP dùng hook/browser API" — nó không hề tắt server-render.

**💻 Minh chứng:**

```tsx
// app/components/Counter.tsx
'use client'

import { useState } from 'react'

export default function Counter() {
  console.log('[Counter] render, typeof window =', typeof window)
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>
}
```

Load trang lần đầu, bạn sẽ thấy **2 dòng log ở 2 nơi khác nhau**:

```
# Terminal (server, lúc SSR):
[Counter] render, typeof window = undefined

# Browser console (lúc hydration):
[Counter] render, typeof window = object
```

Component được render **2 lần, ở 2 môi trường**. Lần 1 (server) tạo HTML ban đầu — `'use client'` không ngăn điều này. Nó chỉ đảm bảo: (a) `useState`/`onClick` được phép dùng (chỉ có ý nghĩa ở lần render 2), và (b) JS của component này được gửi xuống client để lần render 2 diễn ra.

**💥 Hệ quả production:**

```tsx
'use client'
export default function Greeting() {
  // 💥 Giá trị KHÁC NHAU giữa lần render server và lần render client
  return <p>Bây giờ: {new Date().toLocaleTimeString()}</p>
}
```

HTML server gửi xuống ("10:42:01") khác HTML client tính ra khi hydrate ("10:42:03") → **Hydration Mismatch Error**. Người tin "use client = client only" sẽ bối rối: *"code chạy ở client thì so sánh với cái gì mà mismatch?"* — trong khi đây CHÍNH XÁC là hệ quả của việc component chạy ở cả hai nơi. Đây là root cause của phần lớn lỗi trong **M16.1 (Hydration Errors)**.

---

### Hiểu lầm #6: "Dính lỗi hooks → cứ thêm `'use client'` lên đầu file, không có hệ quả"

🏷️ **Vi phạm:** Chiều 1 — hệ quả trực tiếp của #5: nếu không hiểu `'use client'` là gì, nó trở thành "nút tắt báo lỗi"

📜 **Nguồn gốc:** Lớp 3

🚨 **Câu nói báo động:** *"Lỗi `useState is not allowed in Server Components` → thêm `'use client'` đầu file, build pass, xong."*


**❌ Niềm tin:**

`'use client'` là fix cục bộ, chỉ ảnh hưởng file đang sửa. Build pass = đã giải quyết xong.

**✅ Sự thật:**

Boundary `'use client'` **lan xuống toàn bộ subtree** mà component đó render — trừ khi children được truyền vào qua `children`/props (composition pattern). Thêm `'use client'` ở một component CHA sẽ kéo TẤT CẢ component CON vào client bundle, dù chúng không cần interactivity gì.

**💻 Minh chứng:**

```tsx
// app/dashboard/page.tsx
// ❌ "Fix" sai: 'use client' ở component CHA
'use client'

import { useState } from 'react'
import { Sidebar } from './Sidebar'
import { RevenueChart } from './RevenueChart'
import { RecentOrders } from './RecentOrders'

export default function DashboardPage() {
  const [tab, setTab] = useState('overview')
  // 💥 Không thể `await fetchDashboardData()` ở đây nữa —
  // Client Component không được là async function
  return (
    <div>
      <Sidebar />        {/* bị kéo vào client bundle, dù không cần */}
      <RevenueChart />   {/* bị kéo vào client bundle, dù không cần */}
      <RecentOrders />   {/* bị kéo vào client bundle, dù không cần */}
    </div>
  )
}
```

```tsx
// ✅ Fix đúng: tách phần CẦN state thành Client Component nhỏ, riêng biệt
// app/dashboard/page.tsx — vẫn là Server Component (async OK)
import { Sidebar } from './Sidebar'
import { RevenueChart } from './RevenueChart'
import { RecentOrders } from './RecentOrders'
import { TabSwitcher } from './TabSwitcher' // chỉ file NÀY có 'use client'

export default async function DashboardPage() {
  const data = await fetchDashboardData() // ✅ fetch trực tiếp trên server

  return (
    <div>
      <Sidebar />
      <TabSwitcher />              {/* island nhỏ, chỉ phần này gửi JS xuống client */}
      <RevenueChart data={data.revenue} />
      <RecentOrders data={data.orders} />
    </div>
  )
}
```

```tsx
// app/dashboard/TabSwitcher.tsx
'use client'
import { useState } from 'react'

export function TabSwitcher() {
  const [tab, setTab] = useState('overview')
  return (
    <div>
      <button onClick={() => setTab('overview')}>Overview</button>
      <button onClick={() => setTab('details')}>Details</button>
    </div>
  )
}
```

**💥 Hệ quả production:**

Bundle size tăng dần theo thời gian — mỗi lần dev "fix nhanh" bằng `'use client'` ở cấp cha, và **không ai quay lại tách nhỏ sau đó**. Sau vài tháng, `app/` có cấu trúc App Router nhưng client bundle to như một SPA — trả giá hạ tầng cho App Router (build phức tạp, RSC payload) mà KHÔNG nhận lợi ích (zero-JS subtree). Đây là nguồn dữ liệu chính cho **M11.1 (Bundle Analysis)** và **M6.6 (Phân tích Bundle — Zero JS to client)**.

---

### Hiểu lầm #7: "RSC luôn nhanh hơn → nên ép TẤT CẢ component thành Server Component"

🏷️ **Vi phạm:** Chiều 1 — overcorrection ngược với #6, biến Server/Client thành "thang điểm performance" thay vì "yêu cầu kỹ thuật"

📜 **Nguồn gốc:** Lớp 3 — messaging "zero bundle size!" bị hiểu thành "luôn ưu tiên tuyệt đối"

🚨 **Câu nói báo động:** *"Component này sao cần `'use client'`? Cố giữ Server Component được không?"* — khi component đó là nút Like, dropdown, search input...


**❌ Niềm tin:**

Server Component = tốt hơn về mọi mặt (bundle nhỏ, an toàn, nhanh). Mục tiêu nên là tối đa hoá Server Components, tối thiểu hoá Client Components — codebase "nhiều `'use client'`" = codebase tệ.

**✅ Sự thật:**

Server vs Client **không phải một cái nút chỉnh performance** — nó là câu hỏi về YÊU CẦU của component. Cần state/effect/event handler/browser API → **bắt buộc** Client Component, đây không phải lựa chọn. Cố ép một nút Like (cần phản hồi tức thì <100ms) thành Server Component nghĩa là biến mọi click thành một network round-trip đầy đủ.

**💻 Minh chứng:**

```tsx
// ❌ Ép thành Server Component: Like button qua Server Action + full reload
// KHÔNG có 'use client'
import { likePost } from './actions'

export default async function LikeButton({ postId, liked, count }: Props) {
  return (
    <form action={likePost.bind(null, postId)}>
      <button type="submit">{liked ? '❤️' : '🤍'} {count}</button>
    </form>
  )
}
```

Mỗi click → submit → re-render TOÀN BỘ route trên server → trả lại HTML mới. Không có optimistic update — độ trễ network lộ rõ trên một hành động lẽ ra phải tức thì.

```tsx
// ✅ Client Component nhỏ + useOptimistic — UX tức thì, vẫn gọi Server Action
'use client'
import { useOptimistic } from 'react'
import { likePost } from './actions'

export function LikeButton({ postId, liked, count }: Props) {
  const [state, addOptimistic] = useOptimistic(
    { liked, count },
    (s, newLiked: boolean) => ({ liked: newLiked, count: s.count + (newLiked ? 1 : -1) })
  )

  return (
    <button onClick={() => { addOptimistic(!state.liked); likePost(postId) }}>
      {state.liked ? '❤️' : '🤍'} {state.count}
    </button>
  )
}
```

**💥 Hệ quả production:**

Đây là nguồn gốc của phàn nàn *"Next.js app cảm giác nặng hơn SPA"* — không phải vì RSC chậm, mà vì các tương tác <100ms bị ép vào model request/response, tạo round-trip không cần thiết cho mọi thao tác nhỏ. Nghệ thuật không phải "tối đa hoá RSC" mà "đặt boundary đúng chỗ" — chủ đề chính của **M6.5 (Boundaries)**.

---

### Nhóm D — Hiểu lầm về Caching

---

### Hiểu lầm #8: "Set `revalidate` một lần là cache 'đúng mãi mãi', không cần nghĩ lại"

🏷️ **Vi phạm:** Chiều 2 (Cache Lifecycle) + Chiều 3 (Version Stability)

📜 **Nguồn gốc:** Lớp 2 — mental model ISR của Pages Router, nhưng số lớp cache đã nhân lên

🚨 **Câu nói báo động:** *"Mình set `revalidate: 3600` từ lúc setup, xong, không đụng nữa"* / *"Sao data cũ vẫn hiện sau khi update DB?"*


**❌ Niềm tin:**

`revalidate: N` là cấu hình một lần — đặt theo "dữ liệu này đổi bao lâu một lần", hệ thống tự lo phần còn lại mãi mãi.

**✅ Sự thật:**

`revalidate` là **ngưỡng chấp nhận cũ theo thời gian**, không phải đảm bảo tươi mới. Có cả một **cache hierarchy** (Request Memoization, Data Cache, Full Route Cache, Router Cache phía client) — mỗi tầng có timing riêng. Quan trọng hơn: time-based revalidation nghĩa là **luôn có thể tồn tại độ trễ tới N giây**, kể cả ngay sau khi data vừa đổi. Nếu yêu cầu là "vừa publish phải thấy ngay" (admin preview), `revalidate` thuần KHÔNG đáp ứng được — cần on-demand invalidation.

**💻 Minh chứng:**

```tsx
// app/products/[id]/page.tsx
export const revalidate = 60 // set 1 lần lúc setup

export default async function ProductPage({ params }) {
  const { id } = await params
  const res = await fetch(`https://api.shop.com/products/${id}`)
  return <ProductDetail product={await res.json()} />
}
```

Admin sửa giá lúc 10:00:00 → khách (và cả admin refresh trang preview) vẫn thấy giá CŨ tới 10:01:00. Với flash-sale, "tới 60 giây" là khoảng thời gian khách có thể mua với giá sai — vấn đề business, không chỉ "cache hơi cũ".

```ts
// app/actions/update-product.ts
'use server'
import { revalidateTag } from 'next/cache'

export async function updateProductPrice(id: string, newPrice: number) {
  await db.product.update({ where: { id }, data: { price: newPrice } })
  revalidateTag(`product-${id}`) // ✅ invalidate NGAY — đây là việc revalidate: 60 không làm được
}
```

```tsx
const res = await fetch(`https://api.shop.com/products/${id}`, {
  next: { tags: [`product-${id}`], revalidate: 3600 }, // 3600 = "tối đa cũ bao lâu NẾU không ai update"
})
```

**💥 Hệ quả production:**

*"Tại sao data không update?"* là một trong những bug report phổ biến nhất (chủ đề chính **M7.11**, **M16.2**). Root cause hầu như luôn là: `revalidate` set một lần, không có on-demand invalidation cho mutation, và không ai còn nhớ con số "60" từ đâu ra hay còn hợp lý không.

---

### Hiểu lầm #9: "Không hiểu cache → `force-dynamic` toàn bộ cho 'an toàn', mất hết lợi ích Next.js"

🏷️ **Vi phạm:** Chiều 2 — overcorrection ngược với #8: từ "cũ mãi" sang "chậm mãi nhưng đúng"

📜 **Nguồn gốc:** Lớp 3 — phản ứng sợ sau khi từng bị bug như #8

🚨 **Câu nói báo động:** *"Bị bug cache hoài, giờ cứ `force-dynamic` hết cho chắc"*


**❌ Niềm tin:**

Cache là "kẻ thù" — nguồn gốc của bug khó hiểu. Lựa chọn "an toàn" là tắt hết (`force-dynamic` mọi route, `cache: 'no-store'` mọi fetch). Performance là vấn đề "tối ưu sau".

**✅ Sự thật:**

Điều này biến MỌI request thành full server round-trip — kể cả cho content GIỐNG NHAU với hàng nghìn user (blog post, trang marketing). Next.js trở thành "Express app chậm vẽ React" — trả full chi phí SSR (query DB, gọi API, render) cho mỗi request, kể cả với một bài blog publish 6 tháng trước không hề đổi.

**💻 Minh chứng:**

```tsx
// app/blog/[slug]/page.tsx
// ❌ "An toàn": force-dynamic cho blog — content tĩnh, publish xong không đổi
export const dynamic = 'force-dynamic'

export default async function BlogPost({ params }) {
  const { slug } = await params
  const post = await getPostBySlug(slug) // query DB MỖI request, dù content tĩnh
  return <article>{post.content}</article>
}
```

Nếu bài viết lên top Hacker News (traffic spike), MỖI request đập vào DB → connection pool exhausted → toàn site sập, không chỉ trang blog.

```tsx
// ✅ Đúng đối tượng: SSG/ISR — build 1 lần, revalidate khi publish mới
export async function generateStaticParams() {
  const posts = await getAllPostSlugs()
  return posts.map(({ slug }) => ({ slug }))
}

export default async function BlogPost({ params }) {
  const { slug } = await params
  const post = await getPostBySlug(slug)
  return <article>{post.content}</article>
}
// Publish mới: revalidatePath('/blog') — không cần force-dynamic
```

```tsx
// Cái THỰC SỰ cần force-dynamic: data per-user, real-time
export const dynamic = 'force-dynamic'

export default async function Dashboard() {
  const session = await getSession()
  const liveData = await getLiveMetrics(session.userId)
  return <DashboardView data={liveData} />
}
```

**Phân biệt mấu chốt:** `force-dynamic` không phải "default an toàn" — nó là công cụ cho ĐÚNG một loại trang (per-request, per-user, real-time). Áp cho toàn site nghĩa là coi trang marketing và dashboard cá nhân hoá như nhau.

**💥 Hệ quả production:** 

TTFB tăng vọt toàn site, infra cost tăng tuyến tính theo traffic (không gì serve được từ CDN edge) — cùng traffic đó, với cache đúng, phần lớn gần như miễn phí. Quyết định "đúng correctness, sai kinh tế" mà **M11** và **M7.10** phải gỡ lại.

---

### Nhóm E — Hiểu lầm về Phạm vi & Kiến trúc

---

### Hiểu lầm #10: "Next.js dùng được cho MỌI loại project theo cùng kiểu kiến trúc — landing page và enterprise system như nhau"

🏷️ **Vi phạm:** Cả 3 chiều, nhưng ở cấp độ KIẾN TRÚC, không phải code-level

📜 **Nguồn gốc:** Lớp 3 — messaging "full-stack framework" bị hiểu thành "one architecture fits all"

🚨 **Câu nói báo động:** *"Mình dùng Next.js cho mọi project luôn, kiến trúc set sẵn rồi, cứ thêm feature vào"*


**❌ Niềm tin:**

Vì Next.js "full-stack" (routing, rendering, Server Actions đủ làm mutation), nó có thể là kiến trúc cho MỌI quy mô — từ landing page 5 trang tới SaaS multi-team — theo cùng một cách: "mọi thứ nằm trong `app/`, tổ chức theo route".

**✅ Sự thật:**

"Dùng được" (capability) và "nên cấu trúc/scale như nhau" (architecture) là hai câu hỏi khác nhau. Next.js cung cấp BUILDING BLOCKS — không quyết định: domain logic phức tạp nên nằm ở đâu, route nào team nào own, khi nào "Next.js làm backend luôn" là hợp lý vs khi nào cần service riêng. Một SaaS multi-tenant tổ chức giống landing page (mọi thứ phẳng, `actions.ts` chứa hết business logic) sẽ "chạy được" — cho tới khi team scale qua ~5 người, kiến trúc sụp đổ không phải vì Next.js "không làm được", mà vì kiến trúc không scale theo TEAM.

**💻 Minh chứng:**

```
// ❌ "Next.js architecture" cho landing page, áp cho SaaS đa team
app/
├── dashboard/
│   ├── page.tsx       ← UI + fetch + business logic + validation, tất cả trộn
│   └── actions.ts      ← 800 dòng, mọi domain logic của cả app dồn vào đây
├── billing/
│   └── page.tsx        ← copy-paste pattern từ dashboard
└── lib/
    └── db.ts            ← mọi feature import trực tiếp, không qua layer nào
```

2 team cùng sửa `actions.ts` của 2 feature khác nhau → merge conflict liên tục. Logic "tính billing cycle" rải rác trong `actions.ts` của nhiều route — đổi rule phải tìm-sửa nhiều nơi, dễ sót.

```
// ✅ Kiến trúc tính tới scale theo TEAM (M15.2)
app/
├── (dashboard)/dashboard/page.tsx   ← chỉ UI + compose
├── (dashboard)/billing/page.tsx
└── (dashboard)/settings/page.tsx

features/
├── billing/
│   ├── actions.ts    ← Server Actions, GỌI service layer
│   ├── service.ts     ← business logic, KHÔNG biết gì về Next.js
│   └── schema.ts       ← Zod validation
├── dashboard/{actions.ts, service.ts}
└── settings/{actions.ts, service.ts}
```

`service.ts` không import gì từ `next/*` — lớp này tái dùng được nếu sau cần expose API cho mobile app, hoặc tách microservice — điều cấu trúc "mọi thứ trong route" không cho phép mà không viết lại từ đầu.

**💥 Hệ quả production:**

Chủ đề chính **M15**, nhưng hậu quả xuất hiện sớm hơn nhiều người nghĩ. Một startup 2 người chọn flat structure là HOÀN TOÀN ĐÚNG cho giai đoạn đó — over-engineer sớm cũng là lỗi. Hiểu lầm thực sự không phải "nên dùng kiến trúc nào", mà là **tin rằng đây là quyết định "set once, forever"** — không checkpoint để re-evaluate khi team/scope đổi. Kết quả: "big rewrite" panic, thường trùng đúng lúc traffic/business pressure cao nhất.

---

## 5. Legacy Recognition — "Code Smell" của 10 Hiểu Lầm

Đây là checklist bạn dùng khi **review một codebase lạ lần đầu** (mục tiêu #2 và #12 của chương trình: nhận diện era + review PR). Mỗi dòng là một dấu hiệu cụ thể, grep được:

| Hiểu lầm | Code smell cần tìm | Cách tìm nhanh |
|---|---|---|
| #1, #2 | `getServerSideProps`/`getStaticProps`/`getInitialProps` nằm trong `app/` — dead code từ Pages Router, không hề được gọi | `grep -rn "getServerSideProps\|getStaticProps\|getInitialProps" app/` |
| #3, #4 | `pages/` và `app/` cùng tồn tại, VÀ có 2 hàm cùng mục đích khác tên (`getUserFromContext` + `getUserFromCookies`) trong `lib/` | `ls pages/ app/` + đọc `lib/auth.ts`, `lib/session.ts` |
| #5, #6 | `'use client'` ở `layout.tsx` cấp cao (root layout, section layout) — nghi vấn "use client creep" | `grep -rln "'use client'" app/**/layout.tsx` |
| #7 | `<form action={...}>` cho hành động cần phản hồi tức thì (like, toggle, filter input, autocomplete) | đọc UI: thao tác nhỏ nhưng dùng `<form>` thay vì `onClick` |
| #8, #9 | `revalidate` là số tròn không kèm comment/tag; HOẶC `force-dynamic` trên route content tĩnh (blog, marketing, docs) | `grep -rn "revalidate\|force-dynamic" app/` rồi đối chiếu route đó có per-user data không |
| #10 | file `actions.ts`/`route.ts` > 300 dòng, chứa validation + business logic + raw DB query trong cùng 1 file | `find app -name "actions.ts" \| xargs wc -l \| sort -rn` |

**Quy tắc đọc nhanh một codebase mới:** chạy 6 lệnh trên TRƯỚC khi đọc bất kỳ feature code nào. Số lượng "hit" cho bạn biết: codebase đang ở "thế hệ tư duy" nào (Lớp 1/2/3 — Section 2), và đâu là khu vực rủi ro cao nhất khi bạn touch vào.



## 6. Modern Approach — Bảng Tổng Hợp Mental Model Đã Sửa

| # | ❌ Hiểu lầm (rút gọn) | ✅ Mental model đúng | Chiều vi phạm |
|---|---|---|---|
| 1 | Next.js = React + Router + SSR | Next.js có execution model RIÊNG — RSC là runtime khác, không phải "React thiếu tính năng" | 1, 2 |
| 2 | Tutorial cũ áp được cho version mới | Mỗi major version có thể đổi DEFAULT behavior và API family — luôn check version trước khi áp pattern | 3 |
| 3 | App Router = đổi tên folder | App Router = rendering model khác (async component, streaming, params là Promise từ v15) | 1, 3 |
| 4 | Migrate route-by-route không ảnh hưởng gì | Shared code (`lib/`) phải support 2 paradigm song song trong giai đoạn migrate — đây là chi phí THẬT, cần kế hoạch | 1 |
| 5 | `'use client'` = chỉ chạy ở browser | `'use client'` = boundary cho bundler; component vẫn render trên server (SSR) trước khi hydrate | 1 |
| 6 | Thêm `'use client'` không hệ quả | `'use client'` lan xuống toàn subtree — đặt boundary CÀNG GẦN phần cần interactivity càng tốt | 1 |
| 7 | RSC luôn tốt hơn, ép dùng mọi nơi | Server/Client là YÊU CẦU kỹ thuật, không phải thang điểm. Tương tác <100ms → Client Component là bắt buộc | 1 |
| 8 | `revalidate` set 1 lần là xong | `revalidate` = ngưỡng chấp nhận cũ theo TIME. Cần invalidate NGAY (`revalidateTag`) cho mutation quan trọng | 2, 3 |
| 9 | Không hiểu cache → tắt hết (`force-dynamic`) | `force-dynamic` chỉ dành cho data per-request/per-user. Content tĩnh luôn nên SSG/ISR | 2 |
| 10 | Next.js = 1 kiến trúc cho mọi quy mô | Capability ≠ Architecture. Quyết định cấu trúc cần CHECKPOINT re-evaluate theo team/scope, không "set once" | 1, 2, 3 |

**Quan sát cuối:** nhìn cột "Chiều vi phạm", Chiều 1 (Execution Location) xuất hiện trong **7/10** hiểu lầm — đây là chiều dễ bị collapse nhất, vì cú pháp JSX/component giống nhau ở cả Server và Client, "trông giống" không có nghĩa là "hoạt động giống".



## 7. Production Scenario — Case Thực Tế: "1 Sprint, 5 Hiểu Lầm, 3 Tuần Debug"

### Context

Team 4 người (1 senior, 2 mid, 1 junior), startup SaaS đã có codebase Pages Router Next.js 12 cho trang admin quản lý sản phẩm và đơn hàng. PM yêu cầu migrate sang App Router "để dùng được Server Actions và Streaming". Timeline: 3 tuần.

Sprint planning: *"Migrate từng route một, mỗi dev nhận 1 route, không block nhau."*

**Kết quả thực tế sau 3 tuần** — build pass, deploy lên staging, nhưng:

- Admin cập nhật giá sản phẩm → 90 giây sau trang product vẫn hiện giá cũ
- Dropdown filter trên dashboard gây full-page reload, trễ 2–3s mỗi lần chọn
- Bundle size tăng 40% so với Pages Router cũ
- `lib/auth.ts` có 3 hàm auth gần giống nhau, không ai dám xoá
- Sau 2 tuần, khi spike traffic, server CPU 100% dù content không đổi gì

Bên dưới là quá trình **dựng lại timeline** từng sự cố — không có điểm thất bại lớn duy nhất, chỉ là chuỗi quyết định nhỏ cộng dồn.

---

### Ngày 1–3: "Migrate nhanh route đầu tiên"

Mid developer A nhận route `/products`. Copy code từ một YouTube tutorial "Next.js 14 App Router" (thực ra tutorial viết cho Next 13, `params` chưa là Promise):

```tsx
// app/products/[id]/page.tsx — tuần 1
export async function getStaticProps({ params }) {  
// ❌ Dead code, không bao giờ được gọi
  return { props: { id: params.id } }
}

export default function ProductPage({ id }) {       
// ❌ id luôn là undefined
  const [product, setProduct] = useState(null)

  useEffect(() => {
    fetch(`/api/products/${id}`).then(r => r.json()).then(setProduct)
  }, [id])

  if (!product) return <Spinner />
  return <ProductDetail product={product} />
}
```

Build pass. TypeScript không báo lỗi vì `getStaticProps` là named export hợp lệ — Next.js chỉ **ignore** hoàn toàn, không throw. Tester mở trang, Spinner quay 2s rồi product load (qua `useEffect` fetch `/api/products/undefined`). Không ai hỏi tại sao API được gọi từ client.

**Hiểu lầm kích hoạt:** #2 (tutorial cũ), #1 (chưa phân biệt execution model)

---

### Ngày 4–5: "'use client' fix được lỗi — tiếp tục"

Commit tiếp theo gây lỗi build: `useState` không được dùng trong Server Component. Fix "nhanh":

```tsx
// app/products/[id]/page.tsx — sau "fix"
'use client'  // ← thêm vào, lỗi build biến mất

// getStaticProps vẫn ở đây — vẫn dead code
// useEffect vẫn ở đây — giờ OK về mặt build vì là Client Component
```

PR checklist: *"Build pass ✅, UI hiện đúng ✅ → Merge."*

**Hiểu lầm kích hoạt:** #6 (`'use client'` thêm bừa, không ai hỏi "tại sao cần"). Hậu quả chưa thấy ở giai đoạn này vì chỉ 1 route, traffic thấp.

---

### Ngày 6–10: Auth shared code bị split

Senior dev migrate `/dashboard` và `/settings` — cần đọc session. 
Nhưng `lib/auth.ts` viết cho Pages Router

Expose `getUserFromContext(context: GetServerSidePropsContext)`. App Router không có context object này.

Giải pháp nhanh: thêm hàm mới, giữ hàm cũ (vì `pages/auth/login.tsx` chưa migrate):

```ts
// lib/auth.ts — sau ngày 10

// cho pages/
export function getUserFromContext(context) { /* ... */ }

// cho app/ — thêm tháng này
export async function getUserFromCookies() { /* ... */ }

// ai đó thêm từ StackOverflow
export async function getCurrentUser() { /* ... */ }
```

3 hàm làm cùng một việc. Mọi thay đổi logic xác thực phải sửa ở 3 chỗ. Sau 6 tháng, không ai còn chắc 3 hàm có behavior giống nhau không.

**Hiểu lầm kích hoạt:** #4 (incremental migration không có kế hoạch shared code)

---

### Ngày 11–14: Cache bug đầu tiên

Junior dev viết lại `/products` đúng hơn lần trước:

```tsx
// app/products/[id]/page.tsx — lần 2
export default async function ProductPage({ params }) {
  const { id } = await params
  const product = await fetch(`https://internal-api/products/${id}`)
                        .then(r => r.json())
  return <ProductDetail product={product} />
}
```

Không có cấu hình cache → Next.js cache theo default của version đang dùng (behavior này khác nhau giữa v13/v14/v15 — **Chiều 3**). 

Admin sửa giá lúc 14:00 → 14:05 khách vẫn thấy giá cũ.

Một dev Google "next.js disable cache", tìm được `force-dynamic`, thêm vào — bug biến mất:

```tsx
export const dynamic = 'force-dynamic'  // ← thêm vào tất cả route "cho chắc"
```

**Hiểu lầm kích hoạt:** #8 (không có on-demand invalidation), #9 (`force-dynamic` lan ra toàn site)

---

### Tuần 3: Traffic spike, server sập

Site được featured trên một group Facebook lớn. Traffic tăng 10x trong 2 giờ. Server CPU 100%, response time > 10s, site sập.

Post-mortem: `force-dynamic` trên trang sản phẩm khiến mỗi user mở trang đập thẳng vào DB và internal API — không một request nào được serve từ `CDN edge`. 10x traffic = 10x DB queries không có buffer.

Nếu dùng `SSG/ISR` đúng, CDN có thể absorb gần như 100% traffic tăng thêm với zero server load.

**Hiểu lầm kích hoạt:** #9 (`force-dynamic` làm mất toàn bộ caching layer bảo vệ origin)

---

### Timeline Analysis

```
Ngày  1– 3: #2, #1     → dead code, fetch client-side thay vì server
Ngày  4– 5: #6         → 'use client' fix nhanh, bundle phình không ai thấy
Ngày  6–10: #4         → auth split, 3 hàm cùng mục đích, dual maintenance bắt đầu
Ngày 11–14: #8, #9    → cache bug → force-dynamic lan ra toàn site như phản xạ
Ngày 15–21: hậu quả #9 → server sập khi spike vì không còn caching layer nào
```

**Không có một điểm thất bại lớn.** 

Mỗi quyết định "có lý" trong ngữ cảnh lúc đó, cộng dồn thành thảm họa. Nếu team chạy checklist `Legacy Recognition(Section 5)` ngay sau ngày 3, họ đã thấy `getStaticProps` trong `app/` + `'use client'` ở route-level + không có `revalidateTag` trong codebase — 3 báo động rõ ràng.


## 8. Debugging Scenario — Walkthrough: "Tại Sao Dashboard Hiện Data Cũ?"

### Bug Report

> **Người báo:** Admin  
> **Triệu chứng:** Cập nhật giá sản phẩm trong CMS lúc 09:15. Refresh trang `/dashboard/products` nhiều lần. Đến 09:28 vẫn thấy giá cũ. Sau 09:29 thì thấy giá mới.  
> **Môi trường:** Production, Next.js 15, `revalidate: 60` trên route `/dashboard/products`

### Bước 1 — Định vị sai lệch thời gian

```
09:15 — Admin update giá
09:16 — Admin refresh: vẫn giá cũ     ← có thể chấp nhận (revalidate: 60)
09:17 — Admin refresh: vẫn giá cũ     ← route đã revalidate, sao vẫn cũ?
09:28 — Admin refresh: vẫn giá cũ     ← 13 phút, không phải 1 phút
09:29 — Admin refresh: giá mới ✅
```

Nếu chỉ có `revalidate: 60`, bug lý ra phải fix tại 09:16. Tại sao data cũ tồn tại 13 phút?

### Bước 2 — Trace qua cache hierarchy (Chiều 2)

```
Request 09:16:
→ [Full Route Cache] Stale → trigger background revalidation
   → Revalidation bắt đầu: gọi lại page component, fetch data...
   → Nhưng fetch bên trong có cache riêng:

   fetch('https://pricing-api/products', {
     next: { revalidate: 900 }  // ← 15 phút
   })

   → [Data Cache] pricing-api response còn fresh (chỉ mới 1 phút / 900s)
   → Data Cache KHÔNG gọi pricing-api thật
   → Background revalidation build HTML mới với giá từ Data Cache — VẪN GIÁ CŨ
   → Full Route Cache cập nhật, nhưng chứa giá cũ

Request 09:28:
→ [Full Route Cache] Hit → trả HTML chứa giá cũ (được tính lúc 09:16)

Request 09:29:
→ Data Cache của pricing-api hết hạn (09:15 + 15 phút = 09:30 gần kề)
  → Revalidation lần này gọi pricing-api thật → giá mới
  → Full Route Cache cập nhật với giá mới ✅
```

### Bước 3 — Root cause

```
2 revalidate timer lồng nhau, không được design cùng nhau:
  Full Route Cache:  revalidate: 60  (1 phút)
  Data Cache:        revalidate: 900 (15 phút) — trong fetch bên trong

Khi route revalidate sau 1 phút, nó tái sử dụng Data Cache còn fresh
→ HTML "mới" nhưng data vẫn cũ.
Window bug: 09:16 – 09:30 (cho đến khi Data Cache của pricing-api hết hạn).
```

**Hiểu lầm kích hoạt:** #8 — đặt `revalidate` không nghĩ đến hierarchy, nghĩ "1 layer" khi thực ra có nhiều layer lồng nhau.

### Bước 4 — Fix

```ts
// lib/products.ts
export async function fetchProductsWithPricing() {
  const [products, pricing] = await Promise.all([
    fetch('/api/products', {
      next: { tags: ['products'], revalidate: 300 }
    }).then(r => r.json()),

    fetch('https://pricing-api/products', {
      // Đồng bộ revalidate — không còn window "route fresh, data stale"
      next: { tags: ['products-pricing'], revalidate: 300 }
    }).then(r => r.json()),
  ])
  return merge(products, pricing)
}
```

```ts
// app/actions/update-product.ts
'use server'
import { revalidateTag } from 'next/cache'

export async function updateProductPrice(id: string, price: number) {
  await db.product.update({ where: { id }, data: { price } })
  // Invalidate CẢ HAI tag cùng lúc — không còn window "route fresh, pricing stale"
  revalidateTag('products')
  revalidateTag('products-pricing')
}
```

Mutation xảy ra → invalidate ngay → lần revalidation tiếp theo gọi cả 2 source thật. Không cần chờ timer nào.

### Checklist Debug Cache — Dùng Lại Cho Mọi Bug Dạng Này

```
□ Có bao nhiêu layer cache đang active trên request này?
  → Full Route Cache? Data Cache (per-fetch)? Router Cache (client)?

□ Timer của mỗi layer là bao nhiêu? Ai set? Còn hợp lý không?

□ Có fetch lồng nhau không?
  → Route revalidate sau 60s, nhưng fetch bên trong cache 900s?

□ Mutation có gọi revalidateTag/revalidatePath tương ứng không?
  → Nếu không: time-based revalidation là phòng tuyến duy nhất.

□ Default cache behavior của Next.js version đang dùng là gì?
  → v13/v14: fetch cached by default
  → v15+: fetch NOT cached by default (opts out Full Route Cache nếu không explicit)
```



## 9. Refactoring Scenario — Before/After: "Rescue Một Route Mang Đủ 5 Hiểu Lầm"

### Target: Route `/admin/orders` — BEFORE

```tsx
// app/admin/orders/page.tsx — 210 dòng, trộn đủ thứ
'use client'  // ← thêm từ tháng 1 vì "lỗi hooks"

import { useState, useEffect } from 'react'
import { OrdersTable }   from './OrdersTable'   // không cần interactivity
import { FilterBar }     from './FilterBar'      // cần state
import { ExportButton }  from './ExportButton'   // cần browser API
import { SummaryCards }  from './SummaryCards'   // không cần interactivity

export async function getServerSideProps() {     // ❌ dead code từ Pages Router
  return { props: {} }
}

export default function OrdersPage() {
  const [orders, setOrders]   = useState([])
  const [filter, setFilter]   = useState({ status: 'all', dateRange: '7d' })
  const [summary, setSummary] = useState(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    setLoading(true)
    Promise.all([
      fetch(`/api/orders?status=${filter.status}&range=${filter.dateRange}`).then(r => r.json()),
      fetch('/api/orders/summary').then(r => r.json()),
    ]).then(([ordersData, summaryData]) => {
      setOrders(ordersData)
      setSummary(summaryData)
      setLoading(false)
    })
  }, [filter])

  const handleExport = async () => {
    // 80 dòng business logic export trực tiếp trong component
    const csv = orders.map(o => `${o.id},${o.total},${o.status}`).join('\n')
    // ... download blob ...
  }

  if (loading) return <div>Đang tải...</div>
  return (
    <div>
      <SummaryCards summary={summary} />
      <FilterBar filter={filter} onChange={setFilter} />
      <OrdersTable orders={orders} />
      <ExportButton onExport={handleExport} />
    </div>
  )
}
```

**Inventory vấn đề:**

| Vấn đề | Hiểu lầm |
|---|---|
| `'use client'` ở page-level kéo tất cả children vào bundle | #6 |
| `getServerSideProps` dead code | #2 |
| Data fetch qua `useEffect` — waterfall, không cache, không stream | #1, #5 |
| Filter thay đổi → 2 fetch sequential, toàn trang block | #7 |
| 80 dòng business logic export trong component | #10 |
| Loading toàn trang, không có skeleton độc lập | hệ quả #6 |

---

### Phân Tích Yêu Cầu Từng Phần

```
SummaryCards  → chỉ render data, không có interactivity       → Server Component
FilterBar     → cần useState, tương tác tức thì              → Client Component (nhỏ)
OrdersTable   → render data từ filter, không có interactivity → Server Component
ExportButton  → trigger download, cần browser API             → Client Component (nhỏ)
```

---

### AFTER — Page Component

```tsx
// app/admin/orders/page.tsx — Server Component, không có 'use client'
import { Suspense } from 'react'
import { SummaryCards }            from './SummaryCards'
import { OrdersTable }             from './OrdersTable'
import { OrdersFilter }            from './OrdersFilter'   // 'use client' chỉ ở đây
import { ExportButton }            from './ExportButton'   // 'use client' chỉ ở đây
import { SummarySkeleton, TableSkeleton } from './Skeletons'

interface Props {
  searchParams: Promise<{ status?: string; range?: string }>
}

export default async function OrdersPage({ searchParams }: Props) {
  const { status = 'all', range = '7d' } = await searchParams

  return (
    <div>
      {/* Mỗi Suspense boundary stream độc lập — không block nhau */}
      <Suspense fallback={<SummarySkeleton />}>
        <SummaryCards range={range} />
      </Suspense>

      {/* Client island nhỏ: chỉ phần filter UI, không kéo gì thêm vào bundle */}
      <OrdersFilter currentStatus={status} currentRange={range} />

      <Suspense fallback={<TableSkeleton />}>
        <OrdersTable status={status} range={range} />
      </Suspense>

      <ExportButton />
    </div>
  )
}
```

### AFTER — Từng Component

```tsx
// app/admin/orders/SummaryCards.tsx — async Server Component, stream riêng
export async function SummaryCards({ range }: { range: string }) {
  const summary = await fetch(`/api/orders/summary?range=${range}`, {
    next: { revalidate: 300, tags: ['orders-summary'] },
  }).then(r => r.json())

  return (
    <div className="grid grid-cols-4 gap-4">
      <Card label="Tổng đơn"    value={summary.total}     />
      <Card label="Doanh thu"   value={summary.revenue}   />
      <Card label="Đang xử lý"  value={summary.pending}   />
      <Card label="Hoàn thành"  value={summary.completed} />
    </div>
  )
}
```

```tsx
// app/admin/orders/OrdersFilter.tsx — Client Component, CHỈ filter UI
'use client'
import { useRouter } from 'next/navigation'

interface Props { currentStatus: string; currentRange: string }

export function OrdersFilter({ currentStatus, currentRange }: Props) {
  const router = useRouter()

  const update = (key: string, value: string) => {
    const params = new URLSearchParams(window.location.search)
    params.set(key, value)
    router.push(`?${params.toString()}`)
    // Filter đổi → URL đổi → Server Component re-fetch với params mới
    // Không cần useEffect, không cần useState cho data
  }

  return (
    <div className="flex gap-2 my-4">
      <select value={currentStatus} onChange={e => update('status', e.target.value)}>
        <option value="all">Tất cả</option>
        <option value="pending">Đang xử lý</option>
        <option value="completed">Hoàn thành</option>
      </select>
      <select value={currentRange} onChange={e => update('range', e.target.value)}>
        <option value="7d">7 ngày</option>
        <option value="30d">30 ngày</option>
        <option value="90d">90 ngày</option>
      </select>
    </div>
  )
  // Component này: ~30 dòng, không fetch gì, không kéo gì vào bundle
}
```

```tsx
// app/admin/orders/OrdersTable.tsx — async Server Component
export async function OrdersTable({
  status,
  range,
}: {
  status: string
  range: string
}) {
  const orders = await db.order.findMany({
    where: buildOrderFilter(status, range),
    orderBy: { createdAt: 'desc' },
    take: 50,
    include: { customer: true },
  })

  if (orders.length === 0) {
    return <p className="text-center py-8 text-gray-500">Không có đơn hàng nào.</p>
  }

  return (
    <table className="w-full">
      <thead>
        <tr>
          <th>Mã đơn</th><th>Khách hàng</th><th>Tổng tiền</th><th>Trạng thái</th>
        </tr>
      </thead>
      <tbody>
        {orders.map(o => (
          <tr key={o.id}>
            <td>{o.id}</td>
            <td>{o.customer.name}</td>
            <td>{o.total.toLocaleString('vi-VN')} đ</td>
            <td>{o.status}</td>
          </tr>
        ))}
      </tbody>
    </table>
  )
}
```

```tsx
// app/admin/orders/ExportButton.tsx — Client Component
'use client'
import { useState } from 'react'
import { exportOrdersAction } from './actions'

export function ExportButton() {
  const [loading, setLoading] = useState(false)

  const handleExport = async () => {
    setLoading(true)
    try {
      await exportOrdersAction()
    } finally {
      setLoading(false)
    }
  }

  return (
    <button onClick={handleExport} disabled={loading}>
      {loading ? 'Đang xuất...' : 'Xuất CSV'}
    </button>
  )
}
```

```ts
// app/admin/orders/actions.ts — Server Action, KHÔNG chứa business logic
'use server'
import { buildOrderExport } from '@/features/orders/export-service'
import { getCurrentUser }   from '@/lib/auth/server'

export async function exportOrdersAction() {
  const user = await getCurrentUser()
  if (!user?.isAdmin) throw new Error('Unauthorized')
  return buildOrderExport({ adminId: user.id })
}
```

```ts
// features/orders/export-service.ts — Pure business logic, không import gì từ next/*
export async function buildOrderExport({ adminId }: { adminId: string }) {
  const orders = await db.order.findMany({
    where: { /* ... */ },
    include: { customer: true, items: true },
  })
  // 80 dòng logic export — testable với Jest, không cần mock Next.js
  const rows = orders.map(o => ({
    id: o.id,
    customer: o.customer.name,
    total: o.total,
    status: o.status,
    items: o.items.length,
  }))
  return convertToCSV(rows)
}
```

---

### So Sánh Before / After

| Tiêu chí | Before | After |
|---|---|---|
| JS gửi xuống client | Toàn bộ page + tất cả children | Chỉ `OrdersFilter` + `ExportButton` |
| Initial data load | `useEffect` waterfall sau khi JS parse | Server fetch parallel, có Suspense stream |
| Cache | Không có | 5 phút cho summary + on-demand invalidation |
| Filter interaction | 2 fetch + setState + re-render toàn trang | URL-driven → Server re-fetch → Suspense |
| Testability | Export logic trong component | `export-service.ts` unit-testable độc lập |
| Loading UX | Spinner toàn trang | Skeleton độc lập theo từng khu vực |
| LCP dự kiến | > 3s (chờ JS parse → fetch → render) | < 1s (server HTML + progressive streaming) |
| Dead code | `getServerSideProps` không ai xoá | Không còn |



## 10. Architecture Notes

### "Quy Tắc Đặt Boundary" — Khi Nào Server, Khi Nào Client

Không phải công thức cứng, nhưng 3 câu hỏi theo thứ tự ưu tiên:

```
1. Component này có cần browser API không?
   (localStorage, geolocation, window, ResizeObserver, WebSocket...)
   → CÓ → Client Component, không thảo luận.

2. Component này có cần phản hồi < 100ms không?
   (click, hover, drag, type, keypress, animation trực quan...)
   → CÓ → Client Component, không thảo luận.

3. Component này chỉ render data, không cần 2 điều trên?
   → Server Component — đây là default trong App Router.
```

**Suy ra (Corollary):** đặt `'use client'` càng gần **leaf** (lá) của component tree càng tốt. Mọi component tổ tiên (ancestor) đều không nên có `'use client'` trừ khi CHÍNH NÓ — không phải con của nó — cần state hoặc effect.

---

### "Quy Tắc Chọn Rendering Strategy"

```
Data giống nhau cho mọi user + ít đổi?
→ generateStaticParams + revalidate dài (SSG/ISR)
→ Ví dụ: blog, trang marketing, docs, catalog sản phẩm thường.

Data giống nhau cho mọi user + đổi theo event (admin action)?
→ revalidate dài + revalidateTag khi mutation
→ Ví dụ: landing page edit từ CMS, product page cập nhật khi admin sửa.

Data per-user hoặc cần real-time?
→ force-dynamic + streaming (Suspense)
→ Ví dụ: dashboard cá nhân hoá, feed notification, live stock price.

Data mix — phần static + phần per-user trên cùng trang?
→ PPR (Partial Prerendering, Next 15+) hoặc layout tĩnh + slot dynamic
→ Ví dụ: trang sản phẩm (static) + badge "Đã mua" (per-user).
```

**Tiêu chí quyết định nhanh (Quick Heuristic):** Nếu 1000 user truy cập cùng một URL mà họ có thể nhận cùng một HTML/response giống nhau, thì trang đó không cần render riêng cho từng user → không nên dùng `force-dynamic`.

---


### Khi nào nên thoát khỏi tư duy "Everything in `app/`"

Dấu hiệu cần tách service layer (Hiểu lầm #10):

- Dấu hiệu 1: `actions.ts` > 150 dòng, chứa validation + business logic + raw DB query trong cùng 1 file

```ts
// ❌ app/orders/actions.ts

"use server"

import { db } from "@/lib/db"
import { revalidatePath } from "next/cache"
import { z } from "zod"

const CreateOrderSchema = z.object({
  productId: z.string(),
  quantity: z.number().min(1),
})

export async function createOrder(input: unknown) {
  // 1. Validation
  const data = CreateOrderSchema.parse(input)

  // 2. Business logic
  const product = await db.product.findUnique({
    where: {
      id: data.productId
    }
  })

  if (!product) {
    throw new Error("Product not found")
  }

  if (product.stock < data.quantity) {
    throw new Error("Not enough stock")
  }

  const total = product.price * data.quantity

  // 3. Raw DB query
  const order = await db.order.create({
    data: {
      productId: product.id,
      quantity: data.quantity,
      total
    }
  })

  // 4. Side effect của Next.js
  revalidatePath("/orders")

  return order
}

/*
* Vấn đề:
* createOrder() đang làm 4 việc khác nhau:
* createOrder
* |
* +-- validate input
* |
* +-- check business rule
* |
* +-- access database
* |
* +-- trigger Next.js cache
* 
* Sau vài tháng: actions.ts lên 350 dòng
* createOrder()
* cancelOrder()
* refundOrder()
* updateShipping()
* calculateDiscount()
* 
* Rất khó test vì muốn test business rule phải mock: 
* next/cache, prisma, headers(), cookies()
*/
```

→ Tách:

```
📁 orders/
├── 📃 actions.ts
├── 📃 service.ts
├── 📃 repository.ts
└── 📃 schema.ts
```

```ts
// 📃 app/orders/actions.ts
"use server"

import { createOrderService } from "./service"
import { revalidatePath } from "next/cache"

export async function createOrder(input: unknown) {
  const order = await createOrderService(input)

  revalidatePath("/orders")

  return order
}

//-------------------------

// 📃 app/orders/service.ts
import { orderRepository } from "./repository"

export async function createOrderService(input) {
  const product = await orderRepository.findProduct(
    input.productId
  )

  if (product.stock < input.quantity) {
    throw new Error("Not enough stock")
  }

  return orderRepository.create({
    productId: product.id,
    quantity: input.quantity,
    total: product.price * input.quantity
  })
}

//-------------------------
//📃....các service khác

```

Giờ test được:

```ts
describe("createOrderService", () => {
  it("reject when stock is insufficient", () => {
    ...
  })
})
// Không cần chạy Next.js.
```

---

- Dấu hiệu 2: Business logic giống nhau xuất hiện ở > 2 route (copy-paste giữa các `actions.ts`)

```ts
// app/admin/orders/actions.ts
// (shipOrder ở admin )
export async function shipOrder(orderId) {
  const order = await db.order.findUnique({
    where:{id: orderId}
  })

  if (order.status !== "PAID") {
    throw Error("Cannot ship")
  }

  await db.order.update({
    data:{
      status:"SHIPPED"
    }
  })
}

// app/api/orders/actions.ts
// (shipOrder ở general )
export async function shipOrder(orderId) {
  const order = await db.order.findUnique({
    where:{id: orderId}
  })

  if (order.status !== "PAID") {
    throw Error("Cannot ship")
  }

  await db.order.update({
    data:{
      status:"SHIPPED"
    }
  })
}
```

→ Hai nơi copy cùng logic. Đây là dấu hiệu logic này thuộc về domain hơn là route.

Nên đưa thành:

```
domain/
└── order/
    └── service.ts
```

```ts
// domain/order/services.ts
export async function shipOrder(orderId) {
  ...
}
```

---

- Dấu hiệu 3: Muốn viết unit test cho business logic nhưng không thể vì dính `Next.js API` phải import `next/cache` hay `next/headers`

```ts
// ❌ khó test
"use server"

import { headers } from "next/headers"
import { cookies } from "next/headers"

export async function checkout() {

  const userId =
    cookies().get("userId")

  const country =
    headers().get("country")

  // 200 dòng business logic

  // Muốn test: test("checkout calculates discount")
  // phải mock: jest.mock("next/headers")
  // và setup: cookies.mockReturnValue(...); headers.mockReturnValue(...)
  // Đây là dấu hiệu business logic đang bị khóa vào framework
}
```


→ Tách phần thuần:

```
checkoutService()
        |
        |
 pure TypeScript
```

```ts
// services/checkout.ts
export function calculateDiscount(cart, user) {
  // pure function
}
```

Test:

```ts
expect(
  calculateDiscount(cart, user)
).toBe(20)
```

---

- Dấu hiệu 4: Team > 4 người với > 2 domain rõ rệt (billing, shipping, inventory...) — merge conflict tăng tuyến tính

Team lớn → conflict tăng

```txt
❌ app/actions.ts

Bảo: thêm logic cho billing
Vũ: shipping
Phước: inventory

=> cùng sửa 1 file `actions.ts`
=> merge conflict liên tục
```

→ Tách theo domain:

```txt
services/
├── billing/
│   └── service.ts
├── shipping/
│   └── service.ts
└── inventory/
    └── service.ts

Member làm độc lập → Ít đụng file
```



**Ngưỡng thực tế:**

```
1–2 dev, < 10 route  → flat structure trong app/ là ĐÚNG — đừng over-engineer
3–5 dev, 10–30 route → bắt đầu tách features/ cho domain phức tạp
5+ dev, 30+ route    → features/ bắt buộc, domain-driven structure, xem M15
```

Hiểu lầm thực sự không phải "chọn kiến trúc nào" — mà là **coi quyết định đó là "set once, forever"**. Kiến trúc cần checkpoint re-evaluate khi team/scope đổi, không phải khi đã sụp đổ.



## 11. Interview Questions — Junior đến Architect

<details>
<summary><strong>Q1 [Junior]</strong> — Giải thích sự khác nhau giữa Server Component và Client Component. Khi nào dùng cái nào?</summary>

**Điểm cần có trong câu trả lời:**

Server Component chạy trên server, không có state/effect/event handler, có thể `async`, fetch data trực tiếp, không gửi JS xuống client. Client Component (có `'use client'`) được render trên server (SSR để tạo HTML) VÀ render lại trên client (hydration để gắn interactivity) — **không phải "chỉ chạy ở browser"**.

Dùng Client Component khi: cần `useState`, `useEffect`, event handler (`onClick`, `onSubmit`...), hoặc browser API. Dùng Server Component khi: chỉ render data, không cần interactivity — đây là default, không cần khai báo gì.

**Câu trả lời sai thường gặp:** *"Client Component chỉ chạy ở browser, Server Component chỉ chạy ở server"* — bỏ qua SSR của Client Component, dẫn đến mù hydration mismatch.

**Follow-up tốt để hỏi thêm:** *"Nếu một Server Component render một Client Component qua `import`, điều gì xảy ra với bundler?"* → Bundler đóng gói Client Component và dependencies của nó để gửi xuống client. Server Component render trên server, truyền kết quả qua serialization boundary.

</details>

---

<details>
<summary><strong>Q2 [Junior]</strong> — Team vừa migrate sang App Router nhưng còn <code>getServerSideProps</code> trong <code>app/</code>. Có vấn đề gì không?</summary>

**Điểm cần có:**

`getServerSideProps`, `getStaticProps`, `getStaticPaths` **không được gọi** trong `app/` directory — chúng là API độc quyền của Pages Router. Next.js không throw error: chúng chỉ là named exports thông thường, bị ignore hoàn toàn. Props nhận vào component sẽ là `undefined`.

Đây chính xác là lý do hiểu lầm này nguy hiểm: **fail silently** — build pass, TypeScript pass, chỉ data là `undefined`.

**Fix:** xóa các export cũ, viết page component thành `async function` và fetch data trực tiếp bên trong.

**Câu trả lời sai thường gặp:** *"Build sẽ throw lỗi ngay"* — không, đây là điểm mấu chốt.

</details>

---

<details>
<summary><strong>Q3 [Junior-Mid]</strong> — <code>'use client'</code> được thêm vào một layout component ở cấp cao. Hậu quả là gì?</summary>

**Điểm cần có:**

`'use client'` tạo boundary cho bundler: mọi component được **import bởi** file đó đều bị kéo vào client JavaScript bundle — dù chúng không cần state hay event handler gì. Exception (ngoại lệ) duy nhất: component được truyền qua `children` prop (composition pattern) — vì khi đó chúng được render bởi một Server Component ở tầng trên, không phải bởi component có `'use client'`.

Hậu quả thực tế: bundle size tăng đột ngột, mọi Server Component con bị mất lợi ích zero-JS. Với root layout, điều này ảnh hưởng toàn bộ app.

**Fix:** tách phần cần interactivity thành Client Component nhỏ, layout quay về Server Component. Phát hiện nhanh bằng `grep -rln "'use client'" app/**/layout.tsx`.

</details>

---

**Q4 [Mid]**  — Tại sao đoạn code này gây Hydration Mismatch Error?
```tsx
'use client'
export function CurrentTime() {
  return <p>Bây giờ: {new Date().toLocaleTimeString()}</p>
}
```
<details>
<summary>Đáp án Q4</summary>


**Điểm cần có:**

Client Component vẫn được render trên server (SSR) để tạo HTML ban đầu. 

`Server render` lúc T=10:42:01 → HTML có chuỗi "10:42:01". 

`Client hydrate` lúc T=10:42:03 → tính ra "10:42:03" 

→ **không khớp** với HTML server → React throw `Hydration Mismatch`.

`'use client'` **không ngăn server render** — nó chỉ đánh dấu component được phép dùng hooks và sẽ được gửi JS xuống client.

**Fix:** render rỗng trên server, set time thật sau hydration:

```tsx
'use client'
import { useState, useEffect } from 'react'

export function CurrentTime() {
  const [time, setTime] = useState('')  // rỗng trên server → không có gì để mismatch

  useEffect(() => {
    // Chỉ chạy trên client, sau khi hydration xong
    setTime(new Date().toLocaleTimeString())
    const id = setInterval(() => setTime(new Date().toLocaleTimeString()), 1000)
    return () => clearInterval(id)
  }, [])

  return <p>Bây giờ: {time || '--:--:--'}</p>
}
```

</details>

---

<details>
<summary><strong>Q5 [Mid]</strong> — Route <code>/blog/[slug]</code> có <code>revalidate: 3600</code> (1 giờ), nhưng sau khi publish bài viết mới, người dùng vẫn thấy nội dung cũ ngay cả sau hơn 2 giờ. Hướng debug như thế nào?</summary>

**Điểm cần có:**

Trace qua cache hierarchy theo thứ tự:

**Full Route Cache** — trước tiên xác nhận route có thực sự đang sử dụng Full Route Cache hay không. `revalidate: 3600` có đang được áp dụng đúng không? Lần revalidation gần nhất diễn ra khi nào? Kiểm tra xem route có vô tình opt-out khỏi Full Route Cache bởi `fetch({ cache: 'no-store' })`, `dynamic = 'force-dynamic'`, hoặc các dynamic APIs khác như `headers()`, `cookies()` hay không. Nếu route đã opt-out (không có tác dụng cache nên sẽ không thể bị stale ở đây), thì nguyên nhân không nằm ở Full Route Cache và cần chuyển sang điều tra các lớp cache khác bên dưới.

**Data Cache** — fetch bên trong có `revalidate` riêng lớn hơn 3600 không? Nếu có, thì do route revalidate sau 1 giờ nhưng dùng data từ cache vẫn fresh của fetch bên trong → Nên là HTML mới nhưng content cũ (chính xác là root cause của Debugging Scenario).

**Router Cache (phía client)** — xác nhận "content vẫn cũ sau 2 giờ" được đo như thế nào. User có thực sự reload trang (hard refresh) hay chỉ điều hướng trong ứng dụng bằng `Link`, `router.push()`, hoặc `back/forward` button? Trong trường hợp điều hướng phía client, nội dung có thể đang được phục vụ từ `Router Cache` thay vì `request mới` lên server. Chỉ khi tái hiện được vấn đề bằng một request mới hoàn toàn (reload hoặc mở tab mới), mới nên kết luận nguyên nhân nằm ở `server-side` cache.


```txt
User:
9:00 mở bài viết

9:05 publish content mới

11:00 bấm Back
11:00 bấm vào bài viết lần nữa
```

Người dùng nói:

> "Tôi vẫn thấy content cũ sau 2 giờ."

Nhưng thực tế:

```txt
11:00
↓
Không có request mới lên server
↓
Router Cache trả dữ liệu đã lưu
```

Nghĩa là **"2 giờ" là thời gian người dùng quan sát được**, không phải thời gian dữ liệu đã nằm trong cache server.

Vì vậy khi debug stale content, câu hỏi đầu tiên luôn là:

> "Người dùng thấy dữ liệu cũ sau 2 giờ" có nghĩa là sau 2 giờ và **một request mới**, hay chỉ sau 2 giờ và **vẫn ở trong cùng một phiên điều hướng client-side**?

---

**Câu hỏi chẩn đoán thêm** — đang dùng Next.js phiên bản nào? (Next.js 15 thay đổi một số hành vi caching mặc định so với v14, nên cần loại trừ khác biệt do version. Không caching `no-store` mặc định thay vì cache mặc định như trước). Khi publish bài viết mới, hệ thống có gọi `revalidateTag()` hoặc `revalidatePath()` hay không? Nếu không có cache invalidation chủ động, toàn bộ cơ chế cập nhật nội dung sẽ phụ thuộc vào timer (`revalidate: 3600`), dẫn đến việc người dùng phải chờ tới chu kỳ revalidation tiếp theo mới thấy dữ liệu mới.

> **Giải pháp** — không nên chỉ dựa vào time-based revalidation cho nội dung được cập nhật bởi CMS. Gắn tag cho dữ liệu bài viết và gọi `revalidateTag()` (hoặc `revalidatePath()`) ngay khi publish. Cách này chủ động invalidation Data Cache và Full Route Cache tại thời điểm mutation xảy ra, thay vì chờ hết 1 giờ mới được refresh.

Ví dụ:

```ts
// page.tsx
const post = await fetch(
  `https://cms/posts/${slug}`,
  {
    next: {
      tags: [`post:${slug}`],
      revalidate: 3600
    }
  }
)
```

```ts
// publish action / webhook
import { revalidateTag } from "next/cache"

export async function publishPost(slug: string) {
  await cms.publish(slug)

  revalidateTag(`post:${slug}`)
}
```

Kết quả:

```txt
❌ Chỉ dùng revalidate: 3600

Publish
    ↓
Chờ tối đa 1 giờ
    ↓
User mới thấy content mới


✅ Dùng revalidateTag()

Publish
    ↓
revalidateTag()
    ↓
Cache bị invalid ngay
    ↓
Request kế tiếp thấy content mới
```

Điểm cần ứng viên nhận ra là: **`revalidate: 3600` là chiến lược "eventually fresh", còn `revalidateTag()` / `revalidatePath()` là chiến lược "fresh ngay sau mutation".** Đây thường là đáp án mong đợi trong các hệ thống CMS, blog, e-commerce admin panel.


</details>

---

<details>
<summary><strong>Q6 [Mid]</strong> — Khi nào nên dùng <code>force-dynamic</code>? Khi nào KHÔNG nên?</summary>

**Điểm cần có:**

**Nên dùng khi:**
- Data per-user (dashboard, profile, notification feed cá nhân hoá)
- Data real-time (live price, live chat, stock ticker)
- Request phụ thuộc vào headers/cookies của request đó (session, locale, A/B test flag)

**KHÔNG nên dùng khi:**
- Blog post, docs, trang marketing — content giống nhau cho mọi user
- Product listing page — nên dùng ISR + `revalidateTag` khi update
- Bất kỳ trang nào có thể serve 1000 user bằng 1 HTML

**Hệ quả của dùng sai:** mọi request đập vào origin — không có gì được serve từ CDN edge. Dưới spike traffic, DB connection pool exhausted, toàn site sập.

**Heuristic:** *"Nếu 100 user mở cùng URL cùng lúc, có thể serve tất cả bằng 1 response không?"* — Nếu có: đừng dùng `force-dynamic`.

</details>


---

<details>
<summary><strong>Q7 [Mid-Senior]</strong> — Giải thích "serialization boundary" trong RSC. Những loại data nào KHÔNG thể truyền từ Server Component sang Client Component qua props?</summary>

**Điểm cần có:**

Server Component render trên server và gửi kết quả (RSC payload) sang client qua một format đặc biệt. Props truyền qua boundary phải **serializable** — có thể chuyển thành format gửi qua network.

**Không thể truyền:**
- **Function** — bao gồm callback handler thông thường (không thể serialize). *Exception:* Server Action được serialize như một reference endpoint URL.
- **Class instance** với method
- **Date object thô** — cần `.toISOString()` trước
- **`undefined`** — serialize thành `null` trong transport, dễ gây bug ngầm
- **Symbol, WeakMap, WeakSet**
- **React element chứa Server Component** bên trong

**Có thể truyền:** string, number, boolean, null, array, plain object (JSON-serializable)

```tsx
// Server Component
async function ServerParent() {
  const product = await db.product.findFirst({ where: { id } })
  return (
    <ClientChild
      name={product.name}                    // ✅ string
      price={product.price}                  // ✅ number
      createdAt={product.createdAt.toISOString()} // ✅ string — KHÔNG phải Date object
      onSave={handleSave}                    // ❌ function không serialize được
      instance={new ProductModel(product)}   // ❌ class instance
    />
  )
}
```

</details>

---

<details>
<summary><strong>Q8 [Senior]</strong> — Team đang migrate codebase Pages Router sang App Router. Auth lib hiện tại expose <code>getUserFromRequest(req: NextApiRequest)</code>. Kế hoạch migrate như thế nào để tránh dual maintenance?</summary>

**Điểm cần có:**

Chiến lược: tách auth lib thành 2 lớp — core logic (framework-agnostic) và adapter cho từng paradigm.

```ts
// lib/auth/core.ts — Pure business logic, KHÔNG import gì từ next/*
// Testable với unit test thuần Jest, không cần mock
export async function verifyAndDecodeToken(
  token: string | undefined
): Promise<User | null> {
  if (!token) return null
  try {
    return await jwtVerify(token, process.env.JWT_SECRET!)
  } catch {
    return null
  }
}
```

```ts
// lib/auth/server.ts — Adapter cho App Router
import { cookies } from 'next/headers'
import { verifyAndDecodeToken } from './core'

export async function getCurrentUser(): Promise<User | null> {
  const cookieStore = await cookies()
  return verifyAndDecodeToken(cookieStore.get('token')?.value)
}
```

```ts
// lib/auth/pages.ts — Adapter cho Pages Router (xóa khi migrate xong)
import type { NextApiRequest } from 'next'
import { verifyAndDecodeToken } from './core'

export async function getUserFromRequest(req: NextApiRequest): Promise<User | null> {
  return verifyAndDecodeToken(req.cookies.token)
}
```

**Lợi ích:** mọi thay đổi logic xác thực (rotate secret, thêm claim, đổi JWT library) chỉ sửa `core.ts`. Khi migrate xong, xóa `pages.ts` — không sửa gì ở `core.ts`. Business logic không bao giờ phải tồn tại song song ở 2 nơi.

</details>

---

<details>
<summary><strong>Q9 [Senior]</strong> — Giải thích Request Memoization trong Next.js. Khi nào hữu ích, khi nào không hoạt động như bạn nghĩ?</summary>

**Điểm cần có:**

Trong cùng một server request, nếu nhiều Server Component gọi `fetch()` với **cùng URL + cùng options**, Next.js chỉ thực sự thực hiện network call 1 lần — lần sau trả từ in-memory. Cơ chế này dùng React's `cache()` và Next.js's `fetch` monkey-patching.

```tsx
// RootLayout và DashboardPage cùng cần user — không double-fetch
async function RootLayout({ children }) {
  const user = await getCurrentUser() // fetch /api/me — thực sự gọi network
  return <html>...</html>
}

async function DashboardPage() {
  const user = await getCurrentUser() // cùng fetch /api/me → được memoize, trả từ memory
  return <div>Hello {user.name}</div>
}
```

**Khi KHÔNG hoạt động:**
- Fetch method là `POST` (không được deduplicate)
- Dùng `axios`, `got`, hoặc bất kỳ HTTP client nào khác — Next.js chỉ patch **native `fetch`**
- Fetch có `cache: 'no-store'` (opt out deduplication)
- **Memoization reset sau mỗi request** — không persist qua request khác (khác với Data Cache)

**Fix cho non-fetch function** (ví dụ: query DB trực tiếp):

```ts
import { cache } from 'react'

export const getCurrentUser = cache(async () => {
  return db.user.findFirst({ where: { id: getSessionId() } })
})
// Mỗi server request: getCurrentUser() gọi nhiều lần → DB query chỉ 1 lần
```

</details>


---

<details>
<summary><strong>Q10 [Senior-Architect]</strong> — Bạn review kiến trúc một Next.js SaaS: mọi business logic nằm trong <code>app/</code> route handlers và Server Actions, không có service layer. App đang ở 5.000 users, dự kiến scale tới 500.000. Bạn raise concern gì và đề xuất roadmap refactor thế nào?</summary>

**Điểm cần có:**

**Concerns kỹ thuật:**

Business logic trong route/action không testable với unit test — phụ thuộc vào Next.js internals (`next/cache`, `next/headers`). Nếu sau này cần expose API cho mobile app hoặc tách microservice, phải rewrite lại logic (vì hiện nó coupled với framework layer). Breaking change trong Next.js buộc refactor cả business logic lẫn infra cùng lúc.

**Concerns về team:**

Nhiều developer đụng vào cùng `actions.ts` của shared domain → merge conflict tăng tuyến tính. Không có clear ownership: *"ai chịu trách nhiệm khi billing logic thay đổi — nó nằm rải rác trong 3 file `actions.ts` khác nhau?"*

**Roadmap đề xuất — không "big rewrite":**

```
Phase 1 (2 tuần, không break production):
→ Tạo features/ directory
→ Với MỌI feature mới, viết service layer trong features/ TRƯỚC
→ Server Action gọi service — không implement logic trực tiếp
→ Quy tắc: actions.ts không được import prisma/db trực tiếp

Phase 2 (song song feature work, ~3 tháng):
→ Với mỗi actions.ts > 100 dòng: extract business logic sang features/[domain]/service.ts
→ Thêm unit test cho service layer — không cần mock Next.js
→ Ưu tiên domain quan trọng nhất (billing, auth) trước

Phase 3 (chỉ khi cần — mobile API, microservice):
→ Service layer đã "Next.js-free" → wrap bằng REST/tRPC mà không rewrite logic
→ Zero coupling: thay Next.js version hay thậm chí framework → service layer không đổi
```

**Không đề xuất:** *"Rewrite toàn bộ kiến trúc ngay"* — quá rủi ro ở giai đoạn growth. 5k users đang tăng là thời điểm tệ nhất để freeze feature dev.

**Câu phản biện hay gặp:** *"Tại sao không tách microservice ngay?"* → Ở 5k users, overhead của microservice (network latency, distributed tracing, eventual consistency, devops complexity) hiếm khi justified. Service layer trong monorepo cho phép scale team và logic rõ ràng, giữ deployment đơn giản cho đến khi **thực sự** cần tách.

</details>



## 12. Exercises

### Exercise 1 — Classify the Components [~30 phút]

Phân loại từng component: **(a) nên là Server Component, (b) phải là Client Component, (c) đang sai — cần refactor:**

```tsx
// Component A: SearchBar với debounce
'use client'
import { useState } from 'react'
export function SearchBar({ onSearch }: { onSearch: (q: string) => void }) {
  const [query, setQuery] = useState('')
  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setQuery(e.target.value)
    onSearch(e.target.value)
  }
  return <input value={query} onChange={handleChange} placeholder="Tìm kiếm..." />
}
```

```tsx
// Component B: ProductCard — chỉ render data
'use client'
export function ProductCard({ name, price, imageUrl }: Props) {
  return (
    <div>
      <img src={imageUrl} alt={name} />
      <h3>{name}</h3>
      <p>{price.toLocaleString('vi-VN')} đ</p>
    </div>
  )
}
```

```tsx
// Component C: UserGreeting — cần biết session
export async function UserGreeting() {
  const user = await getCurrentUser()
  if (!user) return <a href="/login">Đăng nhập</a>
  return <p>Xin chào, {user.name}!</p>
}
```

```tsx
// Component D: DashboardPage
'use client'
import { SummaryChart }    from './SummaryChart'     // không cần interactivity
import { RecentActivity }  from './RecentActivity'   // không cần interactivity
import { DateRangePicker } from './DateRangePicker'  // cần useState

export default function DashboardPage() {
  return (
    <div>
      <DateRangePicker />
      <SummaryChart />
      <RecentActivity />
    </div>
  )
}
```

<details>
<summary>Đáp án</summary>

**Component A — (b) + cần xem xét thêm:** `'use client'` đúng vì cần `useState`. Nhưng prop `onSearch` là function — nếu parent là Server Component, đây là serialization boundary problem (function không serialize được). Xem xét thay bằng URL search params: khi query thay đổi, update URL → Server Component tự re-render với params mới, không cần callback.

**Component B — (c) sai:** `'use client'` không cần thiết. `ProductCard` chỉ render data, không có hook hay event handler. Xóa `'use client'` → Server Component, zero JS gửi xuống client cho component này.

**Component C — (a) đúng:** `async` Server Component fetch session trực tiếp — pattern chuẩn cho data cần thiết khi render, không cần interactivity.

**Component D — (c) sai:** `'use client'` ở page-level kéo `SummaryChart` và `RecentActivity` vào bundle không cần thiết. Fix: giữ page là Server Component, tách `DateRangePicker` thành Client Component riêng — hai component kia không bị ảnh hưởng.

</details>

---

### Exercise 2 — Cache Audit [~45 phút]

Xác định cache layer đang active, vấn đề tiềm ẩn, và đề xuất fix:

```tsx
// app/shop/[category]/page.tsx
export const revalidate = 1800 // 30 phút

export default async function CategoryPage({
  params,
}: {
  params: Promise<{ category: string }>
}) {
  const { category } = await params

  const [products, banner] = await Promise.all([
    fetch(`https://api.shop.com/products?category=${category}`, {
      next: { revalidate: 300 }, // 5 phút
    }).then(r => r.json()),

    fetch(`https://cms.shop.com/banners/${category}`, {
      cache: 'no-store', // luôn fresh
    }).then(r => r.json()),
  ])

  return <CategoryView products={products} banner={banner} />
}
```

**Câu hỏi:**
- `revalidate: 1800` (route) vs `revalidate: 300` (fetch products) — layer nào quyết định?
- `cache: 'no-store'` trên banner fetch ảnh hưởng gì đến Full Route Cache của cả route?
- Admin thay banner lúc 10:00 → khách thấy banner mới khi nào?
- Products có thực sự được cache 5 phút như dự định không?

<details>
<summary>Đáp án</summary>

**Full Route Cache bị opt-out toàn bộ:** chỉ cần 1 fetch `cache: 'no-store'` trong route là đủ để opt-out Full Route Cache — route này thực chất chạy như `force-dynamic`, dù không khai báo. Mỗi request rebuild toàn trang.

**Products không được cache 5 phút như dự định:** vì Full Route Cache đã bị opt-out, `revalidate: 300` trên fetch products có nghĩa là Data Cache cho fetch đó — nhưng vì route luôn re-run, Data Cache vẫn được kiểm tra mỗi request (và chỉ thực sự gọi API sau 300s). Hiệu quả thực tế: products cached ở Data Cache layer, nhưng route vẫn chạy server-side mỗi request.

**Admin thay banner → khách thấy ngay** (vì `no-store`). Nhưng đây kéo theo cost: mọi request đều hit server, không ai được serve từ CDN edge.

**Fix nếu muốn có caching tốt hơn:**

```tsx
// Option A: Tách banner thành Suspense slot riêng
export default async function CategoryPage({ params }) {
  const { category } = await params
  const products = await fetch(`...products?category=${category}`, {
    next: { revalidate: 300, tags: [`category-${category}`] }
  }).then(r => r.json())

  return (
    <div>
      {/* Banner là Suspense slot riêng, always fresh, không drag down cả route */}
      <Suspense fallback={<BannerSkeleton />}>
        <CategoryBanner category={category} />   {/* Server Component riêng, no-store bên trong */}
      </Suspense>
      <ProductGrid products={products} />
    </div>
  )
}
```

</details>

---

### Exercise 3 — Migration Planning [~60 phút]

Cho `pages/checkout.tsx` sau, lên kế hoạch migrate sang App Router:

```tsx
// pages/checkout.tsx
import { getUserFromContext } from '@/lib/auth'
import { getCartItems }       from '@/lib/cart'
import { getShippingOptions } from '@/lib/shipping'

export async function getServerSideProps(context) {
  const user = await getUserFromContext(context)
  if (!user) return { redirect: { destination: '/login', permanent: false } }

  const [cart, shipping] = await Promise.all([
    getCartItems(user.id),
    getShippingOptions(user.address),
  ])

  return { props: { user, cart, shipping } }
}

export default function CheckoutPage({ user, cart, shipping }) {
  // form checkout: selected shipping method, voucher input, payment selection
  // nhiều useState, nhiều useEffect, gọi /api/checkout khi submit
}
```

**Câu hỏi:**

1. Shared code nào cần được update trước khi migrate page này?
2. `CheckoutPage` là Server hay Client Component sau khi migrate? Tại sao?
3. Phần nào nên là Server Component, phần nào phải là Client Component?
4. Middleware có thể thay thế redirect check không?

*(Tự làm — gợi ý: xem lại Q8 về cách tách auth lib, xem Refactoring Scenario về cách tách Server/Client boundary trong form phức tạp)*



## 13. Summary

### 3 Điều Quan Trọng Nhất Từ Bài Này

**1. Nguồn gốc của hiểu lầm quan trọng hơn bản thân hiểu lầm.**

Không ai "sai" khi dùng `getServerSideProps` trong App Router — họ đang áp một pattern từ kỷ nguyên khác. Khi bạn thấy mình hoặc đồng nghiệp làm điều gì đó "có vẻ sai", câu hỏi đầu tiên không phải *"tại sao làm vậy?"* mà là *"pattern này đúng ở context nào, và tại sao context đó không còn apply nữa?"* — điều này chuyển code review từ chỉ trích thành chuyển giao mental model.

**2. Lăng Kính 3 Chiều là công cụ kiểm tra chủ động, không phải checklist thụ động.**

Với MỌI đoạn code Next.js bạn viết hoặc review, 30 giây hỏi 3 câu: Chiều 1 — *đoạn code này chạy ở đâu, khi nào?* Chiều 2 — *cache layer nào active, hết hạn thế nào, ai invalidate?* Chiều 3 — *behavior này còn đúng ở version đang dùng không?* Có thể ngăn nhiều giờ debug sau đó.

**3. Hiểu lầm về caching là loại "động" — cần re-verify theo version.**

Khác với các nhóm khác (sai về execution model, về migration), caching behavior đã thay đổi **mặc định** qua từng major version. Điều đúng ở Next.js 13 về fetch caching có thể sai ở Next.js 15. Bất cứ khi nào upgrade version hoặc đọc tutorial về cache, hỏi ngay: *"tutorial này viết cho version nào?"*

---

### Bản Đồ Module — "Điều Trị Tận Gốc"

| Hiểu lầm | Module điều trị |
|---|---|
| #1 — Next.js = React + Router | **M1** (Next.js Architecture), **M6** (RSC Deep Dive) |
| #2 — Tutorial cũ áp được | **M0.2** (Version Recognition), **M2** (Legacy Codebase) |
| #3 — App Router = đổi folder | **M2** (Migration), **M3** (Rendering Models) |
| #4 — Migrate route lẻ tẻ | **M2.9** (Migration Strategy), **M15** (Architecture) |
| #5 — `'use client'` = chỉ browser | **M6.1** (RSC Execution Model) |
| #6 — `'use client'` không hệ quả | **M6.6** (Bundle Analysis), **M11.1** (Performance) |
| #7 — RSC luôn tốt hơn | **M6.5** (Boundary Patterns) |
| #8 — `revalidate` set 1 lần là xong | **M7.11** (Cache Invalidation), **M16.2** (Stale Data Debug) |
| #9 — `force-dynamic` cho "an toàn" | **M7.10** (Cache Strategy), **M11** (Performance) |
| #10 — 1 kiến trúc cho mọi quy mô | **M15** (Architecture Patterns), **M15.2** (Team Scaling) |

---

### "Câu Hỏi Tự Kiểm" Trước Khi Commit Bất Kỳ Next.js Code Nào

```
□ Component này chạy ở đâu? (Server / Client / cả hai khi SSR?)
□ Nếu 'use client' — đây có phải component NHỎ NHẤT cần nó không?
□ Nếu Server Component — nó có thể async và tự fetch không?
□ Data này được cache ở tầng nào? Tôi có kiểm soát invalidation không?
□ Khi mutation xảy ra, tôi gọi revalidateTag/revalidatePath ở đâu?
□ Pattern này lấy từ đâu? Tutorial/doc đó viết cho version nào?
□ Nếu thêm 'use client' ở đây — subtree bên dưới bị ảnh hưởng thế nào?
□ Route này có cần force-dynamic không — hay ISR + revalidateTag đủ?
```

---

**Module tiếp theo — M1: Next.js Architecture** — đi sâu vào execution model mà Hiểu lầm #1, #5, #7 chỉ chạm đến bề mặt: làm thế nào Server Component và Client Component thực sự được compile, bundle, và execute — từ `next build` đến byte đầu tiên gửi xuống browser.

---

