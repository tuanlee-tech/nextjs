---
title: 'M0.3 — Các Công Ty Thực Sự Dùng Next.js Như Thế Nào'
description: 'Phá vỡ ảo tưởng tutorial: 5 kiến trúc thực tế mà công ty dùng Next.js trong production — từ startup 3 người đến enterprise 500 dev.'
outline: deep
tags:
  [
    'nextjs',
    'architecture',
    'production',
    'patterns',
    'bff',
    'saas',
    'monorepo',
    'real-world',
  ]
---

# M0.3 — Các Công Ty Thực Sự Dùng Next.js Như Thế Nào

## 0. Tóm Tắt

<details>
<summary>Thời gian đọc: 5 phút</summary>

Next.js trong production không phải 1 kiến trúc duy nhất — có 5 patterns phổ biến: Full-Stack Monolith, Decoupled Frontend, BFF, Content Platform, và Multi-tenant SaaS. Chọn pattern phụ thuộc vào team size, backend hiện có, SEO needs, và multi-tenant requirements. Tutorial dạy 1 cách — production cần đọc context để hiểu tại sao codebase được thiết kế như vậy.

**📄 LESSON SUMMARY TEMPLATE**

**📌 Lesson Summary — M0.3: Các Công Ty Thực Sự Dùng Next.js Như Thế Nào**

---

**1. Lesson Goal (Học xong phải nhớ gì?)**

- Nhìn job description biết ngay công ty dùng Next.js theo kiến trúc nào
- Hiểu tại sao 2 công ty cùng dùng Next.js nhưng codebase khác hoàn toàn
- Không bị shock khi codebase thực tế khác xa tutorial
- Đặt câu hỏi đúng trong interview: infrastructure, team structure, deployment
- Đưa ra quyết định kiến trúc khi được hỏi "thiết kế hệ thống này như thế nào?"

👉 1 câu:

> Sau bài này tôi phải có khả năng **đọc context của bất kỳ codebase Next.js nào và hiểu tại sao nó được thiết kế theo pattern đó**.

---

**2. Evolution Context (Bài này xuất hiện để giải quyết vấn đề gì?)**

**Trước đó**

- Tutorial dạy xây 1 thứ cụ thể (todo app, blog cá nhân)
- Developer nghĩ "dùng Next.js" = làm theo 1 pattern duy nhất

↓

**Vấn đề**

- Production = vô số thứ cụ thể khác nhau, không ai giống ai
- Join công ty → codebase khác hoàn toàn tutorial → shock, không biết đọc
- Interview bị hỏi kiến trúc → trả lời theo tutorial → fail

↓

**Giải pháp**

- Bản đồ 5 kiến trúc thực tế mà công ty dùng
- Decision framework để chọn pattern phù hợp
- Signal nhận diện pattern từ codebase và job description

---

**3. Mental Model (Ý tưởng cốt lõi)**

> Next.js là **infrastructure layer**, không phải sản phẩm. Sản phẩm thực sự là business logic bên trên.

Analogy: Next.js = khung nhà. Tutorial dạy dựng khung. Production còn phải lo: điện, nước, nội thất, PCCC, an ninh — và tất cả ảnh hưởng đến cách dựng khung.

---

**4. Core Concepts (20% kiến thức quan trọng nhất)**

**Concept 1 — 5 Patterns Thực Tế**

| Pattern                    | Nhận diện                                              | Tốt cho                  | Risk                               |
| -------------------------- | ------------------------------------------------------ | ------------------------ | ---------------------------------- |
| **A. Full-Stack Monolith** | `prisma/`, `actions/`, Vercel                          | Startup, MVP, <20 người  | DB connections, không reusable API |
| **B. Decoupled Frontend**  | `lib/api/client.ts`, KHÔNG prisma                      | Công ty có backend riêng | Type duplication, coordination     |
| **C. BFF**                 | `/services/*.ts`, `Promise.all` trong API routes       | Microservices            | Single point of failure            |
| **D. Content Platform**    | `generateStaticParams`, CMS client, revalidate webhook | Media, blog, docs        | ISR invalidation complexity        |
| **E. Multi-tenant SaaS**   | `middleware.ts` detect subdomain, tenant queries       | B2B SaaS                 | Data isolation, cache pollution    |

**Concept 2 — Cách Chọn Pattern**

```
Team size?
  1-10  → A (ít overhead)
  10-30 → A→B transition
  30+   → B hoặc C

Có mobile app?
  Không → A (Server Actions OK)
  Có/sẽ có → B (REST/GraphQL từ đầu)

SEO quan trọng?
  Không → Bất kỳ
  Có → D hoặc SSG phần của A/B

Nhiều tenant?
  Không → A/B/C
  Có → E
```

**Concept 3 — Evolution Timeline**

```
2019-2020: SSR cho React → Pages Router + getServerSideProps
2021-2022: Framework mọi thứ → API Routes, ISR, Vercel boom
2023-2024: App Router revolution → Adoption chậm, nhiều công ty vẫn Pages Router
2025-nay: Pragmatic adoption → App Router cho greenfield, Pages Router vẫn serve hàng triệu user
         Next.js 16 breaking changes (async params, cookies, headers) ảnh hưởng MỌI codebase
```

👉 Công thức / sơ đồ:

```
TUTORIAL MINDSET                    PRODUCTION MINDSET
    │                                    │
    ▼                                    ▼
Next.js App                        Business Problem
├── pages/app                      └── Giải pháp kỹ thuật
├── components                         ├── Next.js (1 phần)
├── styles                             ├── Database
└── public                             ├── Auth
                                       ├── CDN
                                       ├── Monitoring
                                       ├── CI/CD
                                       └── Infrastructure
```

---

**5. Legacy Recognition (Nhìn code biết gì?)**

Dấu hiệu nhận biết

✅

- `prisma/schema.prisma` → A hoặc E
- `db.product.findMany()` trong page → A (DB direct access)
- `serverFetch('/api/products')` trong page → B (External API)
- `/services/product-service.ts` → C (BFF microservices)
- `createClient()` (Contentful/Sanity) → D (Headless CMS)
- `middleware.ts` check hostname → E (Multi-tenant)
- `generateStaticParams` nhiều chỗ → D (SSG content)
- `revalidateTag` trong webhook → D (CMS invalidation)
- `Promise.all([serviceA, serviceB])` trong API route → C (BFF aggregation)
- `process.env.API_URL` + fetch wrapper → B (Decoupled)
- `.env` có nhiều `*_SERVICE_URL` → C (Microservices BFF)

❌

- Nghĩ "dùng Next.js" = 1 pattern duy nhất
- Expect codebase giống tutorial
- Bỏ qua `middleware.ts` → miss multi-tenant detection
- Bỏ qua `prisma/` → assume decoupled khi thực ra monolith

---

**6. Modern Approach (Ngày nay làm thế nào?)**

| Legacy                           | Modern                                                       |
| -------------------------------- | ------------------------------------------------------------ |
| Pages Router (vẫn phổ biến ~40%) | App Router (greenfield, ~35% basic, ~20% Server Actions)     |
| getServerSideProps               | Server Components + async data fetching                      |
| API Routes cho backend           | Server Actions (Pattern A) hoặc tách API riêng (Pattern B/C) |
| Vercel-only deployment           | Docker + standalone output cho self-host                     |
| ISR cache local per instance     | Redis shared cache cho multi-instance                        |
| `unstable_cache`                 | `use cache` + Cache Components (v16)                         |
| `revalidatePath`                 | `revalidateTag` (v13.4+, better cho shared content)          |

---

**7. Production Scenario (Thực tế)**

**Ví dụ: Startup 15 người, e-commerce B2C**

- **Context:** Team nhỏ, cần MVP nhanh, budget hạn chế
- **Problem:** Chọn kiến trúc nào để ship trong 3 tháng nhưng không paint vào góc khi cần mobile app sau 6-12 tháng?
- **Root Cause:** Chọn A thuần → Server Actions lock business logic trong Next.js → mobile team không reuse được
- **Solution:** Pattern A nhưng business logic trong `lib/api/*`, expose `/api/v1/*` từ đầu. Server Actions chỉ là thin wrapper.
- **Lesson:** Không cần over-engineer từ đầu, nhưng cần abstraction layer để migration sau này không phải rewrite

👉 Rule

> Nếu gặp **"startup cần ship nhanh nhưng plan mobile app sau"** hãy nghĩ ngay đến **Pattern A với API layer abstraction từ đầu**.

---

**8. Debugging Scenario (Debug nhanh)**

Checklist

- □ Có `prisma/`? → Xác định Pattern A/E, check connection pooling (PgBouncer)
- □ Có `/services/*.ts`? → Pattern C, check circuit breaker và timeout
- □ Có `middleware.ts`? → Pattern E, check tenant lookup cache
- □ Có `generateStaticParams`? → Pattern D, check revalidate webhook
- □ Có `process.env.API_URL`? → Pattern B, check CORS và proxy routes
- □ ISR stale? → Check `X-Nextjs-Cache` header, verify revalidate chain
- □ Connection pool exhausted? → Singleton Prisma + PgBouncer cho serverless
- □ Double network hop? → Proxy qua `/api/proxy/*`, không expose backend URL

Lệnh hay dùng

```bash
# Check ISR cache status
curl -I https://site.com/page | grep X-Nextjs-Cache

# Test revalidation webhook
curl -X POST https://site.com/api/revalidate   -H "x-webhook-secret: $SECRET"   -d '{"slug":"article-1","contentType":"article"}'

# Check Prisma connections
npx prisma studio
# Hoặc query DB: SELECT count(*) FROM pg_stat_activity;

# Docker build standalone
next build  # với output: 'standalone' trong next.config.ts
docker build -t nextjs-app .
```

---

**9. Refactoring Scenario (Nhìn code cũ sẽ làm gì?)**

Checklist

1. **Identify pattern hiện tại** — Dùng 4 câu hỏi: có prisma? có services? có CMS? có middleware?
2. **Determine target pattern** — Dựa trên team size, mobile needs, backend team
3. **Extract API layer song song** — Tạo `/api/v1/*` mirror Server Actions, không break existing
4. **Thin the Actions** — Refactor Actions thành thin wrapper call internal API
5. **Evaluate migration cost** — 6-12 tháng cho A→B, không Big Bang

Không sửa khi

- Chỉ có 1-2 engineers → Pattern A vẫn đúng, đừng over-engineer
- Chưa có mobile app plan → Không cần tách API ngay
- Backend team chưa tồn tại → Không thể làm Pattern B/C
- Budget không cho phép DevOps → Vẫn dùng Vercel, đừng self-host

---

**10. Architecture Notes (Decision Framework)**

**Khi nên dùng**

- **Pattern A:** Team <10, MVP, không có backend team, cần ship nhanh
- **Pattern B:** Công ty có existing backend (Java/Go/.NET), team frontend riêng
- **Pattern C:** 3+ microservices, cần aggregation, team đủ lớn để maintain BFF
- **Pattern D:** Content-heavy, SEO-critical, writers không biết code
- **Pattern E:** B2B SaaS, nhiều tenants, cần branding/data isolation

**Khi không nên dùng**

- **Pattern A:** Traffic >10K req/ngày, complex features (payment, workflows), cần scale backend/frontend độc lập
- **Pattern B:** Không có backend team → thêm network hop vô ích
- **Pattern C:** Chỉ có 1-2 services → overkill, thêm complexity không cần thiết
- **Pattern D:** App nội bộ, dashboard → không cần SEO/SSG
- **Pattern E:** Single tenant → thêm complexity không cần

**Trade-off**

Pros

- Biết pattern → đọc được MỌI codebase Next.js
- Chọn đúng pattern → ship nhanh, maintain dễ
- Hiểu context → đặt câu hỏi đúng trong interview

Cons

- Nhiều pattern để nhớ → cần practice với codebase thực tế
- Context quyết định pattern → không có "đáp án đúng" universal
- Migration giữa patterns tốn thời gian và resource

---

**11. Interview Notes**

Junior

- Tại sao công ty dùng Next.js nhưng không có `/app/api/`? → Pattern B (Decoupled Frontend)
- Trade-off của Pattern A (Prisma + Server Actions + Vercel)? → Type safety, iteration speed vs DB connections, vendor lock-in

Mid

- 3 microservices + Next.js → suggest Pattern C (BFF). Tại sao? Aggregation, auth gateway, hide service topology
- BFF cần gì để production-ready? Monitoring, circuit breaker, team ownership, không thành mini monolith

Senior

- Pattern A → mobile app: incremental migration, không Big Bang. Extract API layer song song, thin the Actions, evaluate sau
- Self-host Next.js với ISR + Image Optimization: khả thi nhưng mất Edge Runtime, cần Redis shared cache, tự setup CDN/monitoring

👉 Một câu trả lời ngắn

> "Next.js là infrastructure layer. Chọn pattern dựa trên team size, backend hiện có, và tương lai product — không phải vì tutorial dạy cách nào."

---

**12. Exercises**

Sau bài này nên làm

- □ Đọc 5 job description Next.js → xác định pattern cho mỗi cái
- □ Clone 1 open-source Next.js project → identify pattern từ codebase
- □ Thiết kế kiến trúc cho 1 startup giả định → chọn pattern + justify
- □ Setup Docker standalone cho 1 Next.js app → hiểu self-host trade-offs

Nếu làm được nghĩa là đã hiểu.

---

**13. Summary (One Page)**

**Những gì phải nhớ**

- Next.js = infrastructure, không phải sản phẩm
- 5 patterns: A (Monolith), B (Decoupled), C (BFF), D (Content), E (Multi-tenant)
- Chọn pattern = function(team size, backend exists, mobile needs, SEO, tenants)
- Tutorial dạy 1 cách — production cần đọc context
- Migration A→B: incremental, extract API layer song song, thin the Actions
- Self-host: dùng `output: 'standalone'`, cần Redis cho ISR multi-instance
- Next.js 16: async params/cookies/headers, `use cache`, Build Adapters API

---

**Keyword**

```
Full-Stack Monolith
Decoupled Frontend
BFF (Backend for Frontend)
Content Platform
Multi-tenant SaaS
Server Actions
ISR (Incremental Static Regeneration)
SSG (Static Site Generation)
Connection Pooling
PgBouncer
Standalone Output
revalidateTag
Circuit Breaker
Microservices Aggregation
```

---

**Flow**

```
Nhận requirement
    ↓
Đặt 5 câu hỏi (team size, mobile, SEO, tenants, backend)
    ↓
Chọn pattern (A/B/C/D/E)
    ↓
Identify signal trong codebase
    ↓
Implement / Debug / Refactor
    ↓
Evaluate migration path nếu cần
```

---

**Golden Rules**

- ⭐ "Dùng Next.js" không có nghĩa là 1 pattern duy nhất — hỏi "dùng để làm gì?"
- ⭐ Team topology quan trọng hơn technical purity — không có team BFF thì đừng làm BFF
- ⭐ Business logic trong abstraction layer (`lib/api/*`) — không lock vào Server Actions
- ⭐ Self-host được nhưng phải tự build những gì Vercel cung cấp sẵn
- ⭐ Pages Router vẫn serve hàng triệu user — đừng assume phải migrate

---

**Một câu chốt bài**

> Tutorial dạy bạn xây một thứ. Production là bạn phải hiểu tại sao thứ đó được xây theo cách đó, và khi nào nên xây theo cách khác.

---

**Ví dụ mức độ ngắn gọn**

```
Tutorial
↓
1 pattern (giống nhau)

Production
↓
5 patterns (khác nhau)
↓
Chọn = f(team, backend, mobile, SEO, tenants)

↓

Đọc codebase → 4 câu hỏi → biết pattern
```

</details>

## 1. Lesson Goal

Sau bài này, bạn có thể:

- **Nhìn vào job description** và biết ngay công ty đó dùng Next.js theo kiến trúc nào
- **Hiểu tại sao** hai công ty cùng dùng Next.js nhưng code lại khác nhau hoàn toàn
- **Không bị shock** khi codebase thực tế khác xa những gì tutorial dạy
- **Đặt câu hỏi đúng** trong technical interview: về infrastructure, team structure, deployment
- **Đưa ra quyết định kiến trúc** khi được hỏi "Chúng ta nên thiết kế hệ thống này như thế nào?"

> **Vấn đề cốt lõi:** Tutorial dạy bạn xây một thứ cụ thể. Production là vô số thứ cụ thể khác nhau. Bài này là bản đồ của khoảng cách đó.

## 2. Evolution Context

### Tại sao Next.js được chọn — và chọn vì lý do gì

Không phải công ty nào chọn Next.js đều vì cùng một lý do. Đây là điều quan trọng nhất để hiểu.

```
Công ty A (E-commerce):
  Vấn đề: Google không index được SPA React
  Giải pháp: Next.js vì SSR/SSG → SEO tốt
  Họ dùng: 80% SSG, 20% SSR, App Router optional

Công ty B (SaaS Dashboard):
  Vấn đề: Cần build admin panel nhanh, không cần SEO
  Giải pháp: Next.js vì routing, API routes, DX tốt
  Họ dùng: 90% CSR (Client Side Rendering), Next.js gần như chỉ là router

Công ty C (Startup):
  Vấn đề: Team 3 người, cần full-stack nhanh
  Giải pháp: Next.js vì không cần backend riêng ban đầu
  Họ dùng: Server Actions, API Routes, Prisma, Vercel

Công ty D (Enterprise):
  Vấn đề: Legacy .NET backend, cần modern frontend
  Giải pháp: Next.js là "frontend layer" trên existing API
  Họ dùng: Next.js chỉ fetch từ .NET API, không có API Routes
```

**Lesson:** Khi thấy Next.js trong job requirement, câu hỏi phải hỏi không phải "Họ dùng Next.js", mà là **"Họ dùng Next.js để làm gì?"**

### Tiến hóa của việc sử dụng Next.js trong industry

```
2019–2020: "SSR cho React" — Companies chọn Next.js vì SSR
  → Hầu hết dùng Pages Router + getServerSideProps
  → App Router chưa tồn tại

2021–2022: "Framework cho mọi thứ" — Companies dùng Next.js rộng hơn
  → API Routes cho simple backend
  → ISR cho content sites
  → Vercel deployment boom

2023–2024: "App Router revolution" — Kỳ vọng cao, adoption chậm
  → Nhiều công ty vẫn ở Pages Router (stable, production-proven)
  → Startup mới → App Router
  → Company cũ → "Sẽ migrate sau"

2025–hiện tại: "Pragmatic adoption" — Reality check
  → App Router phổ biến cho "greenfield" (dự án mới xây dựng từ đầu)
  → Nhiều codebase vẫn Pages Router — không migrate vì cost > benefit
  → Next.js 15/16 có cả breaking changes (async `params`, `cookies()`, `headers()`) và features opt-in (`use cache`, `PPR`). Mọi codebase đang ở v15+ đều bị ảnh hưởng bởi breaking changes, không chỉ "những ai cần cutting-edge features."
```

## 3. Mental Model — Next.js Là Infrastructure (Cơ sở hạ tầng), Không Phải Sản Phẩm

**Sai lầm phổ biến nhất của developer mới:** Nghĩ rằng "dùng Next.js" có nghĩa là xây thứ gì đó trông giống tutorial.

**Thực tế:** Next.js là một **infrastructure layer**. Sản phẩm thực sự là business logic bên trên.

```
TUTORIAL MINDSET:
  Next.js App
  ├── pages (hoặc app)
  ├── components
  ├── styles
  └── public

PRODUCTION MINDSET:
  Business Problem
  └── Giải pháp kỹ thuật
      ├── Next.js (một phần của solution)
      ├── Database (Postgres, MySQL, MongoDB...)
      ├── Auth (NextAuth, Auth0, Clerk, custom...)
      ├── CDN (Cloudflare, CloudFront...)
      ├── Monitoring (Datadog, Sentry...)
      ├── CI/CD (GitHub Actions, Jenkins...)
      └── Infrastructure (Vercel, Docker, K8s...)
```

**Analogy:** Next.js giống như khung nhà. Tutorial dạy bạn dựng khung. Công ty thực tế phải lo: điện, nước, nội thất, hệ thống PCCC, an ninh, bảo trì — và tất cả những thứ đó có ảnh hưởng đến cách dựng khung.

## 4. Core Concepts — 5 Kiến Trúc Thực Tế

### 📐 Pattern A — Full-Stack Monolith (Startup / SMB - Small and Medium-sized Business) : Doanh nghiệp nhỏ và vừa

**Phổ biến ở:** Startup giai đoạn đầu, freelance project, MVP, công ty <20 người

**Đặc điểm:** Next.js làm TẤT CẢ — frontend, backend logic, database access, auth

```
startup-app/
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── register/page.tsx
│   ├── (dashboard)/
│   │   ├── layout.tsx
│   │   └── dashboard/page.tsx
│   └── api/           ← Backend sống trong Next.js
│       ├── auth/
│       │   └── [...nextauth]/route.ts
│       └── webhooks/
│           └── stripe/route.ts
├── actions/           ← Server Actions thay thế nhiều API endpoints
│   ├── user.ts
│   ├── product.ts
│   └── order.ts
├── lib/
│   ├── db.ts          ← Prisma client
│   ├── auth.ts        ← Auth config
│   └── stripe.ts      ← Payment
└── prisma/
    └── schema.prisma  ← Database schema ngay trong repo
```

```typescript
// actions/product.ts — Server Action trong Pattern A
// Database access TRỰC TIẾP từ Next.js layer
'use server';

import { db } from '@/lib/db';
import { auth } from '@/lib/auth';
import { revalidatePath } from 'next/cache';
import { z } from 'zod';

const CreateProductSchema = z.object({
  name: z.string().min(1).max(100),
  price: z.number().positive(),
  description: z.string().optional(),
});

export async function createProduct(formData: FormData) {
  // Auth check thẳng trong Server Action
  const session = await auth();
  if (!session?.user) throw new Error('Unauthorized');

  const input = CreateProductSchema.parse({
    name: formData.get('name'),
    price: Number(formData.get('price')),
    description: formData.get('description'),
  });

  // Prisma query thẳng — không qua API layer nào
  const product = await db.product.create({
    data: {
      ...input,
      createdById: session.user.id,
    },
  });

  revalidatePath('/dashboard/products');
  return product;
}
```

```tsx
// app/(dashboard)/dashboard/page.tsx — Data fetching Pattern A
// Server Component fetch thẳng từ DB — không qua REST API
import { db } from '@/lib/db';
import { auth } from '@/lib/auth';
import { redirect } from 'next/navigation';

export default async function DashboardPage() {
  const session = await auth();
  if (!session) redirect('/login');

  // Direct DB query — không có "API layer"
  const [products, orders, revenue] = await Promise.all([
    db.product.findMany({
      where: { createdById: session.user.id },
      orderBy: { createdAt: 'desc' },
      take: 10,
    }),
    db.order.count({
      where: { userId: session.user.id },
    }),
    db.order.aggregate({
      where: { userId: session.user.id, status: 'COMPLETED' },
      _sum: { total: true },
    }),
  ]);

  return (
    <div>
      <h1>Dashboard</h1>
      {/* Render data */}
    </div>
  );
}
```

**Tại sao Pattern A tồn tại:**

```
PRO:
  + Đơn giản nhất — một repo, một deploy, một team
  + Không có network latency giữa frontend và backend (cùng process)
  + Type safety end-to-end tự nhiên (Prisma → TypeScript → Component)
  + Vercel deployment trivial
  + Iteration speed cực nhanh ở giai đoạn đầu

CON:
  - Không scale tốt khi team lớn (ai cũng có access vào DB logic)
  - Database connection management phức tạp trên serverless
  - Không thể reuse API cho mobile app (nếu cần)
  - Vendor lock-in vào Vercel ecosystem (nếu không cẩn thận)
  - Một Next.js deploy = một monolith to tướng
```

**Signal trong job posting:**

> _"Next.js, Prisma, PostgreSQL, Vercel, Server Actions"_

→ Pattern A. Team nhỏ, move fast, full-stack engineers.

---

### 📐 Pattern B — Frontend Layer (Decoupled)

**Phổ biến ở:** Công ty đã có backend (Java, .NET, Python, Go), team frontend riêng

**Đặc điểm:** Next.js chỉ là **presentation layer**.Có thể có **minimal proxy routes** (/api/proxy/\*) để forward requests tới backend mà không expose backend URL ra client. Không có DB access. Tất cả data từ external API.

```
enterprise-frontend/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── products/
│       └── [id]/page.tsx
├── lib/
│   ├── api/
│   │   ├── client.ts        ← Configured API client (axios/fetch wrapper)
│   │   ├── products.ts      ← Product API calls
│   │   └── users.ts         ← User API calls
│   └── types/
│       ├── product.ts       ← Types mirror backend DTOs
│       └── user.ts
└── next.config.ts
# KHÔNG CÓ:
# - /prisma
# - /actions (hoặc rất ít)
# - /app/api (hoặc chỉ proxy routes)
```

```typescript
// lib/api/client.ts — Configured API client
// Đây là pattern rất phổ biến ở enterprise

const BASE_URL = process.env.API_URL; // Internal backend URL

// Server-side API calls (trong Server Components)
export async function serverFetch<T>(
  endpoint: string,
  options?: RequestInit,
): Promise<T> {
  const response = await fetch(`${BASE_URL}${endpoint}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      // Service-to-service auth — không phải user token
      'X-Service-Key': process.env.SERVICE_KEY!,
      ...options?.headers,
    },
  });

  if (!response.ok) {
    throw new Error(`API Error: ${response.status} ${endpoint}`);
  }

  return response.json();
}

// Client-side API calls (trong Client Components)
export async function clientFetch<T>(
  endpoint: string,
  options?: RequestInit,
): Promise<T> {
  // Next.js API route đóng vai trò PROXY
  // Không expose backend URL ra client
  const response = await fetch(`/api/proxy${endpoint}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options?.headers,
    },
    credentials: 'include', // Gửi session cookie
  });

  if (!response.ok) throw new Error(`API Error: ${response.status}`);
  return response.json();
}
```

```typescript
// app/api/proxy/[...path]/route.ts
// Proxy pattern — Next.js chỉ forward request
// Pattern B thường có proxy như thế này
import { NextRequest, NextResponse } from 'next/server';
import { auth } from '@/lib/auth';

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const session = await auth();
  if (!session)
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });

  // Forward to backend với user token
  const { path } = await params;
  const endpoint = path.join('/');
  const url = `${process.env.API_URL}/${endpoint}${request.nextUrl.search}`;

  const response = await fetch(url, {
    headers: {
      Authorization: `Bearer ${session.accessToken}`, // Forward user token
      'X-User-Id': session.user.id,
    },
  });

  const data = await response.json();
  return NextResponse.json(data, { status: response.status });
}

// POST, PUT, DELETE tương tự...
export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const session = await auth();
  if (!session)
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });

  const { path } = await params;
  const body = await request.json();

  const response = await fetch(`${process.env.API_URL}/${path.join('/')}`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${session.accessToken}`,
    },
    body: JSON.stringify(body),
  });

  const data = await response.json();
  return NextResponse.json(data, { status: response.status });
}
```

```typescript
// app/products/[id]/page.tsx — Server Component fetch từ external API
// Pattern B: không có db.product.findUnique — chỉ có API call
import { serverFetch } from '@/lib/api/client';
import type { Product } from '@/lib/types/product';

export default async function ProductPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;

  // Gọi Java/Go/.NET backend — không phải database
  const product = await serverFetch<Product>(`/products/${id}`);

  return (
    <article>
      <h1>{product.name}</h1>
      <p>{product.description}</p>
    </article>
  );
}
```

**Tại sao Pattern B tồn tại:**

```
PRO:
  + Backend team có thể làm việc độc lập (Go team, Java team...)
  + Next.js chỉ quan tâm đến UI — không bị lẫn business logic
  + Backend có thể serve nhiều client (web, mobile, partner API)
  + Dễ swap Next.js sang framework khác nếu cần
  + Security tốt hơn: DB credentials không cần ở frontend layer

CON:
  - Thêm một network hop (browser → Next.js → Backend)
  - Type duplication: phải tạo TS types mirror backend DTOs
  - Two team coordination: API contract thay đổi → frontend bị ảnh hưởng
  - Local development phức tạp hơn (cần chạy cả backend)
```

**Signal trong job posting:**

> _"Next.js frontend, REST API consumption, Java/Spring backend (separate team)"_
>
> _"Experience with API integration, OpenAPI/Swagger"_

→ Pattern B. Frontend-only role, backend là black box.

---

### 📐 Pattern C — BFF (Backend for Frontend)

**Phổ biến ở:** Công ty có microservices, cần aggregation layer, team medium-large

**Đặc điểm:** Next.js đóng vai trò **BFF** — nhận request từ client, gọi nhiều microservices, tổng hợp data, trả về.

```
BFF không phải là "proxy" — BFF có BUSINESS LOGIC:
  - Aggregation: Lấy data từ 3 services, merge thành 1 response
  - Transformation: Convert data format phù hợp với UI cần
  - Orchestration: Gọi service A, dùng kết quả gọi service B
  - Auth gateway: Verify token, enrich request với user context
```

```
bff-app/
├── app/
│   ├── layout.tsx
│   └── [feature]/page.tsx
├── app/api/
│   ├── dashboard/
│   │   └── route.ts      ← Aggregate từ 3 microservices
│   ├── checkout/
│   │   └── route.ts      ← Orchestrate cart + payment + inventory
│   └── search/
│       └── route.ts      ← Query search service + personalization
└── services/              ← Typed clients cho từng microservice
    ├── product-service.ts
    ├── inventory-service.ts
    ├── user-service.ts
    ├── order-service.ts
    └── recommendation-service.ts
```

```typescript
// services/product-service.ts — Typed client cho Product Microservice
const PRODUCT_SERVICE_URL = process.env.PRODUCT_SERVICE_URL;

interface Product {
  id: string;
  name: string;
  price: number;
  categoryId: string;
}

export const productService = {
  async getById(id: string): Promise<Product> {
    const res = await fetch(`${PRODUCT_SERVICE_URL}/products/${id}`, {
      headers: { 'X-Service-Name': 'nextjs-bff' },
      // Service mesh auth (mTLS trong production)
    });
    if (!res.ok) throw new Error(`Product not found: ${id}`);
    return res.json();
  },

  async getMany(ids: string[]): Promise<Product[]> {
    // Batch endpoint — tránh N+1 calls
    const res = await fetch(`${PRODUCT_SERVICE_URL}/products/batch`, {
      method: 'POST',
      body: JSON.stringify({ ids }),
      headers: {
        'Content-Type': 'application/json',
        'X-Service-Name': 'nextjs-bff',
      },
    });
    return res.json();
  },
};
```

```typescript
// app/api/dashboard/route.ts — BFF Aggregation
// Đây là trái tim của Pattern C
import { NextRequest, NextResponse } from 'next/server';
import { auth } from '@/lib/auth';
import { productService } from '@/services/product-service';
import { orderService } from '@/services/order-service';
import { analyticsService } from '@/services/analytics-service';
import { inventoryService } from '@/services/inventory-service';

export async function GET(request: NextRequest) {
  const session = await auth();
  if (!session)
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });

  const userId = session.user.id;

  // BFF PATTERN: Gọi nhiều services PARALLEL
  // Client chỉ cần 1 request thay vì 4 requests
  const [recentOrders, topProducts, analytics, lowStockAlerts] =
    await Promise.all([
      orderService.getRecent(userId, { limit: 5 }),
      productService.getTopSelling(userId, { limit: 10 }),
      analyticsService.getSummary(userId, { period: '30d' }),
      inventoryService.getLowStockAlerts(userId),
    ]);

  // BFF TRANSFORMATION: Merge và format data phù hợp với UI
  // Client không cần biết data đến từ 4 services khác nhau
  const dashboardData = {
    summary: {
      totalRevenue: analytics.revenue,
      orderCount: analytics.orderCount,
      topProductsCount: topProducts.length,
    },
    recentOrders: recentOrders.map((order) => ({
      id: order.id,
      // Flatten nested structure — client nhận format đơn giản hơn
      customerName: `${order.customer.firstName} ${order.customer.lastName}`,
      total: order.lineItems.reduce(
        (sum, item) => sum + item.price * item.quantity,
        0,
      ),
      status: order.status,
      createdAt: order.createdAt,
    })),
    topProducts,
    alerts: lowStockAlerts.filter((alert) => alert.quantity < 10),
    // Thêm computed fields mà không service nào cung cấp
    conversionRate: analytics.orderCount / analytics.visitCount,
  };

  return NextResponse.json(dashboardData);
}
```

**Tại sao Pattern C tồn tại:**

```
PRO:
  + Client chỉ cần 1 request thay vì nhiều requests riêng lẻ
  + Microservices thay đổi nội bộ không ảnh hưởng client
  + Dễ cache ở BFF layer
  + One place để handle cross-cutting concerns: logging, tracing, auth
  + Mobile và web có thể có BFF khác nhau (Mobile BFF vs Web BFF)

CON:
  - Thêm một layer cần maintain
  - BFF có thể trở thành bottleneck
  - Logic aggregation dễ bị trùng lặp nếu nhiều BFF
  - Team cần hiểu cả frontend lẫn service contracts
```

**BFF Không Phải API Gateway**

**Sự khác biệt quan trọng:**

- **API Gateway:** Reverse proxy, routing, rate limiting — generic cho mọi client
- **BFF:** Customized backend cho MỘT frontend — aggregation, transformation, auth enrichment
- **Có thể coexist:** API Gateway → BFF → Microservices

**Anti-pattern cần tránh:** BFF chứa business logic phức tạp → trở thành "mini monolith". BFF chỉ nên làm aggregation và transformation.

**Signal trong job posting:**

> _"Next.js, microservices architecture, API aggregation, GraphQL (optional)"_
>
> _"Experience with service-to-service communication"_

→ Pattern C. Thường gặp ở tech companies dùng microservices.

---

### 📐 Pattern D — Content Platform

**Phổ biến ở:** Media, news, blog, documentation site, marketing website

**Đặc điểm:** Tập trung vào **SEO, performance, CDN**. Pages được build trước (SSG/ISR). Content từ CMS.

```
content-platform/
├── app/
│   ├── layout.tsx
│   ├── page.tsx            ← Homepage, ISR 1 hour
│   ├── [category]/
│   │   └── page.tsx        ← Category page, ISR 30 min
│   └── [category]/[slug]/
│       └── page.tsx        ← Article page, ISR 5 min
├── lib/
│   └── cms/
│       ├── contentful.ts   ← Hoặc Sanity, Strapi, Prismic...
│       ├── queries.ts      ← GraphQL/REST queries for CMS
│       └── transforms.ts   ← Transform CMS data → app types
└── next.config.ts
# Đặc trưng: Không có user auth phức tạp
# Đặc trưng: Nhiều generateStaticParams
# Đặc trưng: revalidatePath/revalidateTag từ CMS webhook
```

```typescript
// lib/cms/contentful.ts — Headless CMS integration
import { createClient } from 'contentful';

const client = createClient({
  space: process.env.CONTENTFUL_SPACE_ID!,
  accessToken: process.env.CONTENTFUL_ACCESS_TOKEN!,
  // Preview token khác — dùng cho draft content
  ...(process.env.CONTENTFUL_PREVIEW_MODE && {
    accessToken: process.env.CONTENTFUL_PREVIEW_TOKEN!,
    host: 'preview.contentful.com',
  }),
});

// CMS query được wrap trong typed functions
export async function getArticle(slug: string) {
  const entries = await client.getEntries<ArticleFields>({
    content_type: 'article',
    'fields.slug': slug,
    include: 2, // Depth của linked entries
    limit: 1,
  });

  if (!entries.items.length) return null;
  return transformArticle(entries.items[0]);
}

export async function getAllArticleSlugs(): Promise<string[]> {
  const entries = await client.getEntries<ArticleFields>({
    content_type: 'article',
    select: ['fields.slug'], // Chỉ lấy slug — tối ưu bandwidth
    limit: 1000,
  });

  return entries.items.map((item) => item.fields.slug);
}
```

```typescript
// app/[category]/[slug]/page.tsx — Article page, Pattern D
import { getArticle, getAllArticleSlugs } from '@/lib/cms/contentful';
import { notFound } from 'next/navigation';

// ISR: Tái tạo trang sau 5 phút nếu có request
export const revalidate = 300;

// Pre-render top articles tại build time
// Các bài khác sẽ được generate khi có request đầu tiên
export async function generateStaticParams() {
  const slugs = await getAllArticleSlugs();
  // Chỉ pre-render 200 bài phổ biến nhất — tránh build time quá dài
  return slugs.slice(0, 200).map(slug => ({
    category: 'news', // Simplified — thực tế phức tạp hơn
    slug,
  }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  const article = await getArticle(slug);
  if (!article) return {};

  return {
    title: `${article.title} | TechNews`,
    description: article.excerpt,
    openGraph: {
      title: article.title,
      description: article.excerpt,
      images: [{ url: article.coverImage.url }],
      type: 'article',
      publishedTime: article.publishedAt,
    },
  };
}

export default async function ArticlePage({
  params,
}: {
  params: Promise<{ category: string; slug: string }>;
}) {
  const { slug } = await params;
  const article = await getArticle(slug);
  if (!article) notFound();

  return (
    <article>
      <h1>{article.title}</h1>
      <time>{new Date(article.publishedAt).toLocaleDateString('vi-VN')}</time>
      {/* Rich text render từ CMS */}
      <div dangerouslySetInnerHTML={{ __html: article.htmlContent }} />
    </article>
  );
}
```

```typescript
// app/api/revalidate/route.ts — Webhook từ CMS trigger revalidation
// Pattern D bắt buộc phải có endpoint này
// Khi editor publish bài mới → CMS gọi webhook → Next.js revalidate

import { NextRequest, NextResponse } from 'next/server';
import { revalidatePath, revalidateTag } from 'next/cache';

export async function POST(request: NextRequest) {
  // Verify webhook secret — chống abuse (lạm dụng)
  const secret = request.headers.get('x-webhook-secret');
  if (secret !== process.env.CMS_WEBHOOK_SECRET) {
    return NextResponse.json({ error: 'Invalid secret' }, { status: 401 });
  }

  const body = await request.json();
  const { contentType, slug, action } = body;

  // Revalidate dựa trên loại content bị thay đổi
  if (contentType === 'article') {
    // Invalidate article page cụ thể
    revalidatePath(`/news/${slug}`);
    // Invalidate listing pages (có thể có article mới xuất hiện)
    revalidatePath('/news');
    revalidatePath('/');
    // Invalidate bằng tag — cách tốt hơn nếu dùng v13.4+
    revalidateTag('articles');
  }

  return NextResponse.json({
    revalidated: true,
    timestamp: new Date().toISOString(),
  });
}
```

**Tại sao Pattern D tồn tại:**

```
PRO:
  + Performance đỉnh cao — pages được serve từ CDN edge
  + SEO hoàn hảo — HTML rendered sẵn, no JavaScript needed to index
  + Scale rất tốt — CDN absorbs traffic spike
  + Writer không cần biết code — CMS là editor

CON:
  - Build time tăng tỷ lệ thuận với số lượng pages
  - ISR revalidation có thể phức tạp để debug
  - Preview mode (draft content) cần setup riêng
  - Personalization khó — static content không có user context
```

**Signal trong job posting:**

> _"Next.js, Contentful/Sanity, ISR, SEO optimization, Core Web Vitals"_

→ Pattern D. SEO-critical site, content team.

---

### 📐 Pattern E — Multi-tenant SaaS

**Phổ biến ở:** B2B SaaS, platform products, agency tools

**Đặc điểm:** Một Next.js app phục vụ **nhiều tenants** (organizations). Mỗi tenant có thể có subdomain riêng, theme riêng, data isolation.

**Database Isolation Strategies**

1. **Database-per-tenant:** Mỗi tenant có DB riêng — isolation cao nhất, overhead lớn nhất
2. **Schema-per-tenant:** Một DB, nhiều schema (PostgreSQL) — balance giữa isolation và overhead
3. **Shared Database with Tenant ID:** Một DB, shared tables với tenant_id column — phổ biến nhất, cần RLS (Row Level Security) hoặc strict query filtering

```sql
-- PostgreSQL RLS example
-- RLS là best practice cho shared database model.
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON projects
  USING (tenant_id = current_setting('app.current_tenant_id')::uuid);
```

```
multitenant-saas/
├── app/
│   ├── layout.tsx          ← Root layout
│   └── [tenantSlug]/       ← Hoặc handle qua middleware + subdomain
│       ├── layout.tsx      ← Tenant-specific layout
│       ├── page.tsx
│       └── dashboard/
│           └── page.tsx
├── middleware.ts            ← QUAN TRỌNG: Tenant detection
├── lib/
│   ├── tenant.ts            ← Tenant resolution logic
│   └── db.ts               ← Tenant-aware DB queries
└── next.config.ts
```

```typescript
// middleware.ts — Tenant detection — Trái tim của Pattern E
// Chạy trên EVERY request trước khi đến route handler
import { NextRequest, NextResponse } from 'next/server';

export function middleware(request: NextRequest) {
  const hostname = request.headers.get('host') || '';
  const url = request.nextUrl.clone();

  // Subdomain detection: acme.app.com → tenant: "acme"
  // Hoặc custom domain: acme.com → lookup in DB
  const mainDomain = process.env.NEXT_PUBLIC_ROOT_DOMAIN; // "app.com"

  let tenantSlug: string | null = null;

  if (hostname.endsWith(`.${mainDomain}`)) {
    // Subdomain pattern: acme.app.com
    tenantSlug = hostname.replace(`.${mainDomain}`, '');
  } else if (hostname !== mainDomain && hostname !== `www.${mainDomain}`) {
    // Custom domain: Cần lookup từ DB/cache
    // Thực tế: Cache tenant mapping để tránh DB hit mỗi request
    tenantSlug = getCachedTenantByDomain(hostname);
  }

  if (tenantSlug) {
    // Rewrite URL để route đến tenant-specific handler
    // User thấy URL: acme.app.com/dashboard
    // Next.js nhận: /tenants/acme/dashboard
    url.pathname = `/tenants/${tenantSlug}${url.pathname}`;
    return NextResponse.rewrite(url);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

```tsx
/* Model:
--------------------------------------------   
Tenant {
  id    String @id @default(cuid())
  slug  String @unique

  settings     TenantSettings?
  subscription Subscription?
  branding     Branding?
}
--------------------------------------------   
*/

// lib/tenant.ts — Tenant context
import { cache } from 'react';
import { db } from './db';

// cache() memoize kết quả của một async function ACROSS COMPONENTS
// trong CÙNG MỘT SERVER REQUEST — cache tồn tại cho đến khi request kết thúc.

// Use case: Nhiều components trong cùng một page cần tenant data.
// Thay vì mỗi component gọi DB riêng, chỉ gọi 1 lần và share kết quả.
//
// ⚠️ Cache bị invalidate sau mỗi server request.
// Không dùng cho cross-request caching (dùng Redis/unstable_cache thay thế).
// ⚠️ Chỉ dùng trong Server Components.

export const getTenant = cache(async (slug: string) => {
  const tenant = await db.tenant.findUnique({
    where: { slug },
    include: {
      settings: true,
      subscription: true,
      branding: true,
    },
  });

  if (!tenant) return null;

  return {
    id: tenant.id,
    name: tenant.name,
    slug: tenant.slug,
    plan: tenant.subscription.plan,
    branding: {
      primaryColor: tenant.branding?.primaryColor ?? '#2563eb',
      logo: tenant.branding?.logoUrl ?? '/default-logo.svg',
    },
    features: getFeaturesByPlan(tenant.subscription.plan),
  };
});

function getFeaturesByPlan(plan: string) {
  const plans = {
    free: { maxUsers: 5, analytics: false, customDomain: false },
    pro: { maxUsers: 50, analytics: true, customDomain: false },
    enterprise: { maxUsers: Infinity, analytics: true, customDomain: true },
  };
  return plans[plan as keyof typeof plans] ?? plans.free;
}
```

```tsx
// app/tenants/[tenantSlug]/layout.tsx — Tenant-aware layout
import { getTenant } from '@/lib/tenant';
import { notFound } from 'next/navigation';

export default async function TenantLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ tenantSlug: string }>;
}) {
  const { tenantSlug } = await params;
  const tenant = await getTenant(tenantSlug);

  if (!tenant) notFound();

  return (
    <div
      // CSS custom properties cho tenant branding
      style={
        {
          '--primary-color': tenant.branding.primaryColor,
        } as React.CSSProperties
      }
    >
      <header>
        <img
          src={tenant.branding.logo}
          alt={tenant.name}
        />
        <nav>{/* Navigation dựa trên tenant.features */}</nav>
      </header>
      {children}
    </div>
  );
}
```

**Tại sao Pattern E tồn tại:**

```
PRO:
  + Một codebase, nhiều khách hàng — operationally (về mặt vận hành) đơn giản
  + Dễ ship features (triển khai tính năng) cho tất cả tenants cùng lúc
  + Middleware xử lý tenant routing gọn gàng

CON:
  - Bug của một tenant có thể ảnh hưởng tenants khác (shared infra)
  - Database isolation là vấn đề phức tạp nhất
  - Performance: Tenant lookup mỗi request → cần cache kỹ
  - Security: Cẩn thận tenant data leakage
```

**Signal trong job posting:**

> _"Multi-tenant architecture, subdomain routing, role-based access control"_
>
> _"Experience with B2B SaaS product"_

→ Pattern E. Phức tạp nhất, senior-level requirement.

## 5. Legacy Recognition — Đọc Pattern Từ Codebase

### Cách Nhận Diện Pattern Nhanh

Khi bạn mở một codebase mới, chỉ cần trả lời 4 câu hỏi:

```
CÂU HỎI 1: Có /prisma hoặc DB connection không?
  Có   →  Pattern A (Full-stack) hoặc E (Multi-tenant)
  Không →  Pattern B (Decoupled) hoặc C (BFF) hoặc D (Content)

CÂU HỎI 2: Có nhiều /services/*.ts files không?
  Có   →  Pattern C (BFF — nhiều microservices)
  Không →  Tiếp tục câu hỏi 3

CÂU HỎI 3: Có /lib/cms hoặc generateStaticParams nhiều không?
  Có   →  Pattern D (Content Platform)
  Không →  Tiếp tục câu hỏi 4

CÂU HỎI 4: Có middleware.ts detect subdomain/tenant không?
  Có   →  Pattern E (Multi-tenant)
  Không →  Pattern A hoặc B
```

### Signal Matrix Nhanh

| Tìm thấy trong codebase                                       | Pattern                      |
| ------------------------------------------------------------- | ---------------------------- |
| `prisma/schema.prisma`                                        | A hoặc E                     |
| `db.product.findMany()` trong page                            | A — DB direct access         |
| `serverFetch('/api/products')` trong page                     | B — External API             |
| `/services/product-service.ts`                                | C — BFF với microservices    |
| `createClient()` (Contentful/Sanity/Strapi)                   | D — Headless CMS             |
| `middleware.ts` check hostname                                | E — Multi-tenant             |
| `generateStaticParams` nhiều chỗ                              | D — SSG content site         |
| `revalidateTag` trong webhook endpoint                        | D — CMS content invalidation |
| `Promise.all([serviceA, serviceB, serviceC])` trong API route | C — BFF aggregation          |
| `process.env.API_URL` + fetch wrapper                         | B — Decoupled frontend       |
| `.env` có nhiều `*_SERVICE_URL`                               | C — Microservices BFF        |

## 6. Modern Approach — Xu Hướng 2024–2025

### Điều Tutorial Không Dạy Bạn

Đây là những thứ thực tế mà không có tutorial nào đề cập đến:

**1. Phần lớn codebase production KHÔNG dùng tính năng mới nhất**

```
Khảo sát (không chính thức) codebase công ty năm 2024–2025:
  - ~40% vẫn dùng Pages Router (stable, không cần migrate)
  - ~35% dùng App Router nhưng chỉ dùng basic features
  - ~20% dùng App Router với Server Actions
  - ~5% dùng cutting-edge features ( Ví dụ: PPR, "use cache")

→ Lesson: Học App Router là đúng, nhưng biết Pages Router vẫn quan trọng
→ "Use cache" (v16) đẹp lắm, nhưng nhiều công ty vẫn chưa cần

Tuy nhiên cần chú ý nếu dùng v16

Next.js 16 Breaking Changes — Ảnh hưởng đến MỌI codebase:
- params, searchParams, cookies(), headers() đều phải await (async)
- serverRuntimeConfig/publicRuntimeConfig removed
- next lint removed
- AMP support removed
→ Không phải "optional" — đây là breaking changes bắt buộc khi upgrade.

```

**2. Vercel không phải lựa chọn của mọi công ty**

```
Startup / Individual:  Vercel (rất phổ biến, DX tốt)
Mid-size company:      Docker + AWS ECS / Kubernetes
Enterprise:            Self-hosted, on-premise (vì compliance)
Banking / Finance:     Private cloud, không dùng public SaaS
```

```dockerfile
# Dockerfile — Pattern của công ty không dùng Vercel
# next build với output: 'standalone' trong next.config.ts

# "Standalone mode" - chế độ build đặc biệt của Next.js cho phép đóng gói
# ứng dụng thành một thư mục chứa mọi thứ cần thiết để chạy mà không cần
# cài đặt dependencies thủ công, giúp đơn giản hóa việc triển khai.

FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci

#COPY <source> <destination>
COPY . .
RUN npm run build

# Next.js sẽ tạo ra :
#
# .next/
#  ├─ standalone/
#  │   ├─ server.js
#  │   ├─ package.json
#  │   └─ node_modules/  (chỉ dependencies cần thiết)
#  │
#  └─ static/
#


# Production image — chỉ copy output cần thiết
FROM node:20-alpine AS runner
WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000

# Standalone output — không cần node_modules đầy đủ
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public

EXPOSE 3000
CMD ["node", "server.js"]
```

**Ví dụ dễ hình dung**

App ban đầu:

```txt
project
├── src
├── package.json
├── node_modules (1000 packages)
├── prisma
├── tests
├── docs
└── .next
```

Sau standalone:

```txt
container
├── server.js
├── node_modules (chỉ package cần chạy)
└── .next/static
```

**Vì sao công ty không dùng Vercel hay dùng standalone?**

Vercel tự tối ưu Next.js deployment.

Nhưng khi tự deploy:

- Docker - đóng gói ứng dụng và dependencies vào các container di động.
- Kubernetes (K8s) - một hệ thống quản lý và điều phối container (container orchestration) ở nhiều máy chủ.
- AWS ECS (Amazon Elastic Container Service) dùng để chạy và quản lý Docker container mà không cần tự quản lý server/container orchestration
- VM (Virtual Machine - Máy chủ ảo)
- bare metal (Máy chủ vật lý thật)

thì `output: 'standalone'` giúp:

- ✅ image nhỏ hơn
- ✅ build nhanh hơn
- ✅ deploy nhẹ hơn
- ✅ giảm attack surface
- ✅ không cần copy cả repo lên production

**Standalone = Next.js build ra một phiên bản "đóng gói độc lập" chỉ chứa những gì cần để chạy production, rất phù hợp khi tự đóng Docker image.**

**Next.js 16 Self-Hosting — Cache Handler:**
Khi self-host với multiple instances, cần custom cache handler:

```js
// next.config.js
module.exports = {
  cacheHandler: require.resolve('./cache-handler.js'),
  cacheMaxMemorySize: 0, // disable in-memory caching
};
```

```js
// cache-handler.js — Redis example
const { createClient } = require('redis');
const client = createClient({ url: process.env.REDIS_URL });

// Next.js 16 có API cacheHandler và refreshTags() rất quan trọng cho self-hosting
module.exports = class CacheHandler {
  async get(key) {
    /* ... */
  }
  async set(key, data, ctx) {
    /* ... */
  }
  async refreshTags() {
    /* sync tag invalidation from Redis */
  }
};
```

**3. TypeScript strict mode thực tế ít nghiêm ngặt hơn bạn nghĩ**

```typescript
// Tutorial: Perfect TypeScript
const product: Product = await getProduct(id);

// Production reality:
const product: any = await getProduct(id); // ← Vẫn thấy trong production code
// @ts-ignore ← Cũng thấy nhiều hơn bạn nghĩ
// @ts-expect-error ← Cũng có
```

**4. Một file có thể là hàng trăm, thậm chí nghìn dòng**

```
Tutorial: Mỗi component 30–50 dòng, clean, tách nhỏ
Reality:  components/checkout/CheckoutForm.tsx → 800 dòng
          lib/api/client.ts → 500 dòng
          app/(shop)/cart/page.tsx → 400 dòng
```

**5. Bao nhiêu comment // TODO là bình thường?**

```typescript
// TODO: Remove this hack after backend team fixes the API response format
const normalizedData = {
  ...response,
  // Backend trả về price là string thay vì number — bug đã report 3 tháng trước
  price: parseFloat(response.price as unknown as string),
};

// FIXME: This causes re-render storm, needs investigation
// Ticket: https://linear.app/company/issue/ENG-1234

// HACK: Workaround for Next.js cache bug in v13.3
// Can remove after upgrading to v13.4+
export const dynamic = 'force-dynamic';
```

## 7. Production Scenario — Phân Tích Job Description

**Bài tập thực tế:** Đọc 3 JD sau và xác định architecture pattern.

---

**JD #1 — Fintech Company**

> _"We are looking for a Senior Frontend Engineer to join our Platform team. You will work on our customer-facing web application built with Next.js 14. Our backend is a Java Spring microservices architecture with ~15 services. You will consume REST APIs and GraphQL federation layer. Experience with token refresh handling, WebSocket for real-time data, and understanding of OAuth2/OIDC flows required. We deploy on AWS EKS, not Vercel."_

**Phân tích:**

```
Tín hiệu nhận diện:
  "Java Spring microservices" → Backend team riêng, không phải monolith
  "consume REST APIs và GraphQL" → Next.js KHÔNG có business logic
  "~15 services" → Pattern C (BFF) hoặc Pattern B (Decoupled)
  "token refresh handling" → Auth phức tạp, token-based
  "AWS EKS, not Vercel" → Docker deployment, không dùng Vercel features
  "real-time data WebSocket" → Có WebSocket upstream, Next.js proxy

→ Kết luận: Pattern B (Decoupled Frontend) với yếu tố BFF
→ Day-to-day: 80% UI work, 20% API integration
→ Senior yêu cầu vì cần hiểu distributed auth flows
```

---

**JD #2 — E-commerce Startup**

> _"We're a 15-person startup building a B2C e-commerce platform. Tech stack: Next.js 15, Prisma, PostgreSQL, Stripe, Resend, Redis for caching. We use Server Actions heavily for mutations. We deploy on Vercel Pro. You'll work on everything from landing pages to checkout flow. TypeScript strict mode required."_

**Phân tích:**

```
Tín hiệu nhận diện:
  "15-person startup" → Nhỏ, move fast, ít chuyên môn hóa
  "Prisma, PostgreSQL" → Database trực tiếp trong Next.js
  "Server Actions heavily" → Pattern A (Full-stack Monolith)
  "Stripe, Resend, Redis" → External services nhưng integrated trong app
  "Vercel Pro" → Serverless deployment
  "everything from landing pages to checkout" → Full-stack role

→ Kết luận: Pattern A (Full-stack Monolith)
→ Day-to-day: Cả UI lẫn backend logic, schema migrations, API design
→ Phải biết: Database queries, auth, payment webhooks, email
```

---

**JD #3 — Media Company**

> _"We publish 500+ articles per day across 10 regional editions. Looking for a Next.js developer experienced with Contentful CMS, ISR, and CDN edge caching (Cloudflare). You'll optimize Core Web Vitals scores, implement structured data (Schema.org), and manage our webhook-based content invalidation pipeline. Experience with A/B testing at CDN level is a plus."_

**Phân tích:**

```
Tín hiệu nhận diện:
  "500+ articles per day" → Volume lớn, ISR/revalidation quan trọng
  "10 regional editions" → Multi-locale, phức tạp routing
  "Contentful CMS" → Pattern D (Content Platform)
  "CDN edge caching (Cloudflare)" → Static-heavy, cache là critical
  "webhook-based content invalidation" → Cần build/maintain revalidate pipeline
  "Core Web Vitals" → Performance là KPI chính
  "Schema.org structured data" → SEO expertise required

→ Kết luận: Pattern D (Content Platform)
→ Day-to-day: CMS integration, performance, SEO, cache strategy
→ Ít business logic hơn, nhiều infrastructure/performance hơn
```

---

## 8. Debugging Scenario — Bugs Đặc Trưng Của Từng Pattern

### Pattern A Bug — Connection Pool Exhausted

````typescript
// Bug phổ biến nhất của Pattern A trên serverless (Vercel)
// Mỗi serverless function spawn (tạo ra) instance (phiên bản) mới
// của Prisma client

// ❌ SAI — Mỗi request tạo Prisma client mới
// lib/db.ts
export const db = new PrismaClient(); // ← Trong serverless: 1000 requests = 1000 connections!

// ✅ ĐÚNG — Singleton pattern, reuse connection
// lib/db.ts
const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const db =
  globalForPrisma.prisma ??
  new PrismaClient({
    log:
      process.env.NODE_ENV === 'development' ? ['query', 'error'] : ['error'],
  });

if (process.env.NODE_ENV !== 'production') {
  // Development: Preserve (duy trì) connection qua hot reloads (tải lại trang)
  globalForPrisma.prisma = db;
}

// Ngoài ra:
// Serverless (như Vercel) có thể tạo nhiều instance function chạy song song.
// Mỗi instance nếu tự mở kết nối DB riêng thì rất nhanh chạm giới hạn
// connection của PostgreSQL.

// Dùng PgBouncer / connection pooler như Supabase pooler
// PgBouncer hoặc Supabase pgbouncer hoạt động như một “trạm gác”
// ngay trước PostgreSQL:

// App Instance 1 ─┐
// App Instance 2 ─┼──► PgBouncer ───► PostgreSQL
// App Instance 3 ─┤
// App Instance N ─┘

// Cơ chế:
// App gửi query đến PgBouncer
// → PgBouncer giữ một pool connection tới PostgreSQL.
// Khi query tới
// → Mượn 1 connection rảnh từ pool → Thực thi query → Trả connection về pool
// → (chờ request tiếp theo tái sử dụng connection) hoặc đóng nếu user logout
// Giống như bãi đỗ xe PostgreSQL có 20 chỗ đỗ. 1000 xe (request) đến
// PgBouncer điều phối để 20 chỗ được dùng luân phiên thay vì mở 1000 chỗ mới.

// Thay vì
// 1000 requests
// → 1000 Prisma Clients
// → 1000 DB connections
// → PostgreSQL quá tải

// Thì chỉ có:
// 1000 requests
// → nhiều Prisma Clients
// → PgBouncer gom lại
// → chỉ dùng khoảng 20–50 DB connections thật

// Kết quả:
// - PostgreSQL chỉ thấy 1 client (PgBouncer)
// - Không bao giờ bị kill vì "too many connections"
// - App có thể scale vô hạn (theo plan PgBouncer)

// Dùng Pattern A nếu:
//   + Team < 10 người
//   + App vừa và nhỏ
//   + Bạn muốn move fast
//   + Không có tài nguyên để maintain backend riêng

// Khi nào không nên dùng:
//   + App traffic > 10,000 requests/ngày
//   + App có nhiều tính năng complex (payment, auth, workflows)
//   + Bạn cần scale độc lập backend và frontend

// Tóm tắt bug:
// - Bug: Connection pool exhausted
// - Nguyên nhân: Mỗi serverless function spawn tạo Prisma client mới
// - Cách khắc phục: Dùng singleton pattern, tối ưu connection pool

// Kết luận:
// - Singleton Prisma Client → giảm số connection bị tạo trong cùng một runtime. Tránh tạo client thừa trong một instance. Singleton pattern chỉ giải quyết một instance — không giải quyết nhiều serverless instances. KHÔNG ĐỦ cho serverless scale

// Với Vercel/AWS Lambda: Mỗi cold start = new container = new connection

// Giải pháp bắt buộc: PgBouncer, Prisma Accelerate, hoặc provider-managed pooler (Supabase port 6543, Neon built-in)

// - PgBouncer/Supabase Pooler → bảo vệ DB khi có nhiều runtime serverless cùng hoạt động.
// Chia sẻ và tái sử dụng connection giữa rất nhiều instance/serverless function để tránh vượt giới hạn connection của PostgreSQL

//Connection limit: connection_limit=1 per instance khi dùng pooler

```env
# Supabase pooled connection
DATABASE_URL="postgresql://user:pass@db.xxx.supabase.co:6543/postgres?pgbouncer=true"
```;
````

### Pattern B Bug — CORS và Double Network Hop

```typescript
// Double Network Hop là khi một request phải đi qua 2 lần kết nối mạng
// liên tiếp trước khi tới nơi xử lý cuối cùng.

// Không proxy (1 hop): Browser ──► Backend
// Proxy (Double Network Hop): Browser ──► Next.js Route Handler ──► Backend

// Bug: Client component gọi trực tiếp backend URL (thay vì proxy qua Next.js)
// Nhằm cố gắng tránh Double Network Hop bằng cách cho Client Component
// gọi trực tiếp Backend API.

// ❌ SAI — Expose backend URL ra client
// Browser -> Backend
'use client';
export function ProductList() {
  useEffect(() => {
    // process.env.API_URL là NEXT_PUBLIC_ → Exposed to browser
    // CORS policy reject request vì origin khác
    fetch(process.env.NEXT_PUBLIC_API_URL + '/products')
      .then((r) => r.json())
      .then(setProducts);
  }, []);
}

// Tuy nhiên việc bỏ tầng proxy của Next.js thường không đáng đổi lấy vài ms latency,
// vì mất đi lợi ích về bảo mật, kiểm soát request và xử lý CORS.

// Hậu quả: CORS error, secrets leaked
// - Backend URL bị expose ra browser (NEXT_PUBLIC_*). Ai mở DevTools cũng thấy
// - Hacker có thể scan endpoint
// - Dễ lộ kiến trúc hệ thống và endpoint nội bộ
// - Browser phải xử lý CORS vì request cross-origin
// - Có thể buộc phải đưa API key/token xuống client (rủi ro bảo mật)
// - Mất khả năng kiểm soát request tại Next.js

//--------------------

// ✅ ĐÚNG — Proxy qua Next.js Route Handler
// Browser -> Next.js Route Handler -> Backend
('use client');
export function ProductList() {
  useEffect(() => {
    // /api/proxy/products → Next.js Route Handler → Backend
    // Không CORS vì same-origin
    // Backend URL không bị expose
    fetch('/api/proxy/products')
      .then((r) => r.json())
      .then(setProducts);
  }, []);
}
// Chấp nhận thêm 1 network hop để đổi lấy:
// - Ẩn Backend URL
// - Same-origin requests
// - Không CORS
// - Không lộ secrets
// - Dễ cache, auth, rate-limit và logging
```

### Pattern D Bug — ISR Stale Data

**Lưu ý về revalidateTag vs revalidatePath:**

- `revalidatePath` chỉ invalidate một path cụ thể
- `revalidateTag` invalidate tất cả data có cùng tag — **tốt hơn cho content shared across multiple pages** (ví dụ: navigation, footer, author bio)
- Từ Next.js v13.4+, `revalidateTag` là recommended approach cho CMS-driven sites

```typescript
// Tag-based revalidation (better)
revalidateTag('articles'); // Invalidate all articles everywhere
revalidateTag(`article-${slug}`); // Invalidate specific article
```

```typescript
// Bug: Editor publish bài mới, nhưng 5 phút sau vẫn thấy bài cũ

// Nguyên nhân: Webhook không gọi đúng revalidation
// app/api/revalidate/route.ts
export async function POST(request: NextRequest) {
  const body = await request.json();

  // ❌ SAI — Chỉ revalidate slug cụ thể, quên revalidate listing
  revalidatePath(`/articles/${body.slug}`);
  // → Bài vẫn không hiện trên homepage, category page

  // ✅ ĐÚNG — Revalidate cả chain
  revalidatePath(`/articles/${body.slug}`); // Article page
  revalidatePath(`/${body.category}`); // Category listing
  revalidatePath('/'); // Homepage (nếu có featured)
  revalidateTag('articles'); // Tag-based (v13.4+)

  return NextResponse.json({ ok: true });
}

// Ngoài ra: Debug bằng cách check response headers
// X-Nextjs-Cache: HIT   → Cache hit, content cũ
// X-Nextjs-Cache: MISS  → Content mới fetch
// X-Nextjs-Cache: STALE → ISR đang revalidate background
```

## 9. Refactoring Scenario — Grow From Pattern A To Pattern B/C

**Kịch bản thực tế:** Startup bắt đầu với Pattern A. 2 năm sau: 50 người, cần mobile app, cần partner API, team backend riêng.

```
TRƯỚC (Pattern A — 2 năm trước):
  next-app/
  ├── app/
  ├── actions/   ← Business logic tất cả ở đây
  └── prisma/

VẤN ĐỀ:
  1. Mobile team muốn dùng cùng business logic → không thể, nó bị lock trong Server Actions
  2. Partner muốn API key access → cần expose API, nhưng actions không có auth API key
  3. Backend team mới muốn dùng Python → không tích hợp được
```

```
MIGRATION PATH: Pattern A → Pattern B (Incremental, không Big Bang)

BƯỚC 1: Extract API layer (Tháng 1-2)
  Tạo /app/api/v1/* Route Handlers mirror các Server Actions
  Mobile team bắt đầu dùng REST API mới
  Actions vẫn hoạt động song song

BƯỚC 2: Thin the Actions (Tháng 3-4)
  Server Actions không còn chứa business logic
  Actions chỉ call API endpoint nội bộ
  Business logic di chuyển vào API layer

BƯỚC 3: Extract Service (Tháng 5-6)
  API layer ready để tách ra thành separate service
  Hoặc giữ trong Next.js nếu không cần tách hoàn toàn
  Mobile, web, partner đều dùng cùng API
```

```typescript
// BEFORE — Action với embedded business logic
'use server';
export async function createOrder(formData: FormData) {
  // ❌ Business logic trong Action — không reusable
  const cart = await db.cart.findUnique({ where: { userId: session.user.id } });
  const inventory = await db.inventory.findMany({ where: { productId: { in: cartItemIds } } });

  if (inventory.some(item => item.quantity < requestedQty)) {
    throw new Error('Insufficient inventory');
  }

  await db.$transaction([
    db.order.create({ ... }),
    db.inventory.updateMany({ ... }),
    db.cart.delete({ ... }),
  ]);
}

// AFTER — Action gọi API (bridge period)
'use server';
export async function createOrder(formData: FormData) {
  // ✅ Action chỉ là thin wrapper
  const response = await fetch('/api/v1/orders', {
    method: 'POST',
    body: JSON.stringify(Object.fromEntries(formData)),
    headers: { /* auth */ },
  });
  if (!response.ok) throw new Error(await response.text());
  revalidatePath('/orders');
}
```

## 10. Architecture Notes

### Khi Nào Chọn Pattern Nào

```
Câu hỏi 1: Team size?
  1-10 người    →  Pattern A (ít overhead nhất)
  10-30 người   →  Pattern A → B transition
  30+ người     →  Pattern B hoặc C
  Enterprise    →  Pattern B hoặc C, tùy có microservices không

Câu hỏi 2: Có mobile app không?
  Không có, không plan  →  A (Server Actions OK)
  Có hoặc sẽ có        →  B (REST/GraphQL API từ đầu)

Câu hỏi 3: SEO có quan trọng không?
  Không (internal tool, dashboard)  →  Bất kỳ pattern nào
  Quan trọng (public site)          →  D (Content) hoặc SSG phần của A/B

Câu hỏi 4: Content có thay đổi thường xuyên không?
  Rất thường (news, blog)  →  D với ISR + webhook
  Ít thay đổi (docs)       →  D với thuần SSG

Câu hỏi 5: Có nhiều tenant/customer khác nhau không?
  Một tenant (internal)    →  A, B, hoặc C
  Nhiều tenant (SaaS)      →  E
```

### Điều Thực Sự Quyết Định Kiến Trúc Hệ Thống (Architecture)

```
THỰC TẾ thường bị bỏ qua:

1. Cấu trúc tổ chức đội ngũ (TEAM TOPOLOGY) quan trọng hơn
"thuần khiết" về mặt kỹ thuật (TECHNICAL PURITY)
   "Pattern C (BFF) là tốt nhất" — nhưng nếu không có team riêng cho BFF,
   ai maintain nó? Ai on-call khi BFF down?

2. Hạ tầng hiện có (EXISTING INFRASTRUCTURE) thường quyết định pattern
   Công ty đã có Java backend → Khả năng cao sẽ chọn Pattern B
   (vì không ai muốn viết lại toàn bộ hệ thống).
   Công ty đã dùng AWS → Vercel ít likely (khả năng xảy ra) hơn

3. Chi phí chuyển đổi (MIGRATION COST) có thể quá cao
   Pattern A → B tưởng dễ nhưng thường mất 6-12 tháng
   Nhiều công ty bị stuck ở Pattern A vì không có đủ resource migrate
   (không có đủ nguồn lực để thực hiện quá trình chuyển đổi)

4. Công nghệ "cũ" (BORING TECHNOLOGY) thường được chọn lại
   Pattern A với getServerSideProps/Pages Router vẫn serve (đáp ứng) hàng triệu user
   Không nhất thiết phải dùng Server Components mới thành công
```

### Next.js 16 Build Adapters API — Impact on Self-Hosting

Từ v16.2 (March 2026), Build Adapters API stable cho phép deploy Next.js lên bất kỳ platform:

- AWS, Cloudflare, Netlify, Google Cloud, self-hosted
- Không còn bị lock-in vào Vercel ecosystem
- Adapter nhận output typed, versioned từ Next.js build

```ts
// next.config.ts
const nextConfig = {
  adapterPath: require.resolve('@next/adapter-aws'),
};
```

**Ý nghĩa:** Self-hosting Next.js giờ đây có API chính thức, không còn là "reverse engineering Vercel internals."

## 11. Interview Questions

### Junior Level

**Q1: Tại sao một công ty có thể dùng Next.js nhưng không có file nào trong `/app/api/`?**

<details>
<summary>Đáp án</summary>

Vì công ty đó đang dùng **Pattern B (Decoupled Frontend)** — Next.js chỉ là presentation layer, toàn bộ API được xử lý bởi một backend service riêng biệt (Java, Go, Python, .NET, etc.).

Trong pattern này:

- Server Components fetch trực tiếp từ external API URL trong môi trường server
- Client Components dùng một proxy route (ví dụ `/api/proxy/*`) để forward requests
- Không có business logic hay database access trong Next.js

Đây là pattern rất phổ biến tại các công ty lớn có team backend riêng, và là lý do tại sao "biết Next.js" không tự động có nghĩa là biết backend development.

</details>

---

**Q2: Developer mô tả project của họ: "Next.js app, PostgreSQL, Prisma, deploy trên Vercel, dùng Server Actions cho form submissions". Đây là pattern gì và trade-offs của nó?**

<details>
<summary>Đáp án</summary>

**Pattern A — Full-Stack Monolith.** Đây là pattern phổ biến nhất ở startup và small team.

**Trade-offs:**

_Ưu điểm:_

- Type safety end-to-end: Schema Prisma → TypeScript → Component
- Không có network hop giữa frontend và database (same process)
- Tốc độ phát triển và thử nghiệm (Iteration speed) rất nhanh
- Deploy đơn giản (trivial) với Vercel

_Nhược điểm:_

- Database connection pooling là vấn đề trên serverless (mỗi Vercel function = new Prisma instance). Cần setup connection pooler (PgBouncer, Supabase pooler, Neon).
- Không thể reuse API cho mobile app nếu dùng Server Actions — phải build REST API riêng sau này
- Scale team khó: Khi team lớn, mọi người có access vào database logic, dễ inconsistent patterns (cách triển khai không nhất quán, làm giảm tính chuẩn hóa của hệ thống)
- Nguy cơ bị phục thuộc (Vendor lock-in) vào Vercel ecosystem nếu không setup để ứng dụng có thể chạy độc lập (standalone output)

</details>

---

### Mid Level

**Q3: Company có 3 microservices: user-service, product-service, order-service. CTO muốn dùng Next.js. Bạn sẽ suggest kiến trúc gì và tại sao?**

<details>
<summary>Đáp án</summary>

**Pattern C — BFF (Backend for Frontend).**

**Lý do:**

Với 3+ microservices riêng biệt, một client phải gọi nhiều services cho một trang (ví dụ order history cần cả user-service lẫn order-service lẫn product-service). Không có BFF dẫn đến:

- Client phải biết về tất cả service URLs
- Nhiều lượt gọi mạng (network round-trips) từ browser
- Không có nơi tập trung xử lý auth, authorization và user context
- Logic aggregation (gom data) bị đẩy xuống frontend → khó maintain và dễ duplicate

**BFF implementation với Next.js:**

- `/app/api/dashboard/route.ts` → Gọi nhiều services song song bằng `Promise.all()`, sau đó merge và transform response thành format UI cần
- `/services/*.ts` → Typed clients cho từng microservice
- Middleware xử lý auth: verify JWT, extract session, inject user context vào các service calls
- Service-to-service communication dùng **internal URLs (private network/VPC/Kubernetes DNS)** (không expose qua internet)

---

**💡 Senior Insight:** Lưu ý quan trọng khi pitch (thuyết trình / trình bày)

- **BFF cần team riêng để vận hành và maintain**
  BFF không nên được xem là “phần phụ” của frontend hay backend team. Đây là một lớp backend độc lập, có trách nhiệm riêng như một product: quản lý API aggregation, performance, auth flow và integration với nhiều microservices. Nếu không có ownership rõ ràng, BFF rất dễ bị viết chồng chéo, khó scale và khó debug.

- **Cần monitoring và alerting đầy đủ cho BFF (vì đây là single point of failure)**
  BFF là điểm vào duy nhất từ client vào hệ thống backend. Nghĩa là nếu BFF bị lỗi, toàn bộ ứng dụng frontend sẽ không hoạt động dù các microservices phía sau vẫn chạy bình thường. Vì vậy cần theo dõi chặt:
  - latency (p95/p99)
  - error rate
  - traffic / throughput
  - tình trạng các dependency (user-service, order-service, …)
    Đồng thời phải có alert real-time để phát hiện sớm sự cố trước khi người dùng bị ảnh hưởng.

- **Cần Circuit Breaker để tránh một service làm sập toàn bộ page**
  Trong hệ thống microservices, việc một service bị chậm hoặc down là chuyện bình thường. Nếu BFF gọi trực tiếp tất cả services theo kiểu “fail là fail toàn bộ”, chỉ cần 1 service lỗi là cả page bị crash.
  Circuit breaker giúp:
  - phát hiện service đang lỗi hoặc timeout
  - tạm ngưng gọi service đó trong một khoảng thời gian
  - trả fallback data (rỗng, cache, hoặc degraded response)
    → Nhờ đó UI vẫn hiển thị được phần còn lại thay vì trắng trang hoàn toàn.

- **Tránh logic bị “phình to” → BFF không nên biến thành “mini monolith backend”**
  BFF chỉ nên tập trung vào 2 việc chính:
  - API aggregation / composition (gọi nhiều service và merge response)
  - Auth / authorization / user session handling

  **Nó KHÔNG nên chứa:**
  - business logic nghiệp vụ (nếu đã có service riêng)
  - complex data transformation
  - database access trực tiếp
    Khi team lớn lên, nếu BFF quá to, mọi người sẽ “dính” vào nó và khó tách service độc lập sau này.

---

👉 _Giải thích thêm về “internal URLs”:_
Các microservices không được gọi qua public domain (ví dụ `api.company.com/user-service`) mà chỉ chạy trong mạng nội bộ như:

- `http://product-service:8080/products/456`
- `http://user-service.internal`
- `http://order-service.default.svc.cluster.local` (Kubernetes)

Browser không thể truy cập các URL này, chỉ có BFF (Next.js server) trong hệ thống mới gọi được.

**Trong thực tế ở công ty lớn**

Thường sẽ là:

```text
Internet
    │
    ▼
CDN
    │
    ▼
Next.js BFF
    │
    ▼
Service Mesh (Istio/Linkerd)
    │
    ├── user-service
    ├── product-service
    ├── order-service
    └── inventory-service
```

BFF là **public entry point**.
Microservices là **private services**. Chỉ BFF (hoặc API Gateway) được phép nói chuyện với chúng.

**BFF không nhất thiết phải làm bằng Next.js.**

Nó có thể làm bằng: Express, FastAPI, Spring Boot, Go, .NET, hoặc bất kỳ ngôn ngữ/framework nào.

Đôi khi, BFF không phải Next.js, mà Next.js chỉ là **UI layer** và gọi qua API Gateway/BFF.

Vì khi sau này đổi:

```text
product-service
```

thành:

```text
catalog-service
pricing-service
inventory-service
```

Browser không cần sửa gì.

BFF chỉ sửa logic:

```ts
const [catalog, pricing, inventory] =
  await Promise.all([...]);

return mergeData(...);
```

Frontend vẫn gọi:

```ts
GET / api / products / 123;
```

như cũ.

Đó chính là giá trị lớn nhất của **BFF trong kiến trúc microservices**: **ẩn toàn bộ topology nội bộ của hệ thống khỏi client**, đồng thời tập trung auth, aggregation và orchestration ở một lớp duy nhất.

</details>

---

### Senior Level

**Q4: Bạn join một team đang dùng Pattern A (Full-stack Monolith). 1 năm sau, company muốn launch mobile app. Bạn sẽ migrate như thế nào mà không break production?**

<details>
<summary>Đáp án</summary>

**Incremental migration, không Big Bang rewrite.**

**Giai đoạn 1 — Tạo API layer song song (2-3 tháng):**

- Tạo `/app/api/v1/*` Route Handlers expose business logic dưới dạng REST
- API handlers call cùng database queries với Server Actions
- Mobile team bắt đầu dùng API mới, web vẫn dùng Server Actions cũ
- Viết API documentation (OpenAPI/Swagger)

**Giai đoạn 2 — Thin the Actions (1-2 tháng):**

- Server Actions refactor để gọi internal API (`/api/v1/*`) thay vì DB trực tiếp
- Đây là bridge: Web dùng Actions, mobile dùng REST, nhưng cùng underlying logic
- Thêm API key auth cho mobile requests

**Giai đoạn 3 — Evaluate và decide (ongoing):**

- Nếu mobile team satisfised và API stable → Có thể giữ nguyên (API trong Next.js vẫn OK)
- Nếu cần tách hoàn toàn → Extract API sang separate service (NestJS, FastAPI, etc.), Next.js thành Pattern B

**Rủi ro phải monitor:**

- Data consistency khi cả Actions và API đều mutate DB
- Auth token management: Session cookie (web) vs API token (mobile)
- Rate limiting và API versioning từ ngày đầu

</details>

---

**Q5: "Next.js trên Vercel rất đắt khi scale. Công ty muốn self-host bằng Docker nhưng vẫn muốn giữ ISR và Image Optimization. Điều này khả thi không? Những gì bị mất?"**

<details>
<summary>Đáp án</summary>

**Khả thi — với một số lưu ý quan trọng.**

**Setup cơ bản:**

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  output: 'standalone', // ← BẮT BUỘC cho Docker deployment
};
```

**Những gì hoạt động khi self-host:**

- ISR: Hoạt động, nhưng cache lưu trên local file system của container
- `revalidatePath`/`revalidateTag`: Hoạt động
- Image Optimization (`next/image`): Hoạt động (Next.js có built-in image server)

**Những gì BỊ MẤT hoặc CẦN SETUP THÊM:**

1. **Edge Runtime functions** — Vercel Edge Network không có khi self-host. Middleware chạy trên Node.js runtime, không phải Edge.
2. **ISR với multiple instances** — Nếu deploy 3 Docker containers. Mặc định, mỗi container có cache riêng → inconsistent ISR. Cần shared cache layer (Redis) để sync.
   Giải pháp Next.js chính thức (v16+) : Dùng `cacheHandler` trong `next.config.js` với Redis/Valkey, hoặc `'use cache: remote'` với custom cache handler. Ngoài ra, cần implement `refreshTags()` trong cache handler để sync tag invalidation across instances.
3. **Automatic scaling** — Vercel tự scale. Docker cần Kubernetes hoặc ECS setup.
4. **Analytics và Speed Insights** — Vercel-specific, không khả dụng.
5. **Automatic SSL, CDN, DDoS protection** — Phải tự setup (Cloudflare, Nginx, etc.).

**Về ISR với multiple instances — đây là vấn đề nghiêm trọng nhất:**

```
Container A: Article "ABC" → Cache HIT (fresh)
Container B: Article "ABC" → Cache MISS (chưa có cache)
→ Load balancer gửi user đến B → User thấy stale content

Fix: Dùng Redis làm shared cache, hoặc sticky sessions trên load balancer
```

**Bottom line (Tóm lại):** Self-host được, nhưng phải tự build nhiều infrastructure mà Vercel cung cấp sẵn. Tính toán TCO (Total Cost of Ownership) bao gồm: engineer time setup infrastructure, monitoring, on-call.

Dưới đây là bảng trade-off rõ ràng giữa **Next.js self-host (Docker)** và **Vercel (managed platform)** trong bối cảnh có ISR + Image Optimization:

---

**📊 Trade-off: Vercel vs Self-host (Docker)**

| Tiêu chí                                   | Vercel (Managed)                 | Self-host (Docker / Kubernetes)                  |
| ------------------------------------------ | -------------------------------- | ------------------------------------------------ |
| **Setup ban đầu**                          | Rất nhanh (zero infra)           | Phức tạp (Docker, Load Balancer, CDN, CI/CD)     |
| **Scale traffic**                          | Auto-scale                       | Tự build (K8s, ECS, autoscaling)                 |
| **ISR (cache)**                            | Shared global cache (consistent) | Local cache per instance → dễ lệch dữ liệu       |
| **ISR consistency**                        | ✔ Consistent across regions      | ❌ Cần Redis / shared storage để sync            |
| **Image Optimization (`next/image`)**      | Edge optimized (CDN built-in)    | Node server xử lý → tốn CPU, cần CDN hỗ trợ      |
| **Edge Functions / Middleware**            | Edge network native              | Chạy Node.js server (không phải Edge thật)       |
| **Latency global**                         | Tối ưu sẵn (Edge network)        | Phụ thuộc setup CDN (Cloudflare, AWS CloudFront) |
| **Observability** - Khả năng quan sát      | Built-in analytics, logs         | Tự setup (Prometheus, Grafana, ELK, etc.)        |
| **Deploy speed**                           | Git push → auto deploy           | CI/CD pipeline tự xây                            |
| **Infrastructure control**                 | Hạn chế                          | Toàn quyền control                               |
| **Cost model**                             | Đắt khi scale lớn                | Rẻ hơn ở scale lớn nhưng tốn engineer time       |
| **Vendor lock-in**                         | Cao                              | Thấp                                             |
| **Maintenance burden (gánh nặng bảo trì)** | Gần như không                    | Cao (on-call, infra, scaling, incidents - sự cố) |

---

**⚠️ Điểm khác biệt quan trọng nhất**

**1. ISR consistency problem (self-host)**

**1.1 ISR cache nằm ở đâu?**

Khi self-host:

- cache nằm trong `.next/cache` hoặc trên filesystem của container instance.
- Mỗi container instance có ISR cache riêng → **không shared** giữa instances

Ví dụ:

```text
Multi-instance deployment
Container A → có ISR cache version 1
Container B → có ISR cache version 2
Container C → cache miss (chưa có cache)
```

👉 Kết quả:

- user mỗi request có thể thấy data khác nhau (inconsistent UI)

✔ Fix:

- Redis shared cache
- hoặc external ISR store
- hoặc sticky session (không scale tốt)

**1.2 Vì sao Vercel không bị lỗi này?**

Vì Vercel có:

- global shared cache layer
- edge network
- global ISR storage

All regions share ISR cache → consistent response

---

**2. Image Optimization**

| Vercel            | Self-host            |
| ----------------- | -------------------- |
| Edge optimized    | Node server xử lý    |
| CDN cache tự động | phải tự cấu hình CDN |
| zero CPU cost     | tốn CPU + memory     |

---

**3. Edge Runtime**

| Vercel                   | Self-host                     |
| ------------------------ | ----------------------------- |
| true edge network        | không có edge thật            |
| middleware chạy gần user | middleware chạy server region |

---

**💰 Trade-off về chi phí (quan trọng khi interview)**

| Scale       | Vercel     | Self-host     |
| ----------- | ---------- | ------------- |
| Small app   | rẻ + nhanh | overkill      |
| Medium app  | ok         | reasonable    |
| Large scale | rất đắt 💸 | tối ưu hơn 💸 |

---

**🧠 Kết luận ngắn gọn**

> _Vercel trades infrastructure complexity for cost and lock-in, while self-host trades simplicity for full control but requires rebuilding everything Vercel provides out-of-the-box._

> _Vercel đánh đổi sự phức tạp về hạ tầng để lấy sự tiện lợi (và đi kèm là chi phí cao + bị phụ thuộc nền tảng), trong khi self-host thì đánh đổi sự đơn giản để lấy toàn quyền kiểm soát, nhưng phải tự xây lại tất cả những gì Vercel đã cung cấp sẵn._

👉 Vercel:

- tối ưu developer experience
- giảm infra burden
- đổi lại chi phí cao + vendor lock-in

👉 Self-host:

- kiểm soát toàn bộ hệ thống
- tối ưu cost khi scale lớn
- đổi lại phải tự build toàn bộ infra (ISR sync, CDN, scaling, observability)

</details>

## 12. Exercises

### Exercise 1 — Pattern Identification (Nhẹ)

Đây là một phần `package.json` và directory tree. Xác định pattern và giải thích tại sao.

```json
{
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "@contentful/rich-text-react-renderer": "^15.22.0",
    "contentful": "^10.13.0",
    "next-seo": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
```

```
app/
├── layout.tsx
├── page.tsx
├── [category]/
│   └── page.tsx
├── [category]/[slug]/
│   └── page.tsx
└── api/
    └── revalidate/
        └── route.ts
```

_Câu hỏi: Pattern gì? Tại sao? Ứng dụng này làm gì? Công ty này likely là công ty gì?_

<details>
<summary>Đáp án</summary>

**Pattern: Pattern D — Content Platform**

**Dấu hiệu nhận diện:**

- `contentful` + `@contentful/rich-text-react-renderer` → Headless CMS integration là core dependency, không phải ORM hay API client
- `next-seo` → SEO optimization là first-class concern, không phải add-on
- `app/api/revalidate/route.ts` → Webhook endpoint để CMS trigger cache invalidation — đặc trưng của ISR content sites
- Route structure: `[category]/[slug]/` → Dynamic content routing cho articles/pages, không phải dashboard hay SaaS features
- Không có `prisma`, `@prisma/client`, `drizzle-orm`, hay bất kỳ database ORM nào → Không có DB access trực tiếp
- Không có `axios`, `zod`, hay `/services/*.ts` → Không có microservice clients hay BFF aggregation layer

**Ứng dụng làm gì:**

Đây là một content-driven website — blog, news portal, documentation site, hoặc marketing website. Các bài viết/articles được quản lý bởi Contentful (CMS), sau đó được render bằng Next.js với ISR cho performance và SEO. Route `[category]/[slug]/` cho phép tổ chức content theo category và permalink. `revalidate` endpoint cho phép editorial team publish/update content mà không cần redeploy.

**Công ty này likely là:**

- Media company / online publisher (báo điện tử, magazine)
- Content marketing agency
- Tech company với heavy content strategy (blog, docs, resource center)
- E-commerce với content-heavy approach (product guides, reviews platform)

Không phải: SaaS dashboard, fintech platform, hay e-commerce pure-play — vì không có auth library, payment integration, hay database dependencies.

**Câu hỏi phỏng vấn nên hỏi và đáp án tham khảo:**

- **"ISR revalidation strategy hiện tại là gì — time-based hay on-demand webhook?"**
  - Đáp án mong đợi: Có cả hai — `revalidate = 300` (5 phút) cho time-based fallback, webhook từ Contentful cho on-demand khi editor publish. Time-based đảm bảo content không bị stale quá lâu nếu webhook fail; webhook giảm latency xuống gần real-time.

- **"CDN setup là gì — Cloudflare, CloudFront, hay Vercel Edge?"**
  - Đáp án mong đợi: Vercel Edge (nếu deploy trên Vercel) hoặc Cloudflare (nếu self-host). Quan trọng là CDN phải respect `Cache-Control` headers từ Next.js ISR. Cloudflare có thể cần bypass cache cho revalidation endpoints.

- **"Content preview workflow cho editors thế nào?"**
  - Đáp án mong đợi: Contentful Preview API với separate preview token. Route `/api/preview` enable preview mode, set cookie để bypass ISR cache. Editors xem draft content qua preview URL trước khi publish. Cần handle preview token security — không expose ra production.

  <details>

  <summary><strong>Demo code (Next.js + Contentful + ISR)</strong></summary>

  **Architecture**

  _Normal Users_

  ```text
  User
    ↓
  Cloudflare CDN
    ↓
  ISR Cache
    ↓
  Next.js
    ↓
  Contentful Delivery API
  ```

  - User nhận cached HTML
  - ISR revalidate mỗi 60 giây
  - Chỉ thấy published content

  ***

  **Editors (Preview Mode)**

  ```text
  Editor
    ↓
  /api/preview
    ↓
  Verify Preview Secret
    ↓
  Set Cookie: preview=true
    ↓
  Redirect → /blog/my-post
    ↓
  Next.js detects preview cookie
    ↓
  Skip ISR Cache
    ↓
  Contentful Preview API
    ↓
  Fetch Draft Content
    ↓
  Render Fresh HTML
  ```

  ***

  **Environment Variables**

  ```env
  CONTENTFUL_SPACE_ID=xxxxx

  CONTENTFUL_DELIVERY_TOKEN=delivery_token
  CONTENTFUL_PREVIEW_TOKEN=preview_token

  PREVIEW_SECRET=super-secret-preview-key
  ```

  ***

  **Contentful Client**

  ```ts
  // lib/contentful.ts

  const SPACE_ID = process.env.CONTENTFUL_SPACE_ID!;

  export async function getPost(slug: string, preview = false) {
    const token = preview
      ? process.env.CONTENTFUL_PREVIEW_TOKEN
      : process.env.CONTENTFUL_DELIVERY_TOKEN;

    const host = preview ? 'preview.contentful.com' : 'cdn.contentful.com';

    const res = await fetch(
      `https://${host}/spaces/${SPACE_ID}/entries?content_type=blogPost&fields.slug=${slug}`,
      {
        headers: {
          Authorization: `Bearer ${token}`,
        },
      },
    );

    const json = await res.json();

    return json.items[0];
  }
  ```

  ***

  **Preview Route**

  Editor click:

  ```text
  https://site.com/api/preview?secret=abc123&slug=my-post
  ```

  ```ts
  // app/api/preview/route.ts

  import { NextResponse } from 'next/server';

  export async function GET(req: Request) {
    const { searchParams } = new URL(req.url);

    const secret = searchParams.get('secret');
    const slug = searchParams.get('slug');

    if (secret !== process.env.PREVIEW_SECRET) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const response = NextResponse.redirect(new URL(`/blog/${slug}`, req.url));

    response.cookies.set({
      name: 'preview',
      value: 'true',
      httpOnly: true,
      secure: true,
      sameSite: 'lax',
      path: '/',
    });

    return response;
  }
  ```

  **Mục đích**
  - Verify preview token
  - Enable preview mode
  - Set secure cookie
  - Redirect editor tới bài viết

  ***

  **Blog Page**

  ```ts
  // app/blog/[slug]/page.tsx

  import { cookies } from "next/headers";
  import { getPost } from "@/lib/contentful";

  export default async function BlogPage({
    params,
  }: {
    params: Promise<{ slug: string }>;
  }) {
    const { slug } = await params;

    const cookieStore = await cookies();

    const isPreview =
      cookieStore.get("preview")?.value ===
      "true";

    const post = await getPost(
      slug,
      isPreview
    );

    return (
      <>
        {isPreview && (
          <div
            style={{
              background: "orange",
              padding: 8,
            }}
          >
            Preview Mode
          </div>
        )}

        <article>
          <h1>{post.fields.title}</h1>

          <div>
            {post.fields.content}
          </div>
        </article>
      </>
    );
  }
  ```

  ***

  **Bypass ISR Cache**

  ```ts
  async function getPost(slug: string, preview: boolean) {
    const token = preview
      ? process.env.CONTENTFUL_PREVIEW_TOKEN
      : process.env.CONTENTFUL_DELIVERY_TOKEN;

    const host = preview ? 'preview.contentful.com' : 'cdn.contentful.com';

    const response = await fetch(`https://${host}/entries?slug=${slug}`, {
      headers: {
        Authorization: `Bearer ${token}`,
      },

      ...(preview
        ? {
            cache: 'no-store',
          }
        : {
            next: {
              revalidate: 60,
            },
          }),
    });

    return response.json();
  }
  ```

  ***

  **Exit Preview Mode**

  ```ts
  // app/api/exit-preview/route.ts

  import { NextResponse } from 'next/server';

  export async function GET(req: Request) {
    const response = NextResponse.redirect(new URL('/', req.url));

    response.cookies.delete('preview');

    return response;
  }
  ```

  Editor:

  ```text
  https://site.com/api/exit-preview
  ```

  ***

  **CDN Configuration**

  **Bypass Cache**

  ```text
  /api/preview
  /api/revalidate
  /api/exit-preview
  ```

  Setting trong Cloudflare Rule:

  ```text
  IF URL starts with /api/preview
  → Cache Level: Bypass

  IF URL starts with /api/revalidate
  → Cache Level: Bypass
  ```

  ***

  **Security Notes**

  Không bao giờ expose env: `CONTENTFUL_PREVIEW_TOKEN` và `PREVIEW_SECRET` cho môi trường Frontend.
  `Preview token` luôn luôn phải giữ (remain) ở `server-side only`.

  ***

  **Interview Summary**

  Content preview được triển khai bằng Contentful Preview API và preview token riêng biệt.

  Editors truy cập `/api/preview`, hệ thống verify secret rồi set cookie `preview=true`.

  Khi Next.js phát hiện preview cookie:
  - bỏ qua ISR cache (`cache: "no-store"`)
  - gọi Contentful Preview API
  - render draft content theo thời gian thực

  Người dùng bình thường vẫn đi qua CDN + ISR và chỉ thấy published content.

  Các endpoint như `/api/preview` và `/api/revalidate` phải bypass CDN cache để đảm bảo preview và revalidation hoạt động chính xác.

  </details>

- **"Multi-language / i18n strategy cho content?"**
  - Đáp án mong đợi: Contentful có built-in localization — mỗi entry ( bài viết hay content record nói chung) có đa ngôn ngữ (multiple locales). Next.js i18n routing (`[locale]/[category]/[slug]`) hoặc subdomain-based (`en.site.com`, `vi.site.com`). ISR phải generate cho từng locale riêng. Revalidation webhook cần trigger cho tất cả locales của một entry.

  <details>
  <summary><strong>Demo code</strong></summary>

  **Kiến trúc tổng thể**

  Contentful hỗ trợ **Localization** built-in.

  Một entry có thể chứa nhiều locale:

  ```text
  Blog Post

  Title:
    en-US: "Hello World"
    vi-VN: "Xin chào thế giới"

  Content:
    en-US: "Content..."
    vi-VN: "Nội dung..."
  ```

  → Không cần tạo 2 entries riêng cho mỗi ngôn ngữ.

  ***

  **Routing Strategy**

  **Option 1 — Locale Prefix (phổ biến nhất)**

  ```text
  /en/blog/hello-world
  /vi/blog/xin-chao-the-gioi
  /ja/blog/konnichiwa
  ```

  Next.js:

  ```text
  app/
  └── [locale]/
      └── blog/
          └── [slug]/
              └── page.tsx
  ```

  ```ts
  export default async function Page({
    params,
  }: {
    params: Promise<{
      locale: string;
      slug: string;
    }>;
  }) {
    const { locale, slug } = await params;

    const post = await getPost({
      slug,
      locale,
    });

    return <Article post={post} />;
  }
  ```

  ***

  **Option 2 — Subdomain-based**

  ```text
  en.site.com/blog/hello-world
  vi.site.com/blog/xin-chao-the-gioi
  ```

  Middleware:

  ```ts
  const locale = request.headers.get('host')?.split('.')[0];
  ```

  Use case:
  - Global websites
  - Marketing sites
  - SEO-focused platforms

  ***

  **Contentful Query by Locale**

  ```ts
  async function getPost({ slug, locale }: { slug: string; locale: string }) {
    const res = await fetch(
      `${CONTENTFUL_API}/entries?fields.slug=${slug}&locale=${locale}`,
    );

    return res.json();
  }
  ```

  Contentful tự trả về field tương ứng locale.

  ***

  **ISR Strategy**

  **Quan trọng**

  Mỗi locale là một page cache riêng.

  Ví dụ:

  ```text
  /vi/blog/react-19
  /en/blog/react-19
  ```

  là hai ISR cache khác nhau.

  ```text
  Cache Key

  vi/react-19
  en/react-19
  ```

  ***

  **Generate Static Params**

  ```ts
  export async function generateStaticParams() {
    const posts = await getAllPosts();

    return posts.flatMap((post) => [
      {
        locale: 'en',
        slug: post.slug.en,
      },
      {
        locale: 'vi',
        slug: post.slug.vi,
      },
    ]);
  }
  ```

  ***

  **Revalidation Strategy**

  **Vấn đề**

  Editor update:

  ```text
  Entry: React 19 Guide
  ```

  Contentful webhook gửi:

  ```json
  {
    "entryId": "abc123"
  }
  ```

  ***

  **Sai**

  ```ts
  revalidatePath('/en/blog/react-19');
  ```

  → chỉ locale EN được update

  ***

  **Đúng**

  Trigger tất cả locales:

  ```ts
  await Promise.all([
    revalidatePath('/en/blog/react-19'),
    revalidatePath('/vi/blog/react-19'),
    revalidatePath('/ja/blog/react-19'),
  ]);
  ```

  ***

  **Webhook Flow**

  ```text
  Editor Publish
        ↓
  Contentful
        ↓
  Webhook
        ↓
  /api/revalidate
        ↓
  Lookup entry locales
        ↓
  revalidatePath() for all locales
  ```

  ***

  **Example Revalidation Route**

  ```ts
  export async function POST(req: Request) {
    const payload = await req.json();

    const entryId = payload.sys.id;

    const post = await getEntry(entryId);

    const locales = ['en', 'vi', 'ja'];

    await Promise.all(
      locales.map((locale) =>
        revalidatePath(`/${locale}/blog/${post.slug[locale]}`),
      ),
    );

    return Response.json({
      revalidated: true,
    });
  }
  ```

  ***

  **SEO Considerations**

  **hreflang**

  ```html
  <link
    rel="alternate"
    hreflang="en"
    href="https://site.com/en/blog/react-19"
  />

  <link
    rel="alternate"
    hreflang="vi"
    href="https://site.com/vi/blog/react-19"
  />
  ```

  Giúp Google hiểu các bản dịch của cùng một nội dung.

  ***

  **Caching Considerations**

  Cache key nên bao gồm locale:

  ```text
  BAD

  /blog/react-19
  ```

  ```text
  GOOD

  /en/blog/react-19
  /vi/blog/react-19
  ```

  Tránh user Việt nhận nội dung tiếng Anh.

  ***

  **Interview Summary**

  Contentful hỗ trợ localization built-in, mỗi entry chứa nhiều locale thay vì tạo nhiều entries.

  Next.js thường dùng locale-based routing:

  ```text
  /[locale]/[category]/[slug]
  ```

  hoặc subdomain:

  ```text
  en.site.com
  vi.site.com
  ```

  Mỗi locale có ISR cache riêng, nên khi content được publish hoặc update, webhook revalidation phải trigger cho tất cả locale liên quan.

  Ngoài ra cần triển khai:
  - locale-aware routing:
  - locale-specific cache keys
  - hreflang SEO tags
  - locale-aware ISR revalidation

  để đảm bảo content, caching và SEO hoạt động chính xác trên môi trường đa ngôn ngữ.

  </details>

</details>

---

### Exercise 2 — Architecture Decision (Trung bình)

Bạn được hire làm first engineer tại một startup:

- **Product:** B2B tool giúp các restaurant quản lý menu và orders
- **Team:** 2 founders (non-technical) + bạn
- **Budget:** Hạn chế — cần scale cẩn thận
- **Timeline:** MVP trong 3 tháng
- **Tương lai:** Sẽ cần mobile app cho restaurant staff (6-12 tháng nữa)

**Câu hỏi:**

1. Bạn chọn pattern nào cho MVP? Tại sao?
2. Biết rằng sẽ cần mobile app trong 6-12 tháng, bạn sẽ chuẩn bị gì ngay từ đầu để việc migration sau này đơn giản hơn?
3. Bạn chọn Vercel hay self-host? Tại sao? Với budget hạn chế, đâu là điểm bạn sẽ cut?

<details>
<summary>Đáp án</summary>

**1. Pattern cho MVP: Pattern A — Full-Stack Monolith (với điều chỉnh)**

> **"Tôi vẫn dùng Full-Stack Monolith để ship nhanh, nhưng business logic được đặt trong `lib/api/*`, API contract (`/api/v1/*`) tồn tại từ đầu. Khi mobile app xuất hiện, tôi chỉ cần expose API hiện có thay vì rewrite toàn bộ backend."**

Chọn Pattern A vì:

- Team 3 người (2 non-technical + bạn) — không có backend team riêng, không có bandwidth cho multiple services
- Timeline 3 tháng cho MVP — cần iteration speed tối đa, không có thời gian cho API design, contract negotiation
- Budget hạn chế — một repo, một deploy target, một monitoring stack giảm operational overhead
- Restaurant menu/order logic không quá phức tạp ở giai đoạn đầu — CRUD operations, simple auth, Stripe payment

**Điều chỉnh quan trọng để prepare cho mobile app:**

- Không dùng Server Actions cho mutations từ đầu — hoặc dùng nhưng wrap trong abstraction layer
- Tạo `/app/api/internal/*` Route Handlers ngay từ MVP — mobile team sẽ consume API này sau
- Database schema design với REST resource mindset: `menus`, `orders`, `items` tables có clear relationships, không denormalize quá sớm

**2. Chuẩn bị cho mobile app migration:**

- **Tách API layer ra khỏi UI ngay từ đầu:**
  - `lib/api/` chứa business logic callable từ cả Server Components lẫn Route Handlers
  - Route Handlers (`/api/v1/menus`, `/api/v1/orders`) gọi `lib/api/` functions
  - Server Actions (nếu dùng) chỉ là thin wrapper call `lib/api/` — không chứa business logic

- **Auth strategy future-proof:**
  - Dùng JWT access token + refresh token pattern từ đầu
  - Web dùng httpOnly cookie, mobile sẽ dùng Bearer token — cùng auth middleware
  - Không dùng session-based auth (NextAuth mặc định) vì khó integrate với mobile

- **Database:**
  - PostgreSQL trên Supabase hoặc Neon — có connection pooler built-in (PgBouncer/Supabase pooler)
  - Schema thiết kế với API contract mindset: tables có clear IDs, timestamps, foreign keys
  - Dùng Prisma với migration files — schema evolution trackable

- **Code organization prepare cho tách:**
  ```
  lib/
  ├── api/           ← Business logic, callable từ anywhere
  │   ├── menus.ts
  │   ├── orders.ts
  │   └── auth.ts
  ├── db.ts          ← Prisma singleton
  └── types/         ← Shared types
  app/
  ├── api/v1/        ← REST API cho mobile (từ đầu)
  │   ├── menus/
  │   └── orders/
  └── (dashboard)/   ← UI pages
  ```

**3. Vercel vs self-host và budget cuts:**

- **Chọn Vercel cho MVP** — lý do:
  - Zero DevOps overhead: không cần setup CI/CD, SSL, CDN, load balancer
  - Automatic preview deployments cho mỗi PR — founders (non-technical) có thể review features qua preview URL
  - ISR/SSG built-in — restaurant menu pages có thể static render, cải thiện performance không cần effort
  - Scale tự động — nếu có traffic tăng đột biến (spike) (ví dụ: được đăng trên Hacker News), không cần manual intervention

- **Điểm sẽ cắt giảm để tiết kiệm budget:**
  - **Vercel Pro ($20/month) thay vì Enterprise** — Pro plan đủ cho MVP, giới hạn team members và build minutes
  - **Supabase Free tier** cho PostgreSQL — 500MB database, 2GB bandwidth, đủ cho MVP vài trăm restaurants
  - **Không dùng paid monitoring** (Datadog, Sentry paid) — Vercel Analytics free + console logs đủ cho giai đoạn đầu
  - **Stripe test mode** cho payment — không cần production Stripe account cho MVP
  - **Không dùng email service paid** — Resend free tier (100 emails/day) đủ cho auth emails
  - **Self-host khi nào:** Khi monthly Vercel bill vượt quá $100-200 (tương đương 1-2 ngày engineer time setup infrastructure), hoặc khi cần features Vercel không có (custom caching, on-premise compliance)

- **Trade-off của việc cắt giảm:**
  - Không có advanced monitoring → bugs khó detect, nhưng với user base nhỏ, manual testing + user feedback đủ
  - Supabase free tier limits → nếu grow nhanh, cần upgrade hoặc migrate DB — nhưng đó là good problem to have
  - Không có CDN custom → Vercel Edge Network đủ cho global delivery ở scale MVP

<details>
<summary><strong>Demo Code Structure (MVP nhưng chuẩn bị cho Mobile App từ đầu)</strong></summary>

Thay vì để business logic nằm trong UI hoặc Server Actions:

- ❌ **BAD** : Page Server → Action → Prisma

- ✅ **GOOD** : Page Server → API Handler → Business Logic → Prisma

Tổ chức code như sau:

```text
src/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── menus/
│   │       │   └── route.ts
│   │       └── orders/
│   │           └── route.ts
│   ├── dashboard/
│   │   └── page.tsx
│   └── orders/
│       └── actions.ts
│
├── lib/
│   ├── api/
│   │   ├── menus.ts
│   │   ├── orders.ts
│   │   └── auth.ts
│   │
│   ├── db.ts
│   └── types/
│
└── prisma/
    └── schema.prisma
```

Business logic:

```ts
// lib/api/orders.ts

import { prisma } from '@/lib/db';

export async function createOrder(input: {
  restaurantId: string;
  items: {
    menuItemId: string;
    quantity: number;
  }[];
}) {
  return prisma.order.create({
    data: {
      restaurantId: input.restaurantId,
      items: {
        create: input.items,
      },
    },
  });
}
```

REST API:

```ts
// app/api/v1/orders/route.ts

import { createOrder } from '@/lib/api/orders';

export async function POST(req: Request) {
  const body = await req.json();

  const order = await createOrder(body);

  return Response.json(order);
}
```

Server Action chỉ là wrapper:

```ts
// app/orders/actions.ts

'use server';

import { createOrder } from '@/lib/api/orders';

export async function createOrderAction(formData: FormData) {
  return createOrder({
    restaurantId: String(formData.get('restaurantId')),
    items: [],
  });
}
```

Web UI:

```tsx
// app/dashboard/page.tsx

import { createOrderAction } from '../orders/actions';

export default function Dashboard() {
  return (
    <form action={createOrderAction}>
      <button>Create Order</button>
    </form>
  );
}
```

6–12 tháng sau khi có mobile app:

```text
Web
 ↓
Server Action
 ↓
lib/api/orders.ts
 ↓
PostgreSQL

------------------------------

Mobile
 ↓
POST /api/v1/orders
 ↓
lib/api/orders.ts
 ↓
PostgreSQL
```

Không cần rewrite business logic.

Mobile chỉ consume REST API đã tồn tại từ ngày đầu tiên.

</details>

</details>

---

### Exercise 3 — Job Description Analysis (Thực chiến)

Đọc job description dưới đây và phân tích đầy đủ:

> _"We're looking for a Senior Next.js Engineer for our fintech platform. Our stack: Next.js 15 (App Router), TypeScript, TailwindCSS. We have a separate backend team running NestJS microservices (8 services). You'll work on Next.js layer, consuming gRPC/REST APIs via our API gateway. We care deeply about performance (our users refresh dashboards every 30 seconds) and real-time data (WebSocket streams for market data). Auth is handled by our identity service (OAuth2/JWT). We're on AWS, using ECS Fargate + ALB, deploying via GitHub Actions. Team: 4 frontend engineers."_

**Phân tích:**

1. Pattern nào? Dấu hiệu nào trong JD confirm điều này?
2. Day-to-day của bạn sẽ trông như thế nào?
3. 3 technical challenges chính của role này là gì?
4. Câu hỏi nào bạn sẽ hỏi trong interview để hiểu rõ hơn về technical stack?
5. Skills nào bạn cần mà bài này chưa đề cập đến?

<details>
<summary>Đáp án</summary>

**1. Pattern: Pattern C — BFF (Backend for Frontend) với yếu tố Pattern B**

Dấu hiệu xác nhận:

- `"separate backend team running NestJS microservices (8 services)"` → Backend riêng biệt, không phải monolith trong Next.js
- `"consuming gRPC/REST APIs via our API gateway"` → Next.js không gọi trực tiếp microservices mà qua API gateway — đây là aggregation/orchestration layer
- `"API gateway"` → Gateway pattern thường đi kèm BFF; Next.js layer có thể là BFF hoặc consumer của gateway
- `"8 services"` → Đủ nhiều services để cần aggregation; dashboard cần data từ nhiều nguồn
- `"identity service (OAuth2/JWT)"` → Auth externalized, Next.js chỉ handle token management
- `"ECS Fargate + ALB, not Vercel"` → Self-hosted, Docker-based — không dùng Vercel serverless features

Tại sao không phải Pattern B thuần:

- Pattern B thuần: Next.js chỉ là presentation layer, proxy/pass-through requests
- Ở đây có `"performance (users refresh dashboards every 30 seconds)"` + `"real-time data"` → Next.js layer likely cần caching, data transformation, WebSocket management — đây là BFF responsibilities, không phải pure frontend

**2. Day-to-day của role:**

- **60% UI development:** Build dashboard components với real-time data (market charts, portfolio tables, transaction feeds) — dùng Tailwind + shadcn hoặc tương đương
- **20% API integration:** Viết typed clients cho gRPC/REST services, handle data transformation từ microservices format → UI format, error handling cho distributed systems
- **15% Performance optimization:** Implement caching strategies (React Server Components cache, SWR/React Query stale-while-revalidate), optimize re-renders cho 30-second refresh cycles, WebSocket connection management
- **5% Infrastructure/DevOps:** GitHub Actions workflows, ECS deployment troubleshooting, ALB routing config, monitoring dashboards

Ví dụ một task thực tế:

> "Build portfolio dashboard page: gọi `/api/gateway/portfolio` (aggregate từ `user-service`, `holding-service`, `market-data-service`), transform response thành chart data format, implement auto-refresh every 30s với SWR + fallback to cached data, handle WebSocket stream cho real-time price updates."

**3. 3 technical challenges chính:**

- **Challenge 1: Distributed auth + token management**
  - OAuth2 flow với identity service: handle authorization code → access token + refresh token
  - Token refresh không block UI (background refresh trước khi expire)
  - WebSocket connections cần auth — gửi JWT qua connection handshake, handle re-auth khi token refresh
  - CORS và secure cookie handling giữa Next.js (ECS) và identity service

- **Challenge 2: Real-time data + performance ở scale**
  - WebSocket streams cho market data: connection pooling, reconnection logic, message deduplication
  - Dashboard refresh every 30s nhưng users không muốn see loading state → implement optimistic UI + stale-while-revalidate
  - Memory leak risk: WebSocket subscriptions không cleanup khi component unmount → dashboard chạy 8 tiếng liên tục sẽ crash
  - ECS Fargate = container restart → WebSocket state lost → cần reconnect + replay logic

- **Challenge 3: Microservices failure resilience**
  - 8 services = nhiều points of failure. Dashboard cần data từ 3-4 services → một service down không nên crash cả page
  - Implement circuit breaker pattern ở BFF layer: nếu `market-data-service` timeout, show cached data + warning banner thay vì error page
  - Partial data rendering: portfolio value có thể tính được ngay cả khi `news-service` down
  - Graceful degradation: real-time stream fallback sang polling nếu WebSocket server unavailable

**4. Câu hỏi phỏng vấn nên hỏi và đáp án tham khảo:**

- **"API gateway là internal BFF layer hay external product (Kong, AWS API Gateway)? Next.js layer gọi gateway trực tiếp hay qua internal network?"**
  - Đáp án mong đợi: Gateway là NestJS service internal, Next.js gọi qua internal VPC (không qua internet). Quan trọng để biết latency và security model.

* **"Dashboard data được cache ở đâu — React Query/SWR client-side, Next.js server cache, hay Redis ở gateway layer?"**
  - Đáp án mong đợi: **Multi-layer caching** — React Query/SWR dùng _stale-while-revalidate_ ở client để tối ưu UX và giảm loading flicker. Ở server layer, Next.js vẫn có thể dùng **`unstable_cache` hoặc fetch cache (revalidate, tags)** để cache dữ liệu theo request/SSR, dù hiện tại **Next.js 16 đang khuyến nghị `use cache` + Cache Components thay thế dần `unstable_cache`** (tuy nhiên `unstable_cache` vẫn dùng được trong nhiều codebase production hiện tại). Ở infrastructure layer, Redis tại gateway (NestJS) dùng để cache cross-container và giảm load microservices (đặc biệt cho market data, portfolio aggregation). Quan trọng nhất là hiểu **cache invalidation cascade**: khi dữ liệu thay đổi (ví dụ sell stock), cần propagate invalidation từ Redis → Next.js cache (`revalidateTag` / invalidate cache boundary) → client cache (React Query invalidate query key), để đảm bảo consistency nhưng vẫn giữ performance cao.

- **"WebSocket streams từ đâu — gateway maintain WebSocket connection hay Next.js proxy đến dedicated WebSocket service?"**
  - Đáp án mong đợi: **WebSocket thường được tách ra thành một dedicated realtime service (ví dụ Socket.io / native WS server) hoặc một module realtime riêng trong backend, thay vì để Next.js xử lý.** Next.js App Router chủ yếu phục vụ request/response (SSR, RSC) nên không phù hợp để giữ long-lived connections. Vì vậy kiến trúc phổ biến là browser (qua Next.js UI) sẽ connect trực tiếp tới WebSocket service (hoặc qua ALB/NLB), còn gateway chỉ xử lý REST/gRPC aggregation và auth. WebSocket service sẽ chịu trách nhiệm maintain connection, fan-out market data stream, reconnect logic và scaling pub-sub. Quan trọng là tách biệt rõ HTTP layer (gateway/Next.js) và streaming layer (WebSocket service) để tránh overload gateway và đảm bảo khả năng scale realtime cho dữ liệu thị trường

- **"Team có 4 frontend engineers — ai làm BFF logic? Frontend team maintain API routes hay có platform/backend team hỗ trợ?"**
  - Đáp án mong đợi: Frontend team own Next.js layer bao gồm API routes (BFF). Backend team own microservices. Clear contract: OpenAPI/Swagger specs. Quan trọng để biết scope và on-call responsibility.

- **"Error handling strategy cho microservices — có standardized error format không? Frontend có retry logic hay gateway handle retry?"**
  - Đáp án mong đợi: Gateway trả về unified error format (RFC 7807 Problem Details hoặc custom). Frontend có exponential backoff retry cho idempotent requests. Non-idempotent (POST orders) không retry tự động.

- **"Monitoring và alerting — có distributed tracing (OpenTelemetry, Jaeger) không? Frontend có visibility vào backend latency không?"**
  - Đáp án mong đợi: Có tracing cross-service (request ID propagate từ Next.js → gateway → microservices). Frontend dùng Datadog/New Relic hoặc CloudWatch. Biết latency bottleneck ở layer nào.

**5. Skills cần mà bài này chưa đề cập:**

- **gRPC client development:** `protobuf` types, gRPC-Web hoặc gRPC over HTTP/2, handling streaming responses. JD mention gRPC nhưng bài chỉ cover REST patterns.
- **WebSocket/Socket.io production:** Connection management, heartbeat, reconnection with exponential backoff, subscription multiplexing, horizontal scaling với Redis adapter.
- **Financial domain knowledge:** Understanding of portfolio valuation, P&L calculation, market data tick formats, regulatory requirements (nếu fintech có compliance constraints).
- **Advanced React performance:** `useDeferredValue`, `useTransition`, concurrent features, virtualized lists cho large datasets (10K+ transactions), canvas/WebGL charts (TradingView, D3).
- **AWS specifics:** ECS service discovery, ALB path-based routing, CloudWatch logs/metrics, IAM roles cho service-to-service auth, Secrets Manager.
- **Security:** XSS prevention với dynamic market data (user-generated content risk), CSRF protection cho state-changing operations, Content Security Policy cho financial apps.
- **Testing distributed systems:** Mocking microservices (MSW, WireMock), contract testing (Pact), E2E với real-time data (Cypress/Playwright với WebSocket intercept).
</details>

## 13. Summary

### Bản Đồ 5 Patterns

```
Pattern A — Full-Stack Monolith
  Nhận diện:  prisma/ + actions/ + Vercel deployment
  Tốt cho:    Startup, MVP, small team
  Risk:       DB connections, không reusable API

Pattern B — Decoupled Frontend
  Nhận diện:  lib/api/client.ts, KHÔNG có prisma/
  Tốt cho:    Công ty có existing backend, enterprise
  Risk:       Type duplication, coordination overhead

Pattern C — BFF
  Nhận diện:  /services/*.ts, Promise.all trong API routes
  Tốt cho:    Microservices architecture
  Risk:       BFF là single point of failure

Pattern D — Content Platform
  Nhận diện:  generateStaticParams nhiều, CMS client, revalidate webhook
  Tốt cho:    Media, docs, marketing, blog
  Risk:       ISR invalidation complexity, multi-instance cache

Pattern E — Multi-tenant SaaS
  Nhận diện:  middleware.ts detect subdomain, tenant DB queries
  Tốt cho:    B2B SaaS, platform products
  Risk:       Data isolation, tenant cache pollution
```

### Câu Hỏi Phải Hỏi Khi Join Công Ty Mới

```
1. "Backend là gì? Team backend riêng hay full-stack?"
   → Xác định Pattern B/C (có backend riêng) vs A (monolith)

2. "Deploy như thế nào? Vercel hay Docker?"
   → Quyết định nhiều thứ: Middleware có dùng Edge không? ISR setup?

3. "Có mobile app không hay chỉ web?"
   → Pattern A có thể không đủ nếu mobile app đang planned

4. "Database access như thế nào?"
   → ORM nào? Connection pooling? (Quan trọng cho serverless)

5. "Migration plan từ Pages Router sang App Router đang ở giai đoạn nào?"
   → Biết mình sẽ làm việc với code era nào (bài M0.2)
```

### Điều Quan Trọng Nhất Của Bài Này

Tutorial dạy bạn xây **một** thứ.

Production là bạn phải hiểu **tại sao** thứ đó được xây theo cách đó, và **khi nào** nên xây theo cách khác.

Biết 5 patterns này không phải để bạn "chọn đúng pattern". Đó là để bạn **đọc được context** khi bước vào bất kỳ codebase nào — hiểu những quyết định đã được đưa ra, tại sao, và trade-offs nào đang được chấp nhận.

Đó là tư duy của engineer, không phải coder.

---

> **Bài tiếp theo:** M0.4 — Hiểu Lầm Phổ Biến: Những Quan Niệm Sai Lầm Phá Hoại Dự Án
